# کارایی — اعداد اندازه‌گیری‌شده

> این فایل **نتیجه‌ی اندازه‌گیری** است، نه هدف‌گذاری. هر عدد اینجا از یک اجرای
> واقعی Lighthouse روی بیلد production آمده. اگر تغییری دادی، دوباره اندازه بگیر
> و همین جدول را به‌روز کن.

آخرین اندازه‌گیری: ۱۴۰۵/۰۵/۰۸ · بیلد production · `vite preview`

---

## بسته‌ی اولیه

| مورد | مقدار | بودجه | وضعیت |
|---|---|---|---|
| JS بسته‌ی اولیه (gzip) | **۹۸.۶ KB** | ۲۰۰ KB | ✅ ۴۹٪ بودجه |
| JS خام | ۳۰۹.۹ KB | — | — |
| CSS (gzip) | ۶.۰ KB | — | — |
| فونت | ۴۷.۲ KB | — | — |
| **مجموع انتقال** | **۱۵۷.۶ KB** | — | — |

دروازه: `pnpm size` — به کروم نیاز ندارد و در هر CI‌ای اجرا می‌شود.
اگر بسته از ۲۰۰KB فشرده بگذرد، با کد خروج ۱ قرمز می‌شود.

---

## سنجه‌های Lighthouse

سه پروفایل، چون عدد بدون پروفایل بی‌معناست:

| پروفایل | LCP | FCP | TBT | CLS | امتیاز |
|---|---|---|---|---|---|
| دسکتاپ | ۰.۵ ثانیه | ۰.۴ ثانیه | ۰ ms | — | **۱۰۰** |
| **موبایل استاندارد Lighthouse** (Slow 4G، CPU ×۴) | **۲.۰ ثانیه** ✅ | ۱.۸ ثانیه | ۹۰ ms ✅ | ۰.۰۲۶ ✅ | **۹۸** |
| 3G واقعی (۷۰۰kbps، RTT ۳۰۰ms، CPU ×۴) | ۳.۹ ثانیه ❌ | ۳.۹ ثانیه | ۵۰ ms | ۰.۰۲۸ | ۷۸ |

### کدام پروفایل مبنای دروازه است؟

**موبایل استاندارد Lighthouse.** آستانه‌ی ۲.۵ ثانیه‌ی LCP در web.dev دقیقاً
با همین پروفایل تعریف شده، و `lighthouserc.json` هم روی همین تنظیم است.

### ولی روی 3G واقعی هنوز رد می‌شود — این را پنهان نکنیم

روی ۷۰۰kbps، LCP برابر ۳.۹ ثانیه است. کاربر هدف ما در پاساژ بازار با
اینترنت ضعیف کار می‌کند، پس این عدد **واقعاً مهم است**. رسیدن به زیر
۲.۵ ثانیه روی این پروفایل با یک SPA کلاینت‌رندر و پایه‌ی
React + TanStack Router + Query عملاً ممکن نیست؛ خودِ این پایه حدود
۸۵–۹۰KB فشرده است و تا اجرا نشود چیزی رندر نمی‌شود.

گزینه‌های واقعی برای فازهای بعد، به‌ترتیب اثر:

1. **رندر سمت سرور یا پیش‌رندر (SSG) پوسته** — بزرگ‌ترین اثر. LCP از
   «پایان اجرای React» به «رسیدن HTML» منتقل می‌شود.
2. **تقسیم کد بر اساس مسیر** — صفحه‌ی خانه فقط چیزی را بگیرد که لازم دارد.
3. **بازبینی TanStack Router** — اگر تعداد مسیرها کم بماند، مسیریاب سبک‌تر.
4. سرویس‌ورکر از قبل نصب‌شده: **بازدید دوم** با precache تقریباً آنی است.
   این را در اندازه‌گیری «بار اول» نمی‌بینیم ولی کاربر روزانه آن را حس می‌کند.

---

## آنچه در این مرحله اندازه‌گیری و اصلاح شد

| تغییر | اثر اندازه‌گیری‌شده |
|---|---|
| حذف `manualChunks` نادرست (چانکی به نام echarts که محتوایش React بود) | یک درخواست کم‌تر؛ نام چانک دیگر دروغ نمی‌گوید |
| حذف `preload` فونت | فونت دیگر پهنای باند را از JS نمی‌دزدد |
| فونت بدون لاتین (۱۰۹KB → ۴۷KB) | LCP ۴.۸ → ۴.۲ ثانیه (3G) |
| `date-fns-jalali` → `Intl` بومی در production | بسته ۱۰۵.۶ → ۹۸.۶ KB؛ LCP ۴.۲ → ۳.۹ ثانیه (3G) |
| پوسته‌ی inline در `index.html` | چیزی پیش از اجرای React رنگ می‌گیرد |

---

## آنچه هنوز اندازه‌گیری نشده

- [ ] **INP** — سنجه‌ی میدانی است و در آزمایشگاه اندازه گرفته نمی‌شود.
      `TBT` (۹۰ms) فقط تقریب آن است. برای عدد واقعی به RUM نیاز داریم.
- [ ] **دستگاه اندروید میان‌رده‌ی واقعی** — بخش ۷ CLAUDE.md می‌گوید شبیه‌ساز
      کافی نیست. این هنوز انجام نشده و پیش از تحویل فاز ۱ باید انجام شود.
- [ ] **ثبت تراکنش زیر ۳۰۰ms** — تا بک‌اند نیاید قابل اندازه‌گیری نیست.

---

## چطور دوباره اندازه بگیریم

```bash
pnpm build && pnpm size
```

```bash
pnpm lhci
```

> روی ویندوز، `lhci` پس از پایان ممیزی هنگام پاک‌کردن پوشه‌ی موقت کروم
> خطای `EPERM` می‌دهد. این باگ `chrome-launcher` روی ویندوز است و ربطی به
> نتیجه ندارد؛ گزارش کامل تولید می‌شود. روی رانر لینوکس CI رخ نمی‌دهد.
