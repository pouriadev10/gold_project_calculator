# AGENTS.md — قواعد ثابت پروژه

نرم‌افزار حسابداری طلا، نقره و سکه. این فایل را پیش از هر تسک بخوان. قواعد این فایل بر هر تصمیم دیگری اولویت دارند.

---

## ۱. محصول در یک پاراگراف

صنف طلا واحد پایه‌ی حسابداری‌اش ریال نیست، **طلا** است. کاربر می‌گوید «این ماه ۱۰۰ گرم سود کردم» و «فلانی ۴۰ گرم بدهکار است» — تا از نوسانات بازار جا نماند. ریال واحد گذراست. تمام نرم‌افزارهای رقیب عکس این را فرض می‌کنند. **این وارونگی کل گزاره‌ی ارزش ماست** — نه رابط کاربری بهتر، نه ویژگی بیشتر. هر تصمیم فنی که این را تضعیف کند، غلط است.

**پرسونای فاز ۱:** خرده‌فروشی که هم زیورآلات و هم سکه می‌فروشد و از مصرف‌کننده طلا می‌خرد.

**مخاطبان کل سیستم (فازهای بعد):** بازاریاب · بنکدار · کارخانه‌ی تولید شمش · **کارخانه و تولیدی ساخت زیورآلات** · معامله‌گر سکه و شمش · معامله‌گر آبشده.

---

## ۲. قواعد غیرقابل مذاکره

این‌ها را هرگز نقض نکن. اگر تسکی ظاهراً نقضشان را می‌طلبد، تسک را متوقف کن و بپرس.

### ۲-۱. دقت عددی

- **هیچ‌جا `number` شناور برای پول یا وزن استفاده نمی‌شود.** نه در بک‌اند، نه در فرانت‌اند، نه در JSON.
- ریال → `bigint`، عدد صحیح، بدون اعشار.
- وزن → `bigint`، واحد **میلی‌گرم طلای خالص ۱۰۰۰** (یا نقره‌ی خالص ۱۰۰۰). استثنا: فقط مشخصات مرجع `coin_type_versions.gross_weight_ug` با `bigint` میکروگرم ذخیره می‌شود تا وزن رسمی سکه بدون گردکردن بماند؛ مانده‌ی دفترکل سکه همچنان فقط تعداد صحیح است.
- سکه → `integer`، **تعداد**. هرگز به گرم تبدیل نمی‌شود در لایه‌ی ذخیره‌سازی.
- عیار → `integer` بین ۱ و ۱۰۰۰ (مثلاً `750`, `995`, `925`).
- در JSON، هر مقدار پولی یا وزنی به‌صورت **رشته** منتقل می‌شود، نه `number`. (`"amountRial": "12500000"`)
- تمام گرد کردن‌ها فقط و فقط در `packages/core-calc/src/rounding.ts` انجام می‌شود. هیچ `Math.round` دیگری در کدبیس مجاز نیست — این را با قاعده‌ی ESLint اجبار کن.

### ۲-۲. سکه یک شیء است، نه یک وزن

سکه حباب دارد و حباب مستقل از قیمت طلا نوسان می‌کند. اگر ۵ سکه را به‌صورت ۳۶.۶ گرم ذخیره کنی، موقعیت حبابی کاربر ناپدید می‌شود و گزارش سود غلط درمی‌آید.

- هر نوع سکه یک بُعد شمارشی مستقل در دفتر کل است.
- تبدیل سکه به گرم فقط یک **نمای گزارشی** است، نه روش ذخیره‌سازی.

### ۲-۳. قانون حباب 🔒

**فقط و فقط سکه‌های ضرب بانک مرکزی حباب دارند.**

**هیچ شمشی در هیچ حالتی حباب ندارد** — داخلی یا خارجی، طلا یا نقره، برنددار یا بی‌برند، هر عیاری. سیستم به‌هیچ‌وجه نباید برای شمش حباب محاسبه، ذخیره یا نمایش دهد.

```
قیمت شمش = وزن × عیار ÷ ۱۰۰۰ × نرخ گرم ۱۰۰۰      ← تمام. بدون پرمیوم.
```

از نظر پیاده‌سازی: تابع `bubble()` فقط ورودی از نوع `CoinType` می‌پذیرد. اگر امضای تابع اجازه دهد شمش به آن پاس داده شود، امضا غلط است — نه فراخوانی.

### ۲-۴. کلید تعویض واحد روی همه‌ی گزارش‌ها

هر جایی که عدد مالی نشان داده می‌شود باید با یک کلیک بین **طلا** و **ریال** جابه‌جا شود — نه فقط صفحه‌ی گزارش سود:

سود · زیان · درآمد · مانده‌حساب · فهرست بدهکاران · فهرست بستانکاران · صورت‌حساب · داشبورد

انتخاب واحد یک **تنظیم سراسری کاربر** است (پیش‌فرض: طلا) که در هر صفحه هم قابل تغییر لحظه‌ای است. اگر صفحه‌ای عدد مالی دارد و کلید ندارد، ناقص است.

### ۲-۵. مرجوعی دو حالت کاملاً متفاوت دارد

**این دو را با یک ماژول مشترک پیاده نکن.** از نظر حسابداری، اولی برگشت است و دومی اصلاً برگشت نیست — یک خرید است.

| | B2B (همکار) | B2C (مصرف‌کننده) |
|---|---|---|
| ماهیت | برگشت | **خرید طلای دست‌دوم** |
| جریمه | **هیچ.** نه وزنی، نه اجرتی، نه ریالی | دارد |
| نرخ | بی‌ربط — وزن به وزن برمی‌گردد | مظنه‌ی **روز مرجوعی** |
| عیار | عیار اصلی کالا | **۷۴۰** (پیش‌فرض قابل تنظیم) |
| اجرت | باطل می‌شود | **کاملاً سوخت می‌شود** |
| مقصد کالا | موجودی مصنوعات | موجودی **آبشده** |
| ماژول | `sales/returns` | `purchase/second-hand` |

سیستم بر اساس **نوع طرف حساب** حالت را خودکار انتخاب می‌کند و به کاربر نشان می‌دهد کدام فعال است.

در حالت B2C، رابط کاربری باید اختلاف را صریح نشان دهد: مبلغ خرید اولیه، مبلغ پرداختی امروز، و منشأ تفاوت (اجرت سوخته + اختلاف عیار + تغییر مظنه). مبلغ کمتر یا بیشتر از خرید اولیه، هر دو طبیعی است — هشدار خطا نده.

### ۲-۶. هیچ عدد صنفی در کد هاردکد نمی‌شود

نرخ مالیات، عیار مبنا، عیار خرید دست‌دوم، وزن و عیار سکه، قواعد اجرت — همه **رکورد دیتابیسی نسخه‌دار با بازه‌ی اعتبار** هستند.

> معیار: فاکتور دو ماه پیش باید امروز دقیقاً همان اعداد را بازتولید کند، حتی اگر تنظیمات از آن زمان تغییر کرده باشد.

### ۲-۷. دفتر کل فقط Append-Only است

- هیچ رکورد `ledger_entry` هرگز `UPDATE` یا `DELETE` نمی‌شود.
- اصلاح = سند جدید، نه ویرایش سند قدیم.
- **ناوردا:** برای هر `transaction_id`، مجموع اثر در **هر بُعد** باید صفر باشد. این را با `CONSTRAINT TRIGGER ... DEFERRABLE INITIALLY DEFERRED` در دیتابیس اجبار کن، نه در کد اپلیکیشن.

### ۲-۸. نرخ در لحظه‌ی رویداد قفل می‌شود

هر سندی که تبدیل ریال↔طلا دارد، **مظنه‌ی همان لحظه را به‌عنوان فیلد ثابت در سند ذخیره می‌کند**. گزارش‌های گذشته هرگز با تغییر قیمت امروز عوض نمی‌شوند. اگر گزارش دیروز فردا عدد دیگری بدهد، کاربر اعتماد را می‌بازد و برنمی‌گردد.

### ۲-۹. ضدالگوها

| نکن | چرا |
|---|---|
| `float` / `double` / `number` برای پول یا وزن | مغایرت ریالی تضمین‌شده |
| نرمال‌کردن سکه به گرم در دیتابیس | حباب و موقعیت سکه‌ای گم می‌شود |
| محاسبه‌ی حباب برای شمش | شمش حباب ندارد. هرگز. |
| یک ماژول مشترک برای مرجوعی B2B و B2C | یکی از آن دو همیشه غلط محاسبه می‌شود |
| کلید تعویض واحد فقط روی صفحه‌ی سود | گزاره‌ی ارزش اصلی را نصفه رها می‌کند |
| MongoDB یا NoSQL برای دفتر کل | تراکنش ACID چندسندی نیاز قطعی است |
| میکروسرویس | تیم ۳ نفره را فلج می‌کند |
| GraphQL | هزینه بدون منفعت در این دامنه |
| CRDT یا ادغام خودکار داده‌ی مالی | فاجعه |
| Kubernetes | تا پیش از ۵۰ مستأجر، هزینه‌ی بی‌بازده |
| `SEQUENCE` برای شماره‌ی فاکتور | در rollback شکاف می‌سازد |
| طراحی دسکتاپ و بعد «ریسپانسیو کردن» | نتیجه‌اش جدول فشرده و دکمه‌ی ریز است |
| تکیه بر کیبورد سیستم‌عامل برای ورود اعداد | هدف ۱۵ ثانیه محقق نمی‌شود |
| اضافه‌کردن ویژگی خارج از دامنه‌ی فاز ۱ | زمان نداریم؛ بخش ۵ را ببین |

---

## ۳. فرمول‌های دامنه

همه در `packages/core-calc` به‌صورت توابع خالص، با تست، و بدون وابستگی به I/O.

```
مثقال                 = ۴.۶۰۸۳ گرم
عیار پایه‌ی مظنه      = ۷۰۵   (طلای آبشده‌ی ۱۷ عیار — قرارداد بازار)
ثابت تبدیل            = ۴.۶۰۸۳ × ۷۰۵ = ۳۲۴۸.۸۵۱۵

وزن خالص              = وزن ناخالص × عیار ÷ ۱۰۰۰

نرخ هر گرم عیار k     = مظنه × k ÷ ۳۲۴۸.۸۵۱۵
  → عیار ۷۵۰:         مظنه ÷ ۴.۳۳۱۸

قیمت مصنوع            = (وزن × نرخ گرم عیار کالا) + اجرت + سود + مالیات

ارزش ذاتی سکه         = وزن سکه × عیار سکه ÷ ۱۰۰۰ × نرخ گرم ۱۰۰۰
حباب سکه              = قیمت بازار سکه − ارزش ذاتی    ← فقط سکه‌ی ضرب بانک مرکزی

قیمت شمش              = وزن × عیار ÷ ۱۰۰۰ × نرخ گرم ۱۰۰۰
                        ← بدون حباب، بدون پرمیوم، بدون استثنا

مبلغ خرید دست‌دوم      = وزن خالص × نرخ گرم عیار۷۴۰ در تاریخ مرجوعی
                        ← اجرت پرداختی مشتری سوخت می‌شود
```

> ⚠️ فرمول `(مظنه ÷ ۴.۶۰۸۳) × (عیار ÷ ۷۵۰)` **غلط است** — عیار پایه‌ی مظنه را ۱۰۰۰ فرض می‌کند در حالی که ۷۰۵ است. حدود ۶٪ خطا روی هر فاکتور. اگر جایی در کدبیس دیدی، باگ است.

**مقادیر پیش‌فرض (در دیتابیس، نه در کد):**

| مورد | مقدار |
|---|---|
| سکه تمام بهار آزادی (طرح جدید) | ۸.۱۳۳ گرم، عیار ۹۰۰ → ۷.۳۱۹۷ گرم خالص |
| نیم سکه | ۴.۰۶۶۵ گرم، عیار ۹۰۰ → ۳.۶۵۹۹ گرم |
| ربع سکه | ۲.۰۳۳۲ گرم، عیار ۹۰۰ → ۱.۸۲۹۹ گرم |
| سکه گرمی | ۱.۰۱۶۶ گرم، عیار ۹۰۰ → ۰.۹۱۴۹ گرم |
| عیار خرید از مصرف‌کننده | پیش‌فرض ۷۴۰ — **قابل تنظیم هر مستأجر** |
| عیارهای نقره | ۹۲۵، ۹۹۹ |
| عیارهای شمش طلا | ۷۵۰ تا ۹۹۵ |

**سیاست گرد کردن:** محاسبات میانی هرگز گرد نمی‌شوند. گرد کردن فقط یک بار، در لحظه‌ی نهایی‌شدن مبلغ فاکتور، با سیاست `roundHalfUp` و واحد گرد کردن قابل تنظیم (پیش‌فرض: ۱۰۰۰ ریال).

---

## ۴. استک و ساختار

```
apps/
├── api/                      NestJS · Node 22 · مونولیت ماژولار
│   └── src/modules/
│       ├── ledger/           دفتر چندواحدی — قلب سیستم
│       ├── pricing/          مظنه، عیار، اجرت، مالیات
│       ├── inventory/        کالا، آبشده، سکه
│       ├── sales/            فروش، فاکتور، مرجوعی، اصلاح
│       ├── purchase/         خرید از مصرف‌کننده
│       ├── parties/          اشخاص و مانده‌ها
│       ├── reporting/        گزارش سود دو مقیاسه
│       ├── tax/              ⬜ فاز ۵ — فقط اسکلت خالی
│       └── platform/         auth, tenant, audit, files
└── web/                      React 18 · Vite · PWA · موبایل‌فرست

packages/
└── core-calc/                توابع خالص، مشترک سرور و مرورگر
```

| لایه | انتخاب |
|---|---|
| زبان | TypeScript سراسری |
| بک‌اند | NestJS روی Node 22 |
| دیتابیس | PostgreSQL 16 |
| ORM | Drizzle (SQL-first) |
| صف | BullMQ + Redis |
| فرانت‌اند | React 18 + Vite + TanStack Router/Query/Table |
| فرم | react-hook-form + zod (اسکیمای مشترک با بک‌اند) |
| UI | Tailwind + shadcn/ui |
| **هوش طراحی** | **اسکیل `ui-ux-pro-max`** — بخش ۴-۱ |
| تاریخ | date-fns-jalali |
| نمودار | Apache ECharts |
| PWA | Workbox (vite-plugin-pwa) |
| استقرار | Docker Compose |

### ۴-۱. اسکیل `ui-ux-pro-max`

منبع: <https://github.com/nextlevelbuilder/ui-ux-pro-max-skill> (MIT)

**نصب** — روش CLI توصیه می‌شود؛ نصب از طریق مارکت‌پلیس در نسخه‌های پیش از ۲.۵.۱ باگ symlink داشت:

```bash
npm install -g ui-ux-pro-max-cli
cd <ریشه‌ی پروژه>
uipro init --ai Codex          # در .Codex/skills/ui-ux-pro-max نصب می‌شود
```

پیش‌نیاز: **Python 3.x**. با توجه به محدودیت‌های شبکه‌ی داخل ایران، نصب را از میرور npm داخلی انجام بده و پوشه‌ی اسکیل را در ریپو کامیت کن تا بقیه‌ی تیم دوباره دانلود نکنند.

**تولید سیستم طراحی پروژه (یک بار، در ابتدای کار):**

```bash
python3 .Codex/skills/ui-ux-pro-max/scripts/search.py \
  "gold jewelry retail POS accounting financial dashboard invoice billing" \
  --design-system --persist -p "GoldAccounting" --stack react
```

خروجی در `design-system/MASTER.md` ذخیره می‌شود. برای صفحات خاص:

```bash
... --persist -p "GoldAccounting" --page "sales"   # design-system/pages/sales.md
```

**روال استفاده در هر تسک فرانت‌اند:**

1. `design-system/pages/<نام-صفحه>.md` را بخوان. اگر نبود، `design-system/MASTER.md`.
2. کد را بر اساس آن بزن.
3. پیش از تیک زدن تسک، چک‌لیست pre-delivery اسکیل را اجرا کن (بدون ایموجی به‌جای آیکون، `cursor-pointer` روی عناصر کلیک‌پذیر، حالت focus برای کیبورد، احترام به `prefers-reduced-motion`، کنتراست ۴.۵:۱).

> ### ⚠️ ترتیب اولویت هنگام تعارض
>
> **قواعد بخش ۲ و ۶ همین فایل بر هر توصیه‌ی `ui-ux-pro-max` اولویت دارند.**
>
> این اسکیل عمدتاً برای محصولات LTR و صفحات بازاریابی تنظیم شده. جاهایی که تعارض پیش می‌آید و **قاعده‌ی ما برنده است**:
>
> | توصیه‌ی احتمالی اسکیل | قاعده‌ی ما |
> |---|---|
> | Glassmorphism / Aurora / گرادیان مش | ممنوع — روی اندروید ارزان بازار کند است و بودجه‌ی ۲۰۰KB را می‌شکند |
> | انیمیشن و micro-interaction فراوان | حداقلی. INP < ۲۰۰ms مقدم است. |
> | breakpoint شروع از دسکتاپ | شروع از **۳۶۰px** |
> | `margin-left` / `padding-right` | فقط خاصیت‌های منطقی (`ms-`/`me-`/`ps-`/`pe-`) |
> | فونت‌های گوگل لاتین | **وزیرمتن**، با Tabular Numbers |
> | تکیه بر hover | ممنوع — این محصول لمسی است |
> | کیبورد سیستمی برای ورودی عددی | کیپد سفارشی ما (بخش ۶) |
>
> نوع محصول درست برای پرس‌وجو از اسکیل: **Financial Dashboard / Invoice & Billing Tool**، نه Luxury E-commerce. اینجا یک ابزار کاری روزانه‌ی پرتراکم است، نه یک برند لوکس.

**چرا TypeScript سراسری:** موتور محاسبات باید *عیناً* یک نتیجه را روی سرور و مرورگر بدهد. یک زبان یعنی یک پیاده‌سازی، یک مجموعه تست، یک حقیقت.

---

## ۵. دامنه‌ی فاز ۱ — این خط را رد نکن

**داخل دامنه:** دفتر چندواحدی · موتور محاسبات · فروش زیورآلات · فروش و خرید سکه · خرید از مصرف‌کننده · **مرجوعی B2C (به‌عنوان خرید دست‌دوم)** · حساب اشخاص · موجودی ساده · مظنه (دستی + فید) · **کلید تعویض واحد روی همه‌ی گزارش‌ها و مانده‌ها** · اصلاح فاکتور با پنجره‌ی زمانی

**خارج از دامنه — حتی اگر ساده به نظر برسد:** مرجوعی B2B · آفلاین · بازاریاب · پورسانت · چندشعبه‌ای · امانی · نقره · شمش · کارخانه · تولیدی زیورآلات · شبکه‌ی تسویه · سامانه مؤدیان · باشگاه مشتریان · یکپارچگی سخت‌افزار · RFID · چک و نسیه‌ی پیچیده

> اگر تسکی تو را به سمت موارد ستون دوم برد، **متوقف شو و بپرس.** یک ماه زمان داریم.

**اما این‌ها را از روز اول رعایت کن، چون بعداً افزودنشان پرهزینه است:**

- شماره‌گذاری فاکتور **بدون شکاف (gapless)** — با جدول شمارنده + `SELECT ... FOR UPDATE`.
- `tenant_id` روی همه‌ی جداول + Row-Level Security پستگرس.
- `Idempotency-Key` اجباری روی هر endpoint نوشتنی.
- مدل «طرف حساب» طوری باشد که بتواند بعداً **یک مستأجر دیگر روی پلتفرم** باشد، نه فقط رکورد محلی.
- فیلدهای هویتی مشتری (کد ملی) در مدل داده، اختیاری.
- دفتر کل از روز اول **چندواحدی** باشد (ریال، طلا، نقره، انواع سکه) حتی اگر فاز ۱ فقط طلا و سکه را استفاده کند.
- ماژول خالی `tax/` و یک لایه‌ی انتزاعی `InvoiceExporter` که فعلاً فقط PDF می‌سازد.

---

## ۶. قواعد رابط کاربری

- **موبایل‌فرست یعنی از عرض ۳۶۰ پیکسل شروع کن**، نه اینکه دسکتاپ را کوچک کنی. نقاط شکست: `640 / 1024 / 1440`.
- **صفحه‌کلید عددی سفارشی درون‌برنامه‌ای** با کلیدهای میان‌بر دامنه‌ای (`۷۵۰`، `۹۹۵`، `۷۴۰`، `۹۲۵`، `گرم↔مثقال`). هدف ثبت زیر ۱۵ ثانیه بدون این محقق نمی‌شود.
- هدف لمسی حداقل ۴۴×۴۴ پیکسل، بدون استثنا.
- اقدام اصلی هر صفحه در یک‌سوم پایین صفحه (منطقه‌ی شست). هرگز بالای صفحه.
- زیر ۶۴۰px جدول وجود ندارد → فهرست کارتی با ۳ تا ۴ فیلد کلیدی.
- زیر ۶۴۰px همه‌ی مودال‌ها Bottom Sheet تمام‌صفحه می‌شوند.
- هیچ اطلاعات یا اقدامی نباید فقط با hover در دسترس باشد.
- **RTL:** خاصیت‌های منطقی CSS (`ps-/pe-/ms-/me-`) به‌جای `left/right`.
- اعداد جدول‌ها حتماً **Tabular Numbers**، وگرنه ستون ارقام موقع تغییر می‌لرزد.
- نمایش با `Intl.NumberFormat('fa-IR')`، اما **مقدار ذخیره‌شده همیشه لاتین و صحیح**.
- **نرمال‌سازی متن فارسی هنگام نوشتن در دیتابیس:** `ی/ي`، `ک/ك`، ارقام عربی/فارسی/لاتین، نیم‌فاصله. بدون این، جست‌وجوی نام مشتری هرگز درست کار نمی‌کند.
- `inputMode="decimal"` روی فیلدهای عددی؛ `autocorrect` و `autocapitalize` روی فیلدهای کد کالا خاموش.

**بودجه‌ی کارایی (الزام، نه آرزو):** بسته‌ی اولیه‌ی JS زیر ۲۰۰KB فشرده · LCP روی 3G شبیه‌سازی‌شده زیر ۲.۵ ثانیه · INP زیر ۲۰۰ms · ثبت تراکنش زیر ۳۰۰ میلی‌ثانیه.

---

## ۷. تست

- **Property-based روی دفتر کل:** برای هر توالی تصادفی از تراکنش‌ها، ناوردای مجموع صفر در **همه‌ی ابعاد** باید برقرار بماند. با `fast-check`.
- **Golden tests روی موتور قیمت‌گذاری:** مجموعه‌ای از فاکتورهای واقعی با نتیجه‌ی دستی‌محاسبه‌شده به‌عنوان مرجع ثابت. این فایل‌ها بدون تأیید صریح تغییر نمی‌کنند.
- پوشش تست `packages/core-calc` باید **۱۰۰٪ خطوط** باشد. بقیه‌ی کدبیس هدف ندارد.
- تست روی حداقل یک دستگاه اندروید میان‌رده‌ی واقعی. شبیه‌ساز کافی نیست.

---

## ۸. روال کار با کلاد کد

1. `TASKS.md` را باز کن، **اولین تسک تیک‌نخورده** را بردار. تسک‌ها ترتیب دارند.
2. اگر تسک بیش از یک نشست طول می‌کشد، اول آن را به تسک‌های کوچک‌تر بشکن و در `TASKS.md` بنویس.
3. **اگر تسک فرانت‌اند است:** پیش از کد زدن، `design-system/pages/<صفحه>.md` و در نبودش `design-system/MASTER.md` را بخوان (بخش ۴-۱).
4. کد بزن. تست بنویس. `pnpm test` و `pnpm typecheck` باید سبز باشند.
5. **معیار «تمام است وقتی» را در خود تسک بخوان و واقعاً بررسی کن.** تیک زدن بدون بررسی ممنوع.
6. تیک بزن، کامیت کن با پیام `[T-xx] عنوان تسک`.
7. اگر چیزی خلاف قواعد این فایل لازم شد، **کد نزن — بپرس.**

### ۸-۱. حافظه‌ی Graphify

- در پرسش‌های مربوط به ساختار، وابستگی‌ها یا محل تغییر کد، ابتدا از `graphify-out/graph.json` پرس‌وجو کن؛ فقط سپس فایل‌های مرتبط را باز کن.
- بعد از تکمیل هر تسک و پیش از تحویل، برای همگام‌سازی کامل (به‌ویژه تغییرات مستندات و تصویر) `graphify . --update` را اجرا کن.
- نتیجه‌ی مفید هر تسک را با `graphify save-result` و `--outcome useful` در حافظه‌ی Graphify ذخیره کن: خلاصه‌ی تغییر، فایل‌ها/گره‌های اصلی، و تست‌های اجراشده. برای نتیجه‌ی نامعتبر یا مسیر بی‌ثمر به‌ترتیب از `--outcome corrected` یا `--outcome dead_end` استفاده کن.
- ابتدای هر نشست، `graphify reflect --if-stale` را اجرا و `graphify-out/reflections/LESSONS.md` را بخوان تا پاسخ‌های مفید قبلی دوباره مصرف شوند.

**قواعد کامیت:** یک تسک، یک کامیت. بدون کامیت «WIP». بدون تغییر فایل‌های خارج از دامنه‌ی تسک.

**Definition of Done برای هر تسک:**

- [ ] معیار «تمام است وقتی» برآورده شده
- [ ] `pnpm typecheck` سبز
- [ ] `pnpm test` سبز
- [ ] هیچ `any`، `@ts-ignore` یا `Math.round` جدیدی اضافه نشده
- [ ] هیچ عدد صنفی هاردکد نشده
- [ ] چک‌باکس در `TASKS.md` تیک خورده

## Imported Claude Cowork project instructions
