-- BE-010 — داده‌ی مرجع نقش‌ها و اعمال RLS روی عضویت‌ها.

-- ── نقش‌های سیستمی ─────────────────────────────────────────────────
-- داده‌ی مرجع است، نه عدد صنفی: قاعده‌ی ۲-۶ درباره‌ی نرخ مالیات، عیار و
-- مشخصات سکه است — چیزهایی که با گذشت زمان عوض می‌شوند و فاکتور قدیمی
-- باید با مقدار همان روز بازتولید شود. نقش‌ها ساختار مجوزدهی‌اند و
-- نسخه‌بندی زمانی برایشان بی‌معناست.
--
-- `ON CONFLICT DO NOTHING` این مهاجرت را برای اجرای دوباره امن می‌کند.
INSERT INTO roles (code, title, sort_order) VALUES
  ('OWNER',   'مالک',       1),
  ('MANAGER', 'مدیر',       2),
  ('CASHIER', 'صندوق‌دار',  3)
ON CONFLICT (code) DO NOTHING;
--> statement-breakpoint

-- ── جداسازی مستأجر روی عضویت‌ها ────────────────────────────────────
-- `tenant_memberships` داده‌ی مستأجر است: فهرست کارکنان یک طلافروشی
-- نباید برای طلافروشی دیگر قابل خواندن باشد. helper ای که در BE-009
-- ساخته شد اینجا اولین مصرف واقعی‌اش را پیدا می‌کند.
--
-- `users` و `roles` عمداً بیرون می‌مانند: کاربر یک شخص است و می‌تواند
-- عضو چند مستأجر باشد، و نقش‌ها سراسری‌اند.
SELECT enable_tenant_rls('tenant_memberships');
