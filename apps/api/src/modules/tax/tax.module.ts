import { Module } from '@nestjs/common';

/**
 * ⬜ فاز ۵ — سامانه مؤدیان.
 *
 * این ماژول **عمداً** خالی است: نه controller، نه provider، نه هیچ endpoint عملیاتی.
 * فقط برای اینکه مرز ماژول از روز اول وجود داشته باشد و بعداً افزودنش
 * ساختار را جابه‌جا نکند. بخش ۵ فایل CLAUDE.md.
 *
 * افزودن هر route به این ماژول در فاز ۱ نقض دامنه است.
 */
@Module({})
export class TaxModule {}
