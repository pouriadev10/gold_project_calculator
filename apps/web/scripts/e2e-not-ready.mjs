#!/usr/bin/env node
/**
 * جاگیر `test:e2e` تا رسیدن به FE-098 (Milestone 22, FrontTasks.md).
 *
 * انتخاب ابزار E2E و نوشتن سناریوی مسیر پایلوت به آن تسک موکول شده —
 * پیش از آماده‌شدن اکثر صفحات فاز ۱، یک suite خالی چیزی را اثبات نمی‌کند.
 * این اسکریپت با کد خروج غیرصفر روشن می‌کند که «سبز» نیست، نه اینکه
 * سکوت کند و به‌اشتباه موفق به‌نظر برسد.
 */
console.error('✖ test:e2e هنوز راه‌اندازی نشده — پیاده‌سازی در FE-098 (FrontTasks.md) است.');
process.exit(1);
