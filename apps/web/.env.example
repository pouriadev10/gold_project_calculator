# ریشه‌ی آدرس API. خالی بگذار تا /api (نسبی) استفاده شود — همانی که MSW
# در توسعه و reverse proxy در production سرو می‌کنند. فقط برای وصل‌شدن به
# بک‌اندی روی origin دیگر مقدار بده، مثلا:
# VITE_API_BASE_URL=http://localhost:3000/api
VITE_API_BASE_URL=
