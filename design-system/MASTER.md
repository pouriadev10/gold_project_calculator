# سیستم طراحی — GoldAccounting

> **منطق استفاده:** هنگام ساخت هر صفحه، اول `design-system/pages/<نام-صفحه>.md` را بخوان.
> اگر وجود داشت، قواعد آن بر این فایل **اولویت** دارد. اگر نبود، همین فایل مرجع است.

> ### ⚠️ ترتیب اولویت
>
> **بخش ۲ و ۶ فایل `CLAUDE.md` و بخش ۱ فایل `BOOTSTRAP.md` بر هر توصیه‌ی این فایل اولویت دارند.**
> این سند از اسکیل `ui-ux-pro-max` تولید شد، ولی اسکیل برای محصولات LTR و صفحات
> بازاریابی تنظیم شده است. هر توصیه‌ای که با قواعد ما تعارض داشت **رد شد** و در
> بخش «تعارض‌های حل‌شده» در انتهای همین فایل با دلیل ثبت شده است.

**پروژه:** GoldAccounting
**دسته:** Financial Dashboard / Invoice & Billing Tool — **نه** Luxury E-commerce
**دیال‌های طراحی:** Variance 2/10 (کم‌نوسان) · Motion 2/10 (حداقلی) · Density 8/10 (متراکم)
**تولید اولیه:** ۱۴۰۵/۰۵/۰۸ — سپس با قواعد پروژه بازنویسی شد

---

## ۰. سه حقیقت که کل طراحی از آن‌ها می‌آید

۱. **واحد پایه طلاست، نه ریال.** هر جا عدد مالی هست، کلید تعویض واحد هم هست.
۲. **کاربر روی گوشی میان‌رده، وسط معامله، با اینترنت ضعیف پاساژ کار می‌کند.** تراکم اطلاعات و سرعت بر زیبایی مقدم است.
۳. **این یک ابزار کاری روزانه‌ی پرتراکم است، نه یک برند لوکس.** هیچ hero، هیچ CTA بازاریابی، هیچ فضای سفید نمایشی.

---

## ۱. پالت رنگ — قفل‌شده

حالت **روشن اجباری**. حالت تاریک در فاز ۱ وجود ندارد.
مقادیر به‌صورت کانال HSL بدون `hsl()` ذخیره می‌شوند تا `bg-primary/10` کار کند.

| نقش | توکن CSS | مقدار HSL | کاربرد |
|---|---|---|---|
| پس‌زمینه | `--background` | `40 20% 98%` | زمینه‌ی صفحه، سفید گرم |
| متن اصلی | `--foreground` | `24 10% 10%` | متن بدنه |
| کارت | `--card` | `0 0% 100%` | سطح کارت‌ها |
| متن کم‌رنگ | `--muted-foreground` | `25 6% 38%` | برچسب، توضیح |
| زمینه‌ی خنثی | `--muted` | `40 12% 94%` | نوار، چیپ، حالت غیرفعال |
| اصلی | `--primary` | `38 95% 28%` | اقدام اصلی، طلایی تیره |
| روی اصلی | `--primary-foreground` | `40 30% 98%` | متن روی دکمه‌ی اصلی |
| مرز | `--border` | `35 12% 88%` | خط جداکننده |
| حلقه‌ی فوکوس | `--ring` | `38 92% 40%` | حالت فوکوس کیبورد |
| بستانکار | `--credit` | `152 60% 26%` | مانده‌ی مثبت، سود |
| بدهکار | `--debit` | `0 72% 38%` | مانده‌ی منفی، زیان |
| خطا | `--destructive` | `0 72% 38%` | خطا و اقدام مخرب |

### نسبت‌های کنتراست — اندازه‌گیری‌شده، نه حدسی

| ترکیب | نسبت | وضعیت |
|---|---|---|
| متن اصلی روی پس‌زمینه | **۱۶.۸:۱** | AAA |
| متن کم‌رنگ روی پس‌زمینه | **۵.۹۴:۱** | AA |
| متن کم‌رنگ روی کارت | **۶.۱۸:۱** | AA |
| متن روی دکمه‌ی اصلی | **۵.۶۹:۱** | AA |
| رنگ اصلی روی پس‌زمینه | **۵.۶۸:۱** | AA |
| بستانکار روی کارت | **۶.۵۷:۱** | AA |
| بدهکار روی کارت | **۷.۴۷:۱** | AA |

> کف پروژه ۴.۵:۱ است. کم‌ترین مقدار فعلی ۵.۶۸:۱ — حاشیه‌ی امن داریم.
> اگر رنگی اضافه شد، پیش از کامیت نسبتش را اندازه بگیر.

---

## ۲. تایپوگرافی

- **فونت واحد: وزیرمتن** — فونت متغیر، **لوکال** در `public/fonts/vazirmatn-variable.woff2` (۱۰۹KB برای همه‌ی وزن‌ها).
- **هیچ CDN خارجی.** نه گوگل‌فونت، نه jsdelivr. شبکه‌ی ایران.
- `font-display: swap` و `preload` در `index.html`.
- **ارقام جدولی سراسری:** `font-variant-numeric: tabular-nums` روی `body`, `input`, `button`.
  بدون این، ستون ارقام موقع تغییر مظنه می‌لرزد.
- اندازه‌ی قلم ورودی‌ها **هرگز زیر ۱۶px** — وگرنه اندروید و iOS هنگام فوکوس صفحه را بزرگ می‌کنند.

---

## ۳. فاصله و تراکم

تراکم ۸/۱۰ — این یک داشبورد کاری است، نه صفحه‌ی فرود.

| توکن | مقدار | کاربرد |
|---|---|---|
| `--space-xs` … `--space-3xl` | ۲ / ۴ / ۸ / ۱۲ / ۱۶ / ۲۴ / ۳۲ پیکسل | مقیاس متراکم |
| `spacing.touch` | **۴۴px** | حداقل هدف لمسی — بدون استثنا |
| `spacing.action` | **۵۶px** | ارتفاع دکمه‌ی اقدام اصلی |
| `--app-nav-h` | **۶۰px** | ارتفاع نوار ناوبری پایین |
| `--app-action-h` | **۸۰px** | ارتفاع نوار اقدام اصلی |
| `--radius` | `0.75rem` | شعاع پایه |

**نقاط شکست:** پایه **۳۶۰px** · `sm: 640` · `lg: 1024` · `xl: 1440`
`screens` در `tailwind.config.ts` **جایگزین** شده، نه گسترش‌یافته — یعنی `md:` اصلاً وجود ندارد
و اگر تایپش کنی کلاسی ساخته نمی‌شود. این عمدی است: جلوی برگشت ناخودآگاه به ذهنیت دسکتاپ.

---

## ۴. قواعد چیدمان

- **موبایل‌فرست یعنی از ۳۶۰px شروع کن**، نه اینکه دسکتاپ را کوچک کنی.
- اقدام اصلی هر صفحه در **یک‌سوم پایین** (منطقه‌ی شست). هرگز بالای صفحه.
- زیر ۶۴۰px **جدول وجود ندارد** → فهرست کارتی با ۳ تا ۴ فیلد کلیدی.
- زیر ۶۴۰px همه‌ی مودال‌ها Bottom Sheet تمام‌صفحه.
- ناوبری: نوار پایین زیر ۱۰۲۴px، سایدبار از ۱۰۲۴px به بالا.
- **هیچ محتوایی زیر نوارهای ثابت پنهان نشود** — از ابزارهای `.pb-nav` و `.pb-action` استفاده کن،
  نه از عدد دستی. ارتفاع نوارها فقط در یک جا تعریف شده است.

### RTL

- فقط خاصیت‌های منطقی: `ms-` `me-` `ps-` `pe-` `start-` `end-` `border-s` `border-e`.
- **هیچ** `ml-` `mr-` `pl-` `pr-` `left-` `right-` `text-left` `text-right` — قاعده‌ی ESLint این‌ها را خطا می‌دهد.
- وسط‌چین‌کردن با `mx-auto` انجام می‌شود، نه با `left-1/2` و ترجمه.

---

## ۵. اعداد مالی

- **هیچ عدد مالی مستقیم در JSX رندر نمی‌شود.** همه از `<AmountDisplay>` عبور می‌کنند.
- نرخ‌ها (مظنه، نرخ گرم) از `<RateDisplay>` — این‌ها ذاتاً ریالی‌اند و از کلید واحد پیروی نمی‌کنند.
- شمارش‌ها (تعداد نفرات، تعداد سکه) از `formatCount` / `formatCoinCount`.
- تاریخ‌ها از `formatJalali` / `formatJalaliDistance` — نه مستقیم از `date-fns-jalali`،
  چون آن کتابخانه نام ماه را فارسی می‌دهد ولی ارقام را لاتین می‌گذارد.
- قالب‌بندی همیشه از `@gold/core-calc` می‌آید — همان کدی که سرور استفاده می‌کند.

---

## ۶. حرکت

**حداقلی. INP < 200ms مقدم است.**

- فقط `transition-colors` روی حالت‌های تعاملی، ۱۵۰–۲۰۰ms.
- بدون انیمیشن ورود، بدون scroll-reveal، بدون شمارش صعودی اعداد.
- `prefers-reduced-motion: reduce` در `globals.css` همه‌ی انیمیشن‌ها را عملاً صفر می‌کند.
- **بدون GSAP.** این وابستگی در پروژه نصب نیست و نباید بشود.

---

## ۷. لمس، نه اشاره‌گر

- **هیچ اطلاعات یا اقدامی نباید فقط با hover در دسترس باشد.** محصول لمسی است.
- `hover:` فقط تزئین مجاز است؛ حالت `active:` برای بازخورد لمسی لازم است.
- `cursor-pointer` روی همه‌ی عناصر کلیک‌پذیر.
- حالت فوکوس کیبورد همیشه دیده شود: `focus-visible:ring-2 ring-ring ring-offset-2`.

---

## ۸. چک‌لیست پیش از تحویل

قبل از تیک زدن هر تسک فرانت‌اند:

- [ ] هیچ ایموجی به‌جای آیکون — همه از Lucide
- [ ] `cursor-pointer` روی همه‌ی عناصر کلیک‌پذیر
- [ ] حالت فوکوس برای ناوبری با کیبورد دیده می‌شود
- [ ] `prefers-reduced-motion` رعایت شده
- [ ] کنتراست متن حداقل ۴.۵:۱
- [ ] هدف لمسی حداقل ۴۴×۴۴px، بدون استثنا
- [ ] روی **۳۶۰px** هیچ اسکرول افقی نیست
- [ ] هیچ محتوایی زیر نوار ثابت پنهان نشده
- [ ] هر عدد مالی از `<AmountDisplay>` عبور کرده
- [ ] کلید تعویض واحد روی صفحه هست و همه‌ی اعداد را هم‌زمان عوض می‌کند
- [ ] هیچ رنگ hex خام و هیچ کلاس جهت‌دار اضافه نشده

---

## ۹. تعارض‌های حل‌شده

هر توصیه‌ی اسکیل `ui-ux-pro-max` که رد شد، با دلیل:

| توصیه‌ی اسکیل | تصمیم | دلیل |
|---|---|---|
| پالت تیره: `--background: #020617`، `--foreground: #F8FAFC` | **رد** | حالت روشن در فاز ۱ اجباری است. مغازه‌ی طلافروشی نور محیطی بالایی دارد و صفحه‌ی تیره زیر نور پاساژ خوانده نمی‌شود. |
| «Light mode default» به‌عنوان ضدالگو | **رد** | دقیقاً برعکس قاعده‌ی ماست. |
| فونت‌های `Fira Code` + `Fira Sans` از Google Fonts CDN | **رد** | فونت باید **وزیرمتن لوکال** باشد. فونت لاتین فارسی را درست رندر نمی‌کند و CDN گوگل از داخل ایران در دسترس نیست. |
| `@import url('https://fonts.googleapis.com/...')` | **رد** | هیچ CDN خارجی. قاعده‌ی ۱۱ BOOTSTRAP. |
| سبک «Exaggerated Minimalism» — تایپوگرافی غول‌پیکر، `clamp(3rem, 10vw, 12rem)`، فضای سفید انبوه | **رد** | این سبک برای پورتفولیو و برند لوکس است. ما یک ابزار کاری پرتراکم داریم که باید حداکثر اطلاعات را روی ۳۶۰px جا دهد. |
| «Best For: luxury brands, editorial, fashion» | **رد** | نوع محصول درست **Financial Dashboard / Invoice & Billing Tool** است. |
| الگوی صفحه «Real-Time / Operations Landing» با Hero و CTA در nav | **رد** | این یک اپلیکیشن پس از ورود است، نه صفحه‌ی فرود. Hero و CTA بازاریابی معنا ندارد. |
| رنگ CTA سبز `#22C55E` | **رد** | سبز در این دامنه معنای «بستانکار» دارد. استفاده‌اش برای دکمه‌ی اقدام، معنای مالی را مخدوش می‌کند. اقدام اصلی طلایی `38 95% 28%` است. |
| سود سبز `#22C55E` و زیان قرمز `#EF4444` مستقیم | **رد** (تعدیل‌شده) | جهت درست است ولی هر دو روی سفید زیر ۴.۵:۱ می‌افتند. با `152 60% 26%` و `0 72% 38%` جایگزین شدند — ۶.۵۷:۱ و ۷.۴۷:۱. |
| `--decimal-places: 2` و `--currency-symbol: $` | **رد** | ریال اعشار ندارد و صحیح است؛ وزن سه رقم اعشار دارد. واحد از `AmountDisplay` می‌آید، نه از یک متغیر CSS. |
| انیمیشن شمارش صعودی اعداد (count-up) | **رد** | روی اندروید ارزان هزینه‌ی رندر دارد و INP را می‌شکند. عدد مالی باید فوراً و ثابت خوانده شود، نه اینکه جلوی چشم بلغزد. |
| `gsap` و `ScrollTrigger` برای scroll-reveal | **رد** | وابستگی اضافه، بودجه‌ی ۲۰۰KB، و انیمیشن تزئینی. GSAP نصب نیست و نباید بشود. |
| `.card:hover { transform: translateY(-2px) }` | **رد** | محصول لمسی است؛ hover وجود ندارد. جابه‌جایی چیدمان هنگام تعامل هم آزاردهنده است. |
| `backdrop-filter: blur(4px)` روی مودال | **تعدیل‌شده** | blur سنگین روی اندروید ارزان کند است. فقط `backdrop-blur-sm` روی نوارهای کوچک، نه روی سطح تمام‌صفحه. |
| نقاط شکست `375 / 768 / 1024 / 1440` | **رد** | پایه‌ی ما **۳۶۰px** است و `768` وجود ندارد. نقاط شکست: `640 / 1024 / 1440`. |
| دکمه‌ی `padding: 12px 24px` با ارتفاع مؤثر ۳۶px | **تعدیل‌شده** | زیر حداقل ۴۴px بود. همه‌ی اندازه‌ها به `min-h-touch` رسیدند و خاصیت‌ها منطقی شدند. |
| ✅ «بدون ایموجی به‌جای آیکون» | **پذیرفته** | با قاعده‌ی ۱۲ BOOTSTRAP یکی است. |
| ✅ «`cursor-pointer` روی عناصر کلیک‌پذیر» | **پذیرفته** | — |
| ✅ «حالت فوکوس دیده شود» و «`prefers-reduced-motion` رعایت شود» | **پذیرفته** | — |
| ✅ «کنتراست حداقل ۴.۵:۱» | **پذیرفته** | اندازه‌گیری شد؛ بخش ۱. |
| ✅ «`Intl.NumberFormat` برای قالب‌بندی عدد» | **پذیرفته** | با `fa-IR` و از داخل `core-calc`. |
| ✅ «ستون چسبان و ردیف جمع در جدول» | **پذیرفته با شرط** | فقط از ۶۴۰px به بالا. زیر آن جدول نداریم. |
