# گزارش تسک — سیستم ورود داده (`FRONTEND-NEXT.md`)

**تاریخ:** ۹ مرداد ۱۴۰۵ (۲۰۲۶-۰۷-۳۱)
**دامنه:** اجرای گام ۱ تا ۴ سند `FRONTEND-NEXT.md`
**نقطه‌ی شروع:** کامیت `f7ceef6` (پایان کار حالت تیره)

---

## ۰. خلاصه

| سنجه | مقدار |
|---|---|
| کامیت‌ها | ۲ |
| کد نوشته‌شده | ۱۶ فایل · ۱۹۶۶ خط |
| تست‌ها | **۱۸۷** (۱۳۳ در `core-calc` + ۵۴ در `web`) |
| پوشش `core-calc` | **۱۰۰٪** در هر چهار معیار |
| بسته‌ی اولیه | **۱۱۲.۵ KB فشرده** — ۵۶٪ بودجه |
| `pnpm gate` | سبز |
| **معیار اصلی تسک (زیر ۸ ثانیه روی دستگاه واقعی)** | ❌ **اندازه‌گیری نشد** |

> بند آخر مهم‌ترین سطر این گزارش است. بخش ۶ را بخوانید.

---

## ۱. کامیت‌ها

| هش | عنوان | تغییرات |
|---|---|---|
| `0d0adb4` | گام ۱ — لایه‌ی API ساختگی با MSW | ۲۲ فایل، ۱۵۸۳+ / ۲۴۷− |
| `60e3e17` | گام ۲ تا ۴ — صفحه‌کلید عددی، NumericField، هارنس | ۲۲ فایل، ۱۸۵۸+ / ۹− |

---

## ۲. دو تصمیمی که سند بست

### SSR / پیش‌رندر — **نه**

گزارش نشست قبلی پیشنهاد داده بود پیش از ساخت صفحات بیشتر درباره‌ی SSR
تصمیم بگیریم (چون LCP روی 3G واقعی ۳.۹ ثانیه بود). سند `FRONTEND-NEXT.md`
بخش ۱-۱ این را رد کرد و دلیلش را نوشت: محصول یک **PWA نصب‌شده** است و
آن ۳.۹ ثانیه فقط یک بار در کل عمر نصب پرداخت می‌شود.

**نتیجه: هیچ کاری روی SSR نشد. مسئله بسته است** تا وقتی داده‌ی واقعی
نشان دهد کاربران مرتب cold load دارند.

### لایه‌ی داده — **MSW با قرارداد REST نهایی**

به‌جای mockهای موضعی، صفحات از TanStack Query به endpointهای واقعی وصل
شدند و فقط پاسخ‌دهنده ساختگی است.

---

## ۳. گام ۱ — لایه‌ی API ساختگی

```
apps/web/src/api/
├── contracts.ts   اسکیمای zod هر endpoint — آماده‌ی اشتراک با بک‌اند
├── client.ts      Idempotency-Key خودکار + خطای یکنواخت
└── queries.ts     هوک‌های TanStack Query

apps/web/src/mocks/
├── browser.ts            سرویس‌ورکر — فقط توسعه
└── handlers/
    ├── index.ts          هفت endpoint
    └── fixtures.ts       داده‌ی ساختگی، ساخته‌شده با core-calc
```

### قرارداد سیم

**هیچ مقدار پولی یا وزنی روی سیم `number` نیست.** همه رشته‌اند و تبدیل
به `bigint` فقط روی مرز و با `zod` انجام می‌شود:

```json
{"credit":{"rial":"196603630103","pureMg":"1330700","rate1000":"147744518"}}
```

دلیلش در خود فایل نوشته شده: `number` جاوااسکریپت بالای
۹٬۰۰۷٬۱۹۹٬۲۵۴٬۷۴۰٬۹۹۱ دقت را از دست می‌دهد و مانده‌ی ریالی یک مغازه‌ی
طلا به‌راحتی از آن رد می‌شود؛ `bigint` هم در JSON سریالایز نمی‌شود.

### endpointها

`GET /api/rates/current` · `GET /api/parties` · `GET /api/parties/balance-summary`
`GET /api/items` · `GET /api/reports/profit` · `GET /api/transactions/recent`
`POST /api/invoices`

> سند چهار endpoint خواسته بود؛ سه‌تای دیگر لازم شد چون همان سند
> می‌خواست **صفحه‌ی اصلی هم به MSW مهاجرت کند** و آن صفحه مانده، سود و
> معامله‌های اخیر می‌خواهد.

### آنچه در مرورگر اثبات شد

| رفتار | نتیجه |
|---|---|
| همان `Idempotency-Key` دوباره | همان فاکتور، بدون تکرار (`200`) |
| کلید تازه | فاکتور تازه (`201`) |
| بدون هدر | `400 IDEMPOTENCY_KEY_REQUIRED` |
| شماره‌گذاری بدون شکاف | `1405-000123` → `000124` |
| صحت مبلغ | `12.350` گرم عیار ۷۵۰ + اجرت → `1,373,483,592` ریال، منطبق با `core-calc` |

### mockهای قدیمی حذف شدند

`src/mocks/dashboard.ts` و `src/mocks/market.ts` پاک شدند تا دو منبع
حقیقت باقی نماند.

---

## ۴. گام ۲ تا ۴ — صفحه‌کلید عددی

```
packages/core-calc/src/
├── input.ts          بافر ورودی رشته‌ای + تبدیل رقم‌به‌رقم به bigint
└── number-bridge.ts  تنها نقطه‌ی مجاز تبدیل bigint → number

apps/web/src/features/keypad/
├── keypad-store.ts     استور جدا (Zustand)
├── shortcuts.ts        میان‌برهای وابسته به نوع فیلد
├── NumericKeypad.tsx   خود کیپد
├── KeypadPreview.tsx   پیش‌نمایش زنده‌ی قیمت
└── NumericField.tsx    پل فرم ↔ کیپد

apps/web/src/features/dev/KeypadHarness.tsx   مسیر /_dev/keypad
design-system/pages/keypad.md                 قواعد و تعارض‌های حل‌شده
```

### چرا مقدار «رشته» است، نه عدد

سه دلیل که در `input.ts` مستند شده:

۱. حالت‌های میانی عدد معتبر نیستند ولی باید نمایش داده شوند: `"12."` یا
   `"0.0"`. اگر به عدد تبدیل شوند، جداکننده و صفرهای انتهایی ناپدید
   می‌شوند و مکان‌نما می‌پرد.
۲. `"0.1" + "2"` روی رشته یعنی `"0.12"`؛ روی float یعنی حدس زدن.
۳. تبدیل نهایی رقم‌به‌رقم است و **هیچ‌جا از float عبور نمی‌کند**.

### سه تصمیم فنی که در کد پیدا نیستند

| تصمیم | چرا |
|---|---|
| `position: fixed` + `transform` | کیپد از جریان چیدمان بیرون است، پس سهمش در CLS **صفر** است. با تغییر `height` هر بار کل صفحه می‌پرید. |
| `onPointerDown` به‌جای `onClick` | روی موبایل `click` تا ۳۰۰ms بعد از تماس شلیک می‌شود. |
| `visibility: hidden` در حالت بسته | فقط `translate` کافی نبود: ۱۸ کلید با `Tab` در دسترس می‌ماندند و صفحه‌خوان می‌خواندشان. |

### میان‌برهای دامنه‌ای

ستون کناری با نوع فیلد فعال عوض می‌شود — همان چیزی که کیبورد سیستمی
نمی‌تواند بدهد:

| فیلد | میان‌برها |
|---|---|
| عیار | `۷۵۰` `۹۹۵` `۷۴۰` `۹۲۵` `۹۰۰` |
| وزن | `÷۵` `×۲` `مثقال` |
| مبلغ / مظنه | `۰۰۰` `۰۰۰۰۰` |
| تعداد | `۱` `۲` `۵` `۱۰` |

---

## ۵. آنچه در مسیر پیدا شد

| # | مورد | چطور پیدا شد | اقدام |
|---|---|---|---|
| ۱ | **Workbox فایل `mockServiceWorker.js` را در سرویس‌ورکر production کش می‌کرد** — سند صریح گفته «حتی یک بایت» نباید بماند | grep روی `dist` | `globIgnores` + افزونه‌ی build که فایل را از خروجی پاک می‌کند |
| ۲ | **`Number()` در ۶ نقطه‌ی واقعی** پس از افزودن قاعده‌ی جدید | خود قاعده | `number-bridge.ts` به‌عنوان تنها نقطه‌ی مجاز — با محافظ دقت که **خطا می‌دهد** به‌جای برگرداندن عدد غلط |
| ۳ | **`eslint-plugin-react-hooks` نصب بود ولی هرگز فعال نشده بود** | ارجاع به قاعده‌ای که وجود نداشت | در `eslint.config.js` وصل شد؛ روی کد موجود نقضی نداشت |
| ۴ | **اسکیماهای `zod` با `.transform()` در `ZodType<T>` جا نمی‌شدند** | خطای typecheck | کلاینت جنریک روی **خود اسکیما** شد (`z.infer<S>`)، نه روی نوع خروجی |
| ۵ | **`zod` وارد بسته‌ی production شد** (+۱۲.۶KB) | مقایسه‌ی حجم پیش و پس | **پذیرفته شد** — اعتبارسنجی پاسخ در زمان اجرا؛ پاسخ خراب که بی‌صدا مانده‌ی غلط بسازد از ۱۲.۶KB گران‌تر است |

---

## ۶. اندازه‌گیری — و آنچه اندازه‌گیری نشد

### در مرورگر دسکتاپ تأیید شد

| معیار | نتیجه |
|---|---|
| کیبورد سیستم‌عامل باز نمی‌شود | `inputMode="none"` + `readOnly` روی هر سه فیلد |
| ارتفاع پنجره هنگام باز شدن کیپد | ۷۸۰ → ۷۸۰ (بدون تغییر) |
| **سهم CLS باز شدن کیپد** | **۰** (با `PerformanceObserver`) |
| کیپد روی نوار ناوبری نمی‌افتد | کف کیپد ۷۲۰ · سقف ناوبری ۷۱۹ |
| هدف لمسی | هر ۱۸ کلید ≥۴۸×۴۸px |
| فاصله‌ی کلیدها | ۸px |
| `aria-label` | ۱۸ از ۱۸ |
| دقت ورودی | `"12.345"` → `12345n` میلی‌گرم |
| میان‌بر مثقال | ۲ مثقال → `9217` میلی‌گرم |

### ❌ اندازه‌گیری **نشد**

| معیار سند | وضعیت |
|---|---|
| ورود سه مقدار **زیر ۸ ثانیه** روی اندروید میان‌رده‌ی واقعی | **انجام نشد** |
| تأخیر ضربه تا رنگ‌آمیزی **زیر ۱۰۰ms** | **انجام نشد** |
| تأیید روی **Safari iOS** | **انجام نشد** |

**دو دلیل:**

۱. پنجره‌ی مرورگر در این نشست فریم رندر نمی‌کرد. یک `CSSTransition` با
   `playState: "running"` و `currentTime: 0` دیده شد — یعنی
   `requestAnimationFrame` اصلاً پیش نمی‌رفت و هر عدد تأخیری که هارنس
   می‌داد بی‌معنا بود.
۲. دستگاه فیزیکی در دسترس نبود.

> سند بخش ۶ می‌گوید «**معیار ✔ تمام است وقتی را واقعاً اجرا کن. تیک زدن
> بدون اندازه‌گیری ممنوع.**» پس تیک نخورد. عدد ۸ ثانیه هنوز یک **حدس** است.

**هارنس آماده است.** روی گوشی باز کنید:

```
http://172.20.10.8:5173/_dev/keypad
```

سه فیلد را پر کنید و «تمام» را بزنید. چهار عدد را می‌دهد. اگر «ارتفاع
پنجره» هنگام تپ پرید، یعنی کیبورد سیستم باز شده و معیار رد شده است.

> بخش ۳ سند برای حالت شکست برنامه دارد (حذف لرزش، `÷۵`، `×۲`، مثقال).
> آن نسخه‌ی حداقلی **فقط پس از اندازه‌گیری** اعمال می‌شود، نه پیش از آن.

---

## ۷. قواعد جدیدی که به دروازه اضافه شد

| قاعده | دامنه |
|---|---|
| `Number()` ممنوع | همه‌جا جز `core-calc/src/number-bridge.ts` |
| `react-hooks/rules-of-hooks` و `exhaustive-deps` | `apps/web/**` |

به‌همراه قواعد قبلی: `Math.round/floor/ceil/trunc` بیرون از `rounding.ts`،
`parseFloat`، `toFixed`، کلاس‌های جهت‌دار Tailwind، و رنگ hex خام.

---

## ۸. بررسی در مرورگر

| مسیر | چه چیزی را نشان می‌دهد |
|---|---|
| `/` | داشبورد — حالا داده‌اش از MSW می‌آید. کلید طلا/ریال باید هر ۱۵ عدد را هم‌زمان عوض کند. |
| `/_dev/keypad` | هارنس کیپد — **جای اندازه‌گیری واقعی** |
| `/_dev/smoke` | اثبات کارکرد ۱۲ پکیج |
| `/more` | تنظیمات: واحد + پوسته |

در DevTools → Network باید `GET /api/...` دیده شود و هر مقدار مالی در
پاسخ **رشته** باشد.

---

## ۹. گام بعد

طبق بخش ۵ سند:

1. **صفحه‌ی فروش (POS)** با MSW و کیپد — معیارش ثبت یک فاکتور تک‌قلمی
   **زیر ۱۵ ثانیه** روی موبایل واقعی
2. صفحه‌ی خرید از مصرف‌کننده — جریان برعکس، با میان‌بر عیار ۷۴۰
3. صفحه‌ی اشخاص با مانده‌ی چندبُعدی

> اما پیش از آن‌ها: **اعداد گام ۴ را روی گوشی بگیرید.** اگر کیپد به ۸
> ثانیه نرسد، صفحه‌ی فروش هم به ۱۵ ثانیه نمی‌رسد و بهتر است پیش از
> ساختنش بدانیم.
