# گزارش نشست — راه‌اندازی فرانت‌اند فاز ۱

**تاریخ:** ۳۰ و ۳۱ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۳۰ تا ۲۰۲۶-۰۷-۳۱)
**دامنه:** اجرای کامل `BOOTSTRAP.md` (گام ۱ تا ۸) + افزودن حالت تیره
**وضعیت شروع:** پوشه‌ی خالی — فقط سه فایل markdown (`CLAUDE.md`، `BOOTSTRAP.md`، سند معماری)

---

## ۱. خلاصه در یک نگاه

| سنجه | مقدار |
|---|---|
| کامیت‌ها | ۴ |
| فایل‌های منبع نوشته‌شده | ۵۴ فایل TS/TSX · ۳۶۷۴ خط |
| تست‌ها | **۱۲۸** (۱۰۲ در `core-calc` + ۲۶ در `web`) |
| پوشش تست `core-calc` | **۱۰۰٪** در هر چهار معیار |
| بسته‌ی اولیه | **۹۹.۸ KB فشرده** (۵۰٪ بودجه‌ی ۲۰۰KB) |
| LCP موبایل استاندارد | **۲.۰ ثانیه** ✅ |
| `pnpm gate` | سبز |

---

## ۲. کامیت‌ها

| هش | زمان | عنوان | تغییرات |
|---|---|---|---|
| `cae8bec` | ۲۲:۵۹ | گام ۱ و ۲ — مونوریپو، ابزار، پکیج `core-calc` | ۳۱ فایل، ۲۵۱۱+ |
| `3df52f4` | ۲۳:۲۹ | گام ۳ — اسکلت Vite + React و صفحه‌ی دود | ۴۸ فایل، ۲۸۱۸+ |
| `a1f384b` | ۰۳:۰۷ | گام ۴ تا ۸ — سیستم طراحی، توکن‌ها، پوسته، صفحه‌ی اصلی، دروازه‌ی کارایی | ۱۶۹ فایل، ۲۷۳۴۰+ |
| `f7ceef6` | ۱۴:۲۴ | افزودن حالت تیره — روشن / تیره / سیستم | ۱۷ فایل، ۶۸۹+ |

> کامیت سوم بزرگ است چون پوشه‌ی اسکیل `ui-ux-pro-max` طبق دستور بخش ۴-۱
> `CLAUDE.md` در ریپو کامیت شد تا بقیه‌ی تیم دوباره دانلودش نکنند.

---

## ۳. `packages/core-calc` — موتور محاسبات

۹ فایل منبع (۵۱۰ خط) · ۹ فایل تست (۶۳۳ خط) · **پوشش ۱۰۰٪**

| فایل | نقش |
|---|---|
| `types.ts` | انواع برندشده: `Rial`، `PureMg`، `GrossMg`، `Karat`، `CoinCount`، `AssetDimension` |
| `rounding.ts` | **تنها خانه‌ی مجاز گرد کردن** — `roundHalfUp`، `mulDivHalfUp` |
| `karat.ts` | تبدیل عیار و مثقال |
| `pricing.ts` | `gramRate`، `gramRate1000`، `valueOfPure` |
| `coin.ts` | سکه و شمش — قانون حباب در سطح **نوع** |
| `dual.ts` | مبلغ دومقیاسه با نرخ قفل‌شده |
| `persian.ts` | نرمال‌سازی ی/ك، ارقام، نیم‌فاصله |
| `format.ts` | قالب‌بندی `fa-IR` |

### آنچه واقعاً اثبات شد

- **جدول پذیرش بخش ۲ BOOTSTRAP مو‌به‌مو بازتولید شد:** هر چهار مقدار
  (`21699976`، `22777279`، `23085080`، `30626207`) دقیق درآمدند.
- **فرمول غلط `÷۴.۶۰۸۳` رد شد** — تستی ثابت می‌کند حدود ۶٪ کم‌تر از واقع می‌دهد.
- **قانون حباب با نوع اجبار شد، نه با شرط:** `bubble(bullion)` **کامپایل نمی‌شود**.
  با `expectTypeOf` و `@ts-expect-error` در `coin.test-d.ts` تأیید شد.
- **رفت‌وبرگشت عیار:** روی شبکه‌ی نمایش‌پذیر خطای **صفر**؛ خارج از آن حداکثر
  **۱ میلی‌گرم** — با `fast-check` روی ۱۰۰۰ مقدار تصادفی.
- **جست‌وجوی «علي» رکورد «علی» را پیدا می‌کند.**

### یک انحراف صریح از سند

BOOTSTRAP خواسته بود رفت‌وبرگشت `750 → pure → 750` روی ۱۰۰۰ مقدار تصادفی
**خطای صفر** بدهد. با ذخیره‌سازی صحیح بر حسب میلی‌گرم، این برای *هر* وزنی
ریاضیاً ممکن نیست (مثلاً ۱۰۰۱mg عیار ۷۵۰ می‌شود ۷۵۰.۷۵mg).

پیاده‌سازی: تابع `karatLatticeMg` گام شبکه‌ی هر عیار را می‌دهد و تست‌ها
**هر دو** خاصیت را اثبات می‌کنند — صفر روی شبکه، و کران ۱ میلی‌گرم خارج از آن.
این صادقانه‌تر از تنظیم تست برای سبز شدن است.

---

## ۴. `apps/web` — رابط کاربری

۴۵ فایل (۳۱۶۴ خط). React 18 + Vite 5 + Tailwind 3 + shadcn/ui + TanStack Router/Query + PWA.

- **موبایل‌فرست از ۳۶۰px**؛ `screens` در Tailwind **جایگزین** شد نه گسترش‌یافته،
  یعنی `md:` اصلاً وجود ندارد و اگر تایپش کنی کلاسی ساخته نمی‌شود.
- **`<AmountDisplay>` تنها مسیر رندر عدد مالی** — با grep تأیید شد.
- **`<RateDisplay>` برای نرخ‌ها** که عمداً از کلید واحد پیروی نمی‌کنند
  (نرخ طلا برحسب طلا همیشه یک است).
- **وزیرمتن لوکال**، هیچ CDN.
- صفحه‌ی دود `/_dev/smoke` که ۱۲ پکیج را اثبات می‌کند و **در بیلد production
  اصلاً وجود ندارد** (با grep روی `dist` تأیید شد).

---

## ۵. باگ‌هایی که پیدا و رفع شدند

این‌ها مهم‌ترین بخش نشست‌اند — هیچ‌کدام از پیش معلوم نبودند.

| # | مشکل | چطور پیدا شد | رفع |
|---|---|---|---|
| ۱ | **قاعده‌ی ESLint جهت‌دار، واریانت‌ها را نمی‌گرفت.** `sm:text-left` رد می‌شد چون الگو مرز کلاس را فقط فاصله/نقل‌قول می‌دانست. | grep دستی سه نقض واقعی پیدا کرد که lint سبز داده بود | `:` به مرز کلاس اضافه شد؛ با فایل آزمون تأیید شد؛ ۳ نقض در `dialog/sheet/drawer` رفع شد |
| ۲ | **نوار اقدام دقیقاً روی نوار ناوبری می‌افتاد** (هر دو ۷۱۹–۷۸۰px) | اندازه‌گیری `getBoundingClientRect` در مرورگر | توکن واحد `--app-nav-h` + ابزار `.bottom-above-nav` |
| ۳ | **چانک دستی به نام `echarts` محتوایش React بود** و بی‌دلیل `modulepreload` می‌شد | بازکردن خود فایل چانک در `dist` | `manualChunks` حذف شد؛ ECharts در production اصلاً استفاده نمی‌شود |
| ۴ | **`echarts-for-react` زیر StrictMode ری‌اکت ۱۸ هیچ بومی نمی‌سازد** — `dispose` بوم را برمی‌دارد ولی صفت `_echarts_instance_` می‌ماند. **بدون هیچ خطای کنسولی.** | بررسی `document.querySelector('canvas')` که صفر برگرداند | استفاده‌ی مستقیم از `echarts` با `useEffect` و `dispose` درست |
| ۵ | **`date-fns-jalali` نام ماه را فارسی می‌دهد ولی ارقام را لاتین** («8 مرداد 1405») | grep روی `[0-9]` در متن رندرشده | `Intl` بومی — هم فارسی‌تر، هم **صفر بایت** |
| ۶ | **اندازه‌های shadcn زیر حداقل لمسی بودند** (۳۶/۳۲/۴۰px در برابر کف ۴۴px) | خواندن `button.tsx` | همه به `min-h-touch` رسیدند + اندازه‌ی `action` (۵۶px) |
| ۷ | **`bg-black/80` در سه مودال** — پرده‌ی مشکی روی سطح تیره جدا نمی‌شود | grep رنگ‌های غیرتوکنی هنگام افزودن حالت تیره | توکن `--overlay` که در حالت تیره عمیق‌تر است |

---

## ۶. کارایی — اندازه‌گیری‌شده، نه ادعا

### بسته

| مورد | مقدار |
|---|---|
| JS بسته‌ی اولیه (gzip) | **۹۹.۸ KB** — ۵۰٪ بودجه |
| CSS | ۶.۰ KB |
| فونت | ۴۷.۲ KB |

### Lighthouse — سه پروفایل

| پروفایل | LCP | TBT | CLS | امتیاز |
|---|---|---|---|---|
| دسکتاپ | ۰.۵ ثانیه | ۰ ms | — | **۱۰۰** |
| **موبایل استاندارد** (Slow 4G، CPU ×۴) | **۲.۰ ثانیه** ✅ | ۹۰ ms ✅ | ۰.۰۲۶ ✅ | **۹۸** |
| 3G واقعی (۷۰۰kbps، RTT ۳۰۰ms) | ۳.۹ ثانیه ❌ | ۵۰ ms | ۰.۰۲۸ | ۷۸ |

### زنجیره‌ی بهینه‌سازی (هر گام اندازه‌گیری شد)

| تغییر | اثر روی LCP (3G) |
|---|---|
| نقطه‌ی شروع | ۴.۸ ثانیه |
| حذف `preload` فونت + پوسته‌ی inline | ۴.۸ ثانیه (بی‌اثر — CSS همچنان رندر را بلوکه می‌کرد) |
| فونت بدون لاتین (۱۰۹KB → ۴۷KB) | **۴.۲ ثانیه** |
| حذف `date-fns-jalali` از production | **۳.۹ ثانیه** |

> **صادقانه: هدف ۲.۵ ثانیه روی 3G واقعی محقق نشد.** با یک SPA کلاینت‌رندر
> و پایه‌ی React + TanStack (حدود ۸۵–۹۰KB فشرده که تا اجرا نشود چیزی رندر
> نمی‌شود) این عملاً ممکن نیست. مسیر واقعی: **SSR یا پیش‌رندر پوسته**.
> جزئیات و گزینه‌های رتبه‌بندی‌شده در `PERFORMANCE.md`.

---

## ۷. سیستم طراحی

اسکیل `ui-ux-pro-max` نصب شد و خروجی تولید شد — ولی خروجی خام برای
**محصولات LTR و صفحات بازاریابی** تنظیم بود. طبق جدول اولویت `CLAUDE.md`،
سند بازنویسی شد و بخش **«تعارض‌های حل‌شده»** با **۲۲ توصیه** و دلیل هرکدام اضافه شد.

نمونه‌ی توصیه‌های ردشده:

- پالت تیره‌ی سرد `#020617` و «Light mode default به‌عنوان ضدالگو»
- فونت‌های `Fira Code`/`Fira Sans` از **Google Fonts CDN**
- سبک «Exaggerated Minimalism» با `clamp(3rem, 10vw, 12rem)` برای برندهای لوکس
- الگوی صفحه‌ی فرود با Hero و «CTA in nav»
- `gsap` + `ScrollTrigger` برای scroll-reveal
- انیمیشن شمارش صعودی اعداد مالی
- نقاط شکست `375/768/1024/1440`

---

## ۸. حالت تیره (افزوده‌ی خارج از BOOTSTRAP اولیه)

> گام ۵ سند در ابتدا گفته بود «حالت تاریک در این فاز نه». این تصمیم به
> درخواست مالک محصول بازنگری شد و **هر دو سند** `BOOTSTRAP.md` و
> `design-system/MASTER.md` به‌روز شدند تا کدبیس با خودش تناقض نداشته باشد.

- سه‌حالته: **روشن / تیره / سیستم**، پیش‌فرض `system`.
- **بدون پرش سفید:** اسکریپت همگام در `<head>` پیش از اولین رنگ‌آمیزی
  (تأیید شد: خط ۱۰۰، `</head>` خط ۱۰۷، باندل خط ۱۱۷).
- **هیچ واریانت `dark:` در هیچ کامپوننتی** — همه‌چیز از توکن می‌آید.
- رنگ نوار مرورگر از خود توکن `--background` خوانده می‌شود، نه از ثابت جدا.
- `/more` به صفحه‌ی تنظیمات واقعی تبدیل شد.

**پالت وارونه‌سازی ساده نیست:** پس‌زمینه مشکی مطلق نیست (لکه‌ی OLED)،
طلایی از ۲۸٪ به ۶۰٪ روشنایی رفت (وگرنه قهوه‌ای دیده می‌شد)، سبز و قرمز
فقط روشن‌تر شدند تا معنای بستانکار/بدهکار ثابت بماند.

**کنتراست اندازه‌گیری‌شده در مرورگر:** دکمه‌ی طلایی ۹.۸۳:۱ · بدهکار ۵.۹۸:۱ ·
برچسب کم‌رنگ ۸.۰۵:۱ — کم‌ترین مقدار پالت **۵.۴۵:۱** (کف ۴.۵:۱).

یک استثنای مستند: مرز کارت در حالت تیره **۲.۱۶:۱** است، زیر ۳:۱. کارت با
**اختلاف سطح** جدا می‌شود نه با خط؛ مرز نقش تکمیلی دارد.

---

## ۹. دروازه‌ی کیفیت

`pnpm gate` = `typecheck && lint && test && build && size`

- **`pnpm size`** — بودجه‌ی بسته، **بدون نیاز به کروم**، پس در هر CI اجرا می‌شود.
  با `BUNDLE_BUDGET_KB=100` تست شد و درست **قرمز شد** (کد خروج ۱).
- **Lighthouse CI** (`lighthouserc.json`) با آستانه‌های LCP/TBT/CLS/حجم اسکریپت.
- **GitHub Actions** (`.github/workflows/ci.yml`) — همه‌ی مراحل به‌عنوان دروازه‌ی merge.

قواعد ESLint که به‌عنوان دروازه عمل می‌کنند:

- `Math.round/floor/ceil/trunc` خارج از `rounding.ts` → خطا
- `parseFloat` و `toFixed` → خطا
- کلاس‌های جهت‌دار Tailwind (شامل واریانت‌ها) → خطا
- رنگ hex خام در JSX → خطا

---

## ۱۰. کارهای انجام‌نشده — صریح

| مورد | وضعیت | چرا |
|---|---|---|
| **تست روی اندروید میان‌رده‌ی واقعی** | ❌ انجام نشد | دستگاه فیزیکی در دسترس نبود. بخش ۷ `CLAUDE.md` می‌گوید شبیه‌ساز کافی نیست. **تنها چک‌باکس تیک‌نخورده‌ی BOOTSTRAP.** |
| **LCP زیر ۲.۵ ثانیه روی 3G واقعی** | ❌ ۳.۹ ثانیه | نیازمند SSR/پیش‌رندر. در `PERFORMANCE.md` مستند شد. |
| **اندازه‌گیری INP** | ❌ ممکن نیست | سنجه‌ی میدانی است؛ در آزمایشگاه اندازه گرفته نمی‌شود. `TBT` (۹۰ms) فقط تقریب است. نیازمند RUM. |
| **اسکرین‌شات بصری** | ❌ در دسترس نبود | پنل مرورگر در این نشست رندر نمی‌شد. همه‌ی تأییدها **اندازه‌گیری برنامه‌نویسی‌شده** است، نه بازرسی چشمی. |
| **`background_color` مانifest در حالت تیره** | ⚠️ محدودیت | مانifest ایستاست؛ splash در حالت تیره سفید می‌زند. |
| **تغییر زنده‌ی پوسته‌ی سیستم در مرورگر** | ⚠️ با تست پوشش داده شد | شبیه‌سازی CDP رویداد `change` را منتشر نمی‌کند؛ با تست واحد اثبات شد. |

---

## ۱۱. اسناد تولیدشده

| فایل | محتوا |
|---|---|
| `design-system/MASTER.md` | پالت هر دو حالت + کنتراست اندازه‌گیری‌شده + ۲۲ تعارض حل‌شده |
| `design-system/pages/home.md` | قواعد اختصاصی صفحه‌ی اصلی + تعارض‌های آن |
| `PERFORMANCE.md` | همه‌ی اعداد اندازه‌گیری‌شده + زنجیره‌ی بهینه‌سازی + کارهای باقی‌مانده |
| `scripts/check-bundle-size.mjs` | دروازه‌ی بودجه‌ی بسته |
| `apps/web/lighthouserc.json` | آستانه‌های کارایی |
| `.github/workflows/ci.yml` | دروازه‌ی merge |

---

## ۱۲. اجرا

```bash
pnpm install
```

```bash
pnpm dev
```

```bash
pnpm gate
```

صفحه‌ی دود فقط در توسعه: `http://localhost:5173/_dev/smoke`

---

## ۱۳. پیشنهاد گام بعد

1. **`TASKS.md` را بساز** — بخش ۸ `CLAUDE.md` روال کار را بر پایه‌ی آن تعریف کرده ولی این فایل هنوز وجود ندارد.
2. **تست روی اندروید واقعی** — تنها معیار برآورده‌نشده‌ی BOOTSTRAP.
3. **تصمیم درباره‌ی SSR/پیش‌رندر** — پیش از اینکه صفحات بیشتری ساخته شوند، چون بعداً تغییرش گران است.
4. سپس بک‌اند: دفتر کل چندواحدی (`ledger/`) که بخش ۲-۷ `CLAUDE.md` آن را قلب سیستم می‌داند.
