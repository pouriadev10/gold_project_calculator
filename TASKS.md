# TASKS.md — فهرست تسک‌ها

ترتیب تسک‌ها معنادار است. بخش ۸ فایل `CLAUDE.md` روال کار را توضیح می‌دهد:
اولین تسک تیک‌نخورده را بردار، «تمام است وقتی» را واقعاً بررسی کن، سپس تیک بزن
و با پیام `[شناسه] عنوان تسک` کامیت کن.

> این فایل تا پیش از BE-001 در مخزن وجود نداشت و تسک‌های انجام‌شده‌ی فرانت‌اند
> (کیپد، حالت تیره، گزارش‌ها) پیش از ساخته‌شدنش کامیت شده‌اند. از اینجا به بعد
> هر تسک باید در همین فایل ثبت و تیک بخورد.

---

## بک‌اند

- [x] **BE-001 — ساخت اپلیکیشن NestJS**
      اسکلت `apps/api` روی NestJS + Fastify با TypeScript strict، اندپوینت
      `GET /health` با پاسخ `{ "status": "ok" }`، و تست واحد و e2e.
      بدون دیتابیس، بدون ماژول دامنه، بدون احراز هویت.
