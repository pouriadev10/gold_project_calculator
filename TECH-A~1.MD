# سند معماری فنی
## نرم‌افزار جامع حسابداری طلا و جواهر

**نسخه:** ۰.۲ — بازنگری‌شده بر پایه‌ی **وب موبایل‌فرست (تک‌پلتفرم)**
**پیوست سند:** PRD-Gold-Accounting-Software.md

> **تغییر نسبت به نسخه‌ی ۰.۱:** اپلیکیشن موبایل بومی و اپلیکیشن دسکتاپ از دامنه‌ی محصول حذف شدند. محصول یک **وب‌اپلیکیشن واحد با طراحی موبایل‌فرست (PWA)** است که روی گوشی، تبلت و دسکتاپ اجرا می‌شود.

---

## ۰. پیامدهای تصمیم «فقط وب، موبایل‌فرست»

این تصمیم درست است و پیچیدگی پروژه را حدود یک‌سوم کم می‌کند. اما سه چیز را حذف نمی‌کند، بلکه **جابه‌جا می‌کند**. باید آگاهانه با آن‌ها روبه‌رو شوید:

| چیزی که حذف شد | جایی که دوباره ظاهر می‌شود |
|---|---|
| اپ بومی موبایل | محدودیت‌های PWA در iOS — به‌ویژه در آفلاین و اعلان |
| اپ دسکتاپ (Electron) | دسترسی به ترازو و چاپگر باید از داخل مرورگر حل شود |
| موتور همگام‌سازی آفلاین کامل | تصمیم صریح درباره‌ی «چقدر آفلاین» — پاسخ دیگر «کامل» نیست |

### چهار محدودیت پابرجا

| محدودیت | پیامد فنی |
|---|---|
| **دقت مطلق مالی** | هیچ‌جا Float مجاز نیست. نوع داده، گرد کردن و ترتیب عملیات باید تعیّن‌گرا باشد. |
| **استقرار دوگانه (SaaS + On-premise)** | مونولیت ماژولار الزامی است. میکروسرویس یعنی نصب محلی عملاً غیرممکن. |
| **ورود سریع داده روی صفحه‌ی کوچک** | جایگزین چالش قبلی (آفلاین) شد. هدف «ثبت زیر ۱۵ ثانیه» روی موبایل به‌مراتب سخت‌تر از دسکتاپ است. |
| **زیرساخت داخل ایران** | Docker Hub، GitHub Actions و سرویس‌های ابری خارجی مسئله‌دارند. |

> **ریسک فنی اصلی پروژه:** ۱) مدل داده‌ی دفتر کل، ۲) کیفیت تجربه‌ی ورود داده روی موبایل. باقی استک کالایی است.

---

## ۱. پیشنهاد نهایی — خلاصه

| لایه | انتخاب پیشنهادی | جایگزین جدی |
|---|---|---|
| زبان اصلی | **TypeScript** (سراسر پروژه) | C# / .NET 8 |
| بک‌اند | **NestJS** روی Node 22 (مونولیت ماژولار) | ASP.NET Core |
| پایگاه داده | **PostgreSQL 16** | — |
| لایه‌ی داده | **Drizzle ORM** یا **Kysely** (SQL-first) | EF Core |
| کش / صف / Pub-Sub | **Redis + BullMQ** | RabbitMQ |
| کلاینت | **یک PWA واحد: React 18 + TypeScript + Vite** | — |
| UI | **Tailwind CSS + shadcn/ui (Radix)** | Mantine |
| ذخیره‌ی محلی مرورگر | **IndexedDB (Dexie) + Workbox** | — |
| ذخیره‌ی فایل | **MinIO** (سازگار با S3، قابل نصب محلی) | — |
| استقرار | **Docker Compose** (نه Kubernetes در فاز ۱) | — |

**منطق TypeScript سراسری:** موتور محاسبات (عیار، مظنه، اجرت، مالیات) باید *عیناً* یک نتیجه را روی سرور و مرورگر بدهد. یک زبان یعنی یک پیاده‌سازی، یک مجموعه تست، یک حقیقت.

**چه زمانی .NET برنده است؟** اگر تیم فعلی شما .NET کار است. استک ناآشنا با تیم آشنا همیشه بازنده است. در آن صورت موتور محاسبات را به‌عنوان کتابخانه‌ی مستقل و به‌شدت تست‌شده جدا کنید و برای کلاینت از طریق API یا WASM فراخوانی‌اش کنید — دوباره ننویسیدش.

---

## ۲. بک‌اند

### ۲-۱. سبک معماری: مونولیت ماژولار

```
apps/api
└── src/modules
    ├── ledger/          ← هسته‌ی دفتر دوگانه (قلب سیستم)
    ├── pricing/         ← موتور عیار، مظنه، اجرت، مالیات
    ├── inventory/       ← انبار، امانی، شعبه
    ├── trading/         ← معاملات آبشده، ری‌گیری، موقعیت باز
    ├── sales/           ← فروش، فاکتور، مشتریان
    ├── commission/      ← پورسانت بازاریاب
    ├── tax/             ← سامانه مؤدیان
    └── platform/        ← auth, tenant, audit, files, notif

packages/
    └── core-calc/       ← موتور محاسبات مشترک بین سرور و کلاینت
```

`packages/core-calc` بسته‌ی مشترک است: توابع خالص، بدون وابستگی به I/O، با پوشش تست بالا. هم API از آن استفاده می‌کند، هم مرورگر برای پیش‌نمایش لحظه‌ای قیمت.

### ۲-۲. طراحی دفتر کل — مهم‌ترین بخش سند

**اصل:** دفتر کل **Append-Only** است. هیچ رکوردی UPDATE یا DELETE نمی‌شود.

```sql
CREATE TABLE ledger_entry (
  id              BIGSERIAL PRIMARY KEY,
  tenant_id       UUID NOT NULL,
  transaction_id  UUID NOT NULL,          -- گروه‌بندی سطرهای یک سند
  account_id      UUID NOT NULL,
  amount_rial     BIGINT NOT NULL DEFAULT 0,   -- ریال، صحیح
  weight_mg       BIGINT NOT NULL DEFAULT 0,   -- میلی‌گرم طلای نرمال‌شده، صحیح
  raw_weight_mg   BIGINT,                      -- داده‌ی خام برای ردیابی
  raw_karat       INTEGER,                     -- مثلاً 995
  occurred_at     TIMESTAMPTZ NOT NULL,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  CHECK (amount_rial <> 0 OR weight_mg <> 0)
);
```

**ناوردا (Invariant) که باید در سطح دیتابیس تضمین شود:**

```
برای هر transaction_id:
    SUM(amount_rial) = 0   و   SUM(weight_mg) = 0
```

با `CONSTRAINT TRIGGER ... DEFERRABLE INITIALLY DEFERRED` اعمال کنید. صحت دفتر کل نباید به درستی کد اپلیکیشن وابسته باشد؛ دیتابیس باید نگهبان باشد.

**قواعد دقت — مکتوب و مرکزی:**

- ریال → `BIGINT`، واحد پایه ریال، بدون اعشار.
- وزن → `BIGINT`، واحد پایه **میلی‌گرم** طلای نرمال‌شده. نگه‌داشتن واحد صحیح، کل ابهام گرد کردن اعشاری را حذف می‌کند.
- همه‌ی گرد کردن‌ها فقط در `core-calc/rounding.ts` با سیاست صریح و مستند.
- نرخ مالیات، عیار مبنا و قواعد اجرت → **رکورد دیتابیسی نسخه‌دار با بازه‌ی اعتبار**، نه ثابت در کد. فاکتور پارسال باید امروز هم دقیقاً همان عدد را بدهد.

**مانده‌ها:** جدول `account_balance` به‌صورت projection افزایشی، نه `SUM()` روی کل تاریخچه. به‌روزرسانی در همان تراکنش دیتابیسی.

**پارتیشن‌بندی:** `ledger_entry` را بر اساس بازه‌ی زمانی پارتیشن کنید (RANGE، ماهانه یا سالانه). با ۵۰۰ هزار سند در سال برای هر مستأجر، از روز اول لازم است.

### ۲-۳. چندمستأجری

**Row-Level Security پستگرس** با `tenant_id` روی همه‌ی جداول:

```sql
ALTER TABLE ledger_entry ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON ledger_entry
  USING (tenant_id = current_setting('app.tenant_id')::uuid);
```

نشت داده بین مشتریان دیگر به یادآوری `WHERE tenant_id = ?` توسط برنامه‌نویس وابسته نیست. برای نصب محلی، همان کد بدون تغییر با یک مستأجر کار می‌کند.

### ۲-۴. الزامات API

- **Idempotency Key اجباری** روی هر endpoint نوشتنی — حتی بدون آفلاین کامل، شبکه‌ی موبایل درخواست تکراری تولید می‌کند.
- **شماره‌گذاری بدون شکاف (Gapless)** برای فاکتور — الزام قانونی مؤدیان. با جدول شمارنده + `SELECT ... FOR UPDATE`، نه `SEQUENCE` (سکوئنس در Rollback شکاف می‌سازد).
- REST + zod. **GraphQL توصیه نمی‌شود.**
- فید قیمت لحظه‌ای با **SSE** نه WebSocket — جریان یک‌طرفه است و SSE از پشت پراکسی‌ها قابل‌اعتمادتر است. روی موبایل، هنگام رفتن اپ به پس‌زمینه اتصال را ببندید و هنگام بازگشت با یک درخواست snapshot دوباره همگام کنید.
- **پاسخ‌های سبک برای موبایل:** endpointهای فهرست باید صفحه‌بندی مبتنی بر cursor و انتخاب فیلد داشته باشند. فرستادن ۵۰۰ ردیف کامل به گوشی روی شبکه‌ی بازار، محصول را کند نشان می‌دهد.
- BullMQ برای کارهای پس‌زمینه: ارسال به مؤدیان با تلاش مجدد نمایی، پیامک، گزارش‌ها، پشتیبان‌گیری.

---

## ۳. کلاینت — یک PWA موبایل‌فرست

```
React 18 + TypeScript + Vite
├── مسیریابی        TanStack Router
├── داده‌ی سرور      TanStack Query (با persist روی IndexedDB)
├── وضعیت محلی      Zustand
├── فرم             react-hook-form + zod (اسکیمای مشترک با بک‌اند)
├── جدول/فهرست      TanStack Table + TanStack Virtual
├── نمودار          Apache ECharts (RTL و کارایی بهتر)
├── تاریخ           date-fns-jalali
├── ذخیره‌ی محلی    Dexie (IndexedDB)
├── Service Worker  Workbox (vite-plugin-pwa)
└── UI              Tailwind CSS + shadcn/ui
```

### ۳-۱. قواعد طراحی موبایل‌فرست

**موبایل‌فرست یعنی از عرض ۳۶۰ پیکسل شروع کنید، نه اینکه دسکتاپ را کوچک کنید.** هر صفحه ابتدا برای گوشی طراحی و ساخته شود، سپس با افزایش عرض، تراکم اطلاعات بالا برود.

| قاعده | جزئیات |
|---|---|
| نقطه‌ی شروع | ۳۶۰–۳۹۰px. نقاط شکست: `640 / 1024 / 1440` |
| اندازه‌ی هدف لمسی | حداقل ۴۴×۴۴ پیکسل. بدون استثنا. |
| ناوبری | نوار پایین (Bottom Nav) با ۴–۵ آیتم روی موبایل؛ تبدیل به سایدبار در ≥1024px |
| منطقه‌ی شست | اقدام اصلی هر صفحه در یک‌سوم پایین صفحه. هرگز بالای صفحه. |
| جدول‌ها | زیر ۶۴۰px جدول وجود ندارد → **فهرست کارتی** با ۳ تا ۴ فیلد کلیدی. جدول کامل فقط در دسکتاپ. |
| مودال | زیر ۶۴۰px همه‌ی مودال‌ها **Bottom Sheet تمام‌صفحه** می‌شوند. |
| نوار جمع | جمع فاکتور به‌صورت sticky در پایین صفحه، همیشه دیده شود. |
| Hover | هیچ اطلاعات یا اقدامی نباید فقط با hover در دسترس باشد. |

### ۳-۲. ورود داده — بحرانی‌ترین بخش رابط کاربری

هدف PRD «ثبت معامله زیر ۱۵ ثانیه» است. روی موبایل این فقط با یک تصمیم محقق می‌شود:

> **صفحه‌کلید عددی سفارشی درون‌برنامه‌ای بسازید. به کیبورد سیستم‌عامل تکیه نکنید.**

دلیل: کیبورد بومی نیمی از صفحه را می‌گیرد، تأخیر باز و بسته شدن دارد، چیدمانش قابل کنترل نیست، و کلید «گرم / عیار / مظنه» ندارد. یک کیپد سفارشی به شما اجازه می‌دهد:

- کلیدهای میان‌بر دامنه‌ای بگذارید: `۷۵۰`، `۹۹۵`، `/۵`، `×۲`، «گرم ← مثقال»
- بدون بستن کیپد بین فیلدها حرکت کنید (Next/Done درون خود کیپد)
- محاسبه‌ی زنده‌ی قیمت را بالای کیپد نشان دهید
- ورودی را همان لحظه اعتبارسنجی کنید (عیار بین ۳۰۰ تا ۹۹۹)

نکات تکمیلی: `inputMode="decimal"` روی همه‌ی فیلدهای عددی برای زمانی که کاربر کیبورد سیستمی را ترجیح می‌دهد؛ `enterkeyhint="next"`؛ و غیرفعال‌کردن autocorrect و autocapitalize روی فیلدهای کد کالا.

### ۳-۳. نکات فارسی و RTL

- خاصیت‌های منطقی CSS (`margin-inline-start`، کلاس‌های `ps-/pe-/ms-/me-` تیلویند) به‌جای `left/right`.
- فونت وزیرمتن یا ایران‌سنس؛ اعداد جدول‌ها حتماً **Tabular Numbers** وگرنه ستون ارقام موقع تغییر می‌لرزد.
- **نرمال‌سازی متن هنگام نوشتن در دیتابیس:** `ی/ي`، `ک/ك`، ارقام عربی/فارسی/لاتین، نیم‌فاصله. بدون این، جست‌وجوی نام مشتری و مدل کالا هرگز درست کار نمی‌کند.
- اعداد را همیشه با `Intl.NumberFormat('fa-IR')` قالب‌بندی کنید، ولی **مقدار ذخیره‌شده همیشه لاتین و صحیح** باشد.

### ۳-۴. بودجه‌ی کارایی (الزام، نه آرزو)

اینترنت پاساژهای بازار ضعیف است. بدون بودجه‌ی صریح، محصول روی گوشی کند خواهد بود:

- بسته‌ی اولیه‌ی JS: **< ۲۰۰KB فشرده** (code-splitting بر اساس ماژول)
- LCP روی شبکه‌ی 3G شبیه‌سازی‌شده: **< ۲.۵ ثانیه**
- INP (پاسخ به تعامل): **< ۲۰۰ms**
- این اعداد را در CI با Lighthouse CI اندازه بگیرید و بیلد را در صورت عبور از بودجه fail کنید.

---

## ۴. استراتژی آفلاین — تصمیمی که باید صریح بگیرید

با حذف اپ بومی، پاسخ «آفلاین کامل» دیگر روی میز نیست. سه سطح ممکن است:

| سطح | توضیح | ارزیابی من |
|---|---|---|
| **سطح ۰ — فقط آنلاین** | بدون شبکه، برنامه کار نمی‌کند | برای خرده‌فروش قابل قبول، برای بازاریاب نه |
| **سطح ۱ — تاب‌آور (Resilient)** | پوسته‌ی برنامه و داده‌ی مرجع کش می‌شود؛ فاکتور در قطعی به‌صورت **پیش‌نویس محلی** ثبت و پس از اتصال ارسال می‌شود | ✅ **پیشنهاد من برای MVP** |
| **سطح ۲ — آفلاین کامل** | دفتر کل محلی، محاسبه‌ی مانده آفلاین | خارج از توان یک PWA. اگر واقعاً لازم شد، همان نقطه‌ای است که باید درباره‌ی اپ بومی تجدیدنظر کنید. |

### پیاده‌سازی سطح ۱

```
Workbox Service Worker
├── App Shell            → Precache (نصب در زمان بیلد)
├── داده‌ی مرجع          → StaleWhileRevalidate
│   (کالاها، مشتریان، قواعد اجرت، آخرین مظنه)
└── عملیات نوشتنی        → صف Outbox در IndexedDB
                           با client_operation_id یکتا
```

**قواعد غیرقابل مذاکره:**

1. **پیش‌نویس، نه سند.** هرچه آفلاین ثبت می‌شود «پیش‌نویس» است و در رابط کاربری صراحتاً با نشان «ارسال‌نشده» دیده می‌شود. سند رسمی فقط پس از تأیید سرور ساخته می‌شود.
2. **شماره‌ی فاکتور را کلاینت صادر نمی‌کند.** شناسه‌ی موقت محلی؛ شماره‌ی رسمی را سرور هنگام همگام‌سازی می‌دهد (الزام gapless numbering).
3. **قیمت آفلاین برچسب زمان دارد.** «مظنه‌ی ۱۴:۳۲ — آفلاین» روی فاکتور نمایش داده شود. هرگز وانمود نکنید قیمت به‌روز است.
4. **تعارض را حل نکنید، نشان دهید.** سرور مرجع نهایی است. عملیات ردشده به وضعیت «نیازمند بررسی» می‌رود و کاربر تصمیم می‌گیرد. ادغام خودکار داده‌ی مالی ممنوع.
5. **CRDT استفاده نکنید.** ادغام خودکار دقیقاً همان چیزی است که در پول نمی‌خواهید.

### ⚠️ محدودیت واقعی iOS — این را جدی بگیرید

- در سافاری iOS، اگر کاربر PWA را **به صفحه‌ی اصلی اضافه نکرده باشد**، داده‌ی IndexedDB پس از حدود ۷ روز عدم استفاده پاک می‌شود.
- `Background Sync API` در سافاری پشتیبانی نمی‌شود → همگام‌سازی فقط وقتی انجام می‌شود که کاربر برنامه را **باز کند**.
- اعلان Push در iOS فقط برای PWA نصب‌شده و از نسخه‌ی ۱۶.۴ به بعد کار می‌کند.

**پیامد عملی:** فرآیند نصب PWA (Add to Home Screen) را به بخشی از آموزش کاربر تبدیل کنید، و یک نشانگر دائمی «۳ فاکتور ارسال‌نشده» در رابط کاربری بگذارید تا کاربر خودش برنامه را باز کند. اگر پس از چند ماه دیدید بازاریاب‌ها به‌خاطر همین محدودیت داده از دست می‌دهند، آن سیگنالِ ساختن اپ بومی است — نه زودتر.

---

## ۵. سخت‌افزار بدون اپ دسکتاپ

با حذف Electron، دسترسی به سخت‌افزار باید از داخل مرورگر حل شود. وضعیت واقعی:

| سخت‌افزار | راهکار در مرورگر | ارزیابی |
|---|---|---|
| **بارکدخوان** | اسکنرهای USB به‌صورت **Keyboard Wedge** کار می‌کنند — یعنی مثل تایپ کیبورد. **صفر خط کد یکپارچه‌سازی.** | ✅ کاملاً حل‌شده |
| **اسکن بارکد با دوربین موبایل** | `BarcodeDetector API` (اندروید/کروم) با fallback به `zxing-wasm` | ✅ حل‌شده — و این دقیقاً همان چیزی است که موبایل‌فرست را ارزشمند می‌کند |
| **ترازوی دیجیتال** | `Web Serial API` — فقط کروم/اج روی دسکتاپ. در سافاری و iOS وجود ندارد. | ⚠️ جزئی. برای گالری‌دار روی دسکتاپ کار می‌کند؛ روی موبایل نه. |
| **چاپ رسید/برچسب** | چاپ از طریق دیالوگ سیستم با CSS `@media print`. کنترل دقیق چاپگر حرارتی از مرورگر ممکن نیست. | ⚠️ محدود |
| **RFID** | از مرورگر عملاً در دسترس نیست | ❌ به فاز بعد موکول شود |

**راهکار پیشنهادی برای ترازو و چاپگر:** یک **Local Bridge Agent** بسیار کوچک — یک باینری تک‌فایلی (Go یا Node SEA، حدود ۱۰MB) که فقط روی سیستم مشتریانی نصب می‌شود که ترازوی متصل دارند. یک وب‌سوکت روی `localhost` باز می‌کند و وب‌اپ به آن وصل می‌شود.

این کار مزیت مهمی دارد: به‌جای نگه‌داری یک اپ دسکتاپ کامل، فقط یک پل ۵۰۰ خطی نگه می‌دارید که سالی یک بار تغییر می‌کند. **و آن را در فاز ۱ نسازید** — با ورود دستی وزن شروع کنید و ببینید واقعاً چند مشتری درخواستش می‌دهند.

---

## ۶. زیرساخت و ملاحظات اجرای داخل ایران

| موضوع | راهکار |
|---|---|
| رجیستری Docker | Docker Hub در دسترس نیست → میرور داخلی + کش ایمیج‌های پایه در رجیستری خصوصی خودتان |
| بسته‌های npm | میرور داخلی + `npm ci` با lockfile قفل‌شده؛ یک `verdaccio` خصوصی |
| CI/CD | GitLab CE یا Gitea + Runner روی سرور خودتان. به GitHub Actions وابسته نشوید. |
| میزبانی | ابر داخلی یا سرور اختصاصی؛ نصب محلی برای مشتریان بزرگ |
| پیامک | کاوه‌نگار / SMS.ir / ملی‌پیامک — پشت یک لایه‌ی انتزاعی تا قابل تعویض باشد |
| پرداخت | درگاه‌های شاپرکی داخلی |
| رصد | Prometheus + Grafana + **GlitchTip** (جایگزین متن‌باز و خودمیزبان Sentry) |
| پشتیبان | **pgBackRest** یا WAL-G با ارسال به MinIO در دو موقعیت جغرافیایی. RPO ۱۵ دقیقه یعنی WAL archiving مستمر، نه dump شبانه. |
| استقرار | Docker Compose در فاز ۱ و ۲. Kubernetes تا پیش از چند ده مستأجر فعال، هزینه‌ی بی‌بازده است. |

**تست بازیابی:** ماهانه، خودکار، مستند. پشتیبانی که بازیابی‌اش تست نشده، پشتیبان نیست.

---

## ۷. استراتژی تست

- **Property-based testing روی دفتر کل:** برای هر توالی تصادفی از تراکنش‌ها، ناوردای `SUM=0` در هر دو واحد باید برقرار بماند. (`fast-check`)
- **Golden tests روی موتور قیمت‌گذاری:** مجموعه‌ای از فاکتورهای واقعی با نتیجه‌ی دستی‌محاسبه‌شده به‌عنوان مرجع ثابت.
- **تست PWA در شرایط واقعی:** Playwright با شبیه‌سازی offline/3G؛ سناریوی «قطع وسط ارسال → باز کردن دوباره → ارسال مجدد».
- **تست روی دستگاه واقعی:** حداقل یک اندروید میان‌رده و یک آیفون. شبیه‌ساز کافی نیست — محدودیت‌های ذخیره‌سازی سافاری فقط روی دستگاه واقعی دیده می‌شوند.
- **Lighthouse CI** با بودجه‌ی کارایی به‌عنوان دروازه‌ی merge.
- تست بار روی ۵۰۰ هزار سند پیش از فروش به اولین مشتری بزرگ.

---

## ۸. آنچه نباید انجام دهید

| ضدالگو | چرا |
|---|---|
| طراحی دسکتاپ و بعد «ریسپانسیو کردن» | این موبایل‌فرست نیست. نتیجه‌اش همیشه جدول‌های فشرده و دکمه‌های ریز است. |
| تکیه بر کیبورد سیستم‌عامل برای ورود اعداد | هدف ۱۵ ثانیه محقق نمی‌شود. |
| وعده‌ی «آفلاین کامل» با PWA | در iOS قابل تحقق نیست؛ اعتماد کاربر را از دست می‌دهید. |
| میکروسرویس در فاز ۱ | نصب محلی را غیرممکن و تیم کوچک را فلج می‌کند. |
| MongoDB یا NoSQL برای دفتر کل | تراکنش ACID چندسندی و ناوردای دفتر کل نیاز قطعی است. |
| `float` / `double` برای پول یا وزن | ضمانت‌شده منجر به مغایرت ریالی می‌شود. |
| CRDT برای داده‌ی مالی | ادغام خودکار پول = فاجعه. |
| GraphQL | هزینه بدون منفعت در این دامنه. |
| Kubernetes پیش از ۵۰ مستأجر | هزینه‌ی عملیاتی بدون بازده. |
| هاردکد کردن نرخ مالیات یا عیار مبنا | قواعد تغییر می‌کنند؛ اسناد گذشته باید بازتولیدپذیر بمانند. |

---

## ۹. تیم حداقلی برای MVP

| نقش | تعداد | تمرکز |
|---|---|---|
| بک‌اند ارشد | ۱ | دفتر کل، موتور قیمت‌گذاری |
| فرانت‌اند ارشد | ۱ | PWA، کیپد سفارشی، کارایی موبایل |
| فول‌استک | ۱ | ماژول‌های عملیاتی |
| طراح محصول | ۰.۵ | جریان ورود داده روی صفحه‌ی کوچک — نه زیبایی‌شناسی |
| مشاور دامنه (طلافروش فعال) | پاره‌وقت | **حیاتی‌ترین نقش تیم** |

نسبت به نسخه‌ی قبل، نقش توسعه‌دهنده‌ی موبایل حذف و در عوض **فرانت‌اند ارشد ارتقا یافت**. با حذف اپ بومی، کیفیت PWA تنها چیزی است که کاربر می‌بیند؛ اینجا صرفه‌جویی نکنید.

آخرین ردیف را حذف نکنید. یک مشاور صنفی که هفته‌ای دو ساعت خروجی را نقد کند، از دو برنامه‌نویس اضافی ارزشمندتر است.
