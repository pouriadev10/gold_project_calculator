this front end tasks : # TASKS-FRONTEND.md — Frontend Phase 1
## دستور اجرای تسک‌ها
* همیشه اولین تسک تیک‌نخورده را انجام بده.
* قبل از هر تسک `CLAUDE.md` را بخوان.
* قبل از هر تسک UI، این فایل‌ها را بخوان:
  * `design-system/pages/<page-name>.md`
  * در صورت نبودن فایل صفحه: `design-system/MASTER.md`
* هر تسک در یک کامیت مستقل انجام شود.
* پیام کامیت باید با شناسه تسک شروع شود:
```text
[FE-001] Audit frontend foundation
```
* هیچ ویژگی خارج از فاز ۱ اضافه نکن.
* هیچ فایل بک‌اندی را تغییر نده، مگر تغییر صریح در `packages/contracts` یا workspace جزو همان تسک باشد.
* هیچ مقدار پولی یا وزنی را با `number` نگهداری نکن.
* پول و وزن دریافتی از API همیشه `string` هستند.
* هرگونه تبدیل به `bigint` فقط در utilityهای مشخص انجام شود.
* هیچ `Math.round`، `parseFloat` یا `toFixed` جدیدی اضافه نکن.
* تمام گرد کردن‌ها از `packages/core-calc` استفاده کنند.
* اعداد نمایشی با `Intl.NumberFormat('fa-IR')` فرمت شوند.
* مقادیر ذخیره‌شده و ارسالی همیشه لاتین باشند.
* هیچ اطلاعات یا عملیاتی فقط با hover قابل دسترس نباشد.
* اندازه هدف لمسی حداقل ۴۴×۴۴ پیکسل باشد.
* زیر ۶۴۰px جدول نمایش داده نشود.
* مودال‌ها زیر ۶۴۰px به Bottom Sheet تمام‌صفحه تبدیل شوند.
* CSS جهت‌دار `left/right` استفاده نشود؛ فقط logical properties.
* اقدام اصلی صفحات عملیاتی در یک‌سوم پایین صفحه باشد.
* تمام صفحات دارای عدد مالی باید کلید سراسری طلا/ریال داشته باشند.
* فرانت محاسبات نهایی مالی را معتبر فرض نکند؛ نتیجه نهایی همیشه از API تأیید شود.
* از شروع خودکار تسک بعدی خودداری کن.
---
# Milestone 1 — بررسی و تثبیت زیرساخت فعلی
## [x] FE-001 — ممیزی وضعیت فعلی فرانت
### هدف
بررسی دقیق کد موجود و ثبت اینکه کدام بخش‌های Foundation قبلاً پیاده‌سازی شده‌اند.
### فایل خروجی
```text
docs/frontend-audit.md
```
### موارد بررسی
* React و Vite
* TypeScript strict
* TanStack Router
* TanStack Query
* Tailwind
* shadcn/ui
* React Hook Form
* Zod
* PWA
* RTL
* فونت وزیرمتن
* Tabular Numbers
* `packages/core-calc`
* بودجه JS
* ESLint rules
* تست‌ها
* design system
* صفحات فعلی
* Mock dataهای فعلی
* API client فعلی
* ساختار state management
### خروجی گزارش
برای هر مورد مشخص شود:
* انجام شده
* ناقص
* انجام نشده
* نیازمند اصلاح
* مسیر فایل مرتبط
### محدودیت‌ها
* در این تسک کد عملیاتی جدید نوشته نشود.
* فقط اصلاح بسیار کوچک برای اجرای audit مجاز است.
* چیزی که وجود دارد دوباره طراحی نشود.
### تمام است وقتی
* وضعیت واقعی تمام Foundation ثبت شده باشد.
* تسک‌های بعدی با نتایج audit تعارض نداشته باشند.
* موارد پیاده‌شده مشخص و قابل تیک‌زدن باشند.
---
## [x] FE-002 — یکپارچه‌سازی اسکریپت‌های فرانت
### هدف
ایجاد دستورات استاندارد برای کنترل کیفیت فرانت.
### اسکریپت‌ها
```json
{
  "dev": "...",
  "build": "...",
  "preview": "...",
  "typecheck": "...",
  "lint": "...",
  "test": "...",
  "test:watch": "...",
  "test:e2e": "...",
  "analyze": "..."
}
```
### تمام است وقتی
این دستورات از ریشه کار کنند:
```bash
pnpm --filter web dev
pnpm --filter web build
pnpm --filter web typecheck
pnpm --filter web lint
pnpm --filter web test
```
---
## [x] FE-003 — Gate کنترل کیفیت فرانت
### هدف
ساخت یک دستور واحد برای بررسی فرانت.
### دستور
```bash
pnpm gate:frontend
```
### شامل
* typecheck
* lint
* unit tests
* component tests
* build
* بررسی حجم bundle
* بررسی ممنوعیت `Math.round`
* بررسی ممنوعیت `parseFloat`
* بررسی ممنوعیت `toFixed`
* بررسی ممنوعیت `any`
* بررسی ممنوعیت `@ts-ignore`
* بررسی کلاس‌های جهت‌دار Tailwind
* بررسی import مستقیم از `apps/api`
### تمام است وقتی
* نقض هر قانون build را fail کند.
* خروجی مسیر فایل و قانون نقض‌شده را نشان دهد.
* CI بتواند همان دستور را اجرا کند.
---
## [x] FE-004 — تثبیت ساختار پوشه‌های فرانت
### ساختار پیشنهادی
```text
apps/web/src/
├── app/
│   ├── router/
│   ├── providers/
│   ├── layouts/
│   └── guards/
├── features/
│   ├── auth/
│   ├── pricing/
│   ├── parties/
│   ├── inventory/
│   ├── sales/
│   ├── purchase/
│   ├── settlements/
│   ├── invoices/
│   └── reporting/
├── shared/
│   ├── api/
│   ├── components/
│   ├── forms/
│   ├── hooks/
│   ├── lib/
│   ├── formatters/
│   ├── schemas/
│   └── types/
└── test/
```
### قواعد
* ساختار موجود بدون دلیل بازنویسی نشود.
* منطق Feature در `features` بماند.
* componentهای عمومی فقط در `shared` قرار بگیرند.
* هیچ Feature مستقیماً internals Feature دیگر را import نکند.
### تمام است وقتی
* boundaryها مستند شده باشند.
* circular dependency وجود نداشته باشد.
* aliasهای TypeScript و Vite درست باشند.
---
# Milestone 2 — قرارداد API و زیرساخت ارتباط
## [x] FE-005 — اتصال `packages/contracts` به فرانت
### وابستگی بک‌اند
```text
BE-003
```
### هدف
استفاده از schemaها و typeهای مشترک API.
### کارها
* اضافه کردن package به dependencies
* import schemaهای مشترک
* حذف typeهای تکراری API در فرانت
* جلوگیری از import مدل‌های دیتابیس
* تعریف typeهای local مخصوص view model
### تمام است وقتی
* قراردادهای مشترک از یک منبع import شوند.
* هیچ DTO مالی تکراری در فرانت وجود نداشته باشد.
* build فرانت مستقل باقی بماند.
---
## [x] FE-006 — API Client پایه
### هدف
ساخت client مرکزی REST.
### قابلیت‌ها
* Base URL از env
* ارسال JSON
* دریافت JSON
* timeout
* AbortSignal
* request ID
* parsing قرارداد خطا
* اعتبارسنجی response با Zod در endpointهای حساس
* عدم استفاده مستقیم از `fetch` در Featureها
### ساختار پیشنهادی
```text
shared/api/
├── api-client.ts
├── api-error.ts
├── request.ts
└── response.ts
```
### تمام است وقتی
* GET و POST نمونه کار کنند.
* timeout و abort تست شده باشند.
* خطای شبکه با خطای دامنه تفکیک شود.
---
## [x] FE-007 — مدیریت Idempotency-Key
### وابستگی بک‌اند
```text
BE-013
```
### هدف
ارسال کلید یکتا برای تمام عملیات نوشتنی.
### کارها
* utility ساخت UUID
* نگهداری کلید تا مشخص شدن نتیجه درخواست
* استفاده مجدد از همان key در retry همان عملیات
* تولید key جدید فقط برای عملیات جدید
* جلوگیری از ارسال دوباره با double tap
### قواعد
* retry شبکه نباید سند جدید بسازد.
* button disabled به‌تنهایی کافی نیست.
* کلید نباید هنگام rerender تغییر کند.
### تمام است وقتی
* double click فقط یک request منطقی ایجاد کند.
* retry همان key را بفرستد.
* شروع فرم جدید key جدید بسازد.
---
## [x] FE-008 — TanStack Query Provider و Query Keys
### هدف
استانداردسازی دریافت و mutation داده.
### کارها
* ایجاد QueryClient مرکزی
* retry policy
* stale time مناسب
* query key factory
* invalidation استاندارد
* مدیریت خطاهای auth
* عدم retry خودکار mutation مالی بدون idempotency
### تمام است وقتی
* keyها از factory استفاده کنند.
* invalidationهای تکراری حذف شده باشند.
* mutationهای مالی رفتار قابل پیش‌بینی داشته باشند.
---
## [x] FE-009 — مدیریت خطاهای API
### وابستگی بک‌اند
```text
BE-017
```
### قابلیت‌ها
* نمایش خطای field-level
* نمایش conflict
* نمایش session expired
* نمایش forbidden
* نمایش network unavailable
* ثبت request ID در بخش جزئیات
* پیام فارسی قابل فهم
### قواعد
* متن خام PostgreSQL یا stack trace نمایش داده نشود.
* خطای مالی با toast کوتاه ناپدید نشود؛ در صفحه هم باقی بماند.
* خطای validation کنار فیلد نمایش داده شود.
### تمام است وقتی
* تمام error codeهای اصلی map شده باشند.
* خطاهای ناشناخته fallback امن داشته باشند.
* تست component برای حالت‌های اصلی وجود داشته باشد.
---
## [x] FE-010 — مدیریت رشته‌های BigInt
### هدف
مدیریت امن پول و وزن بدون استفاده از number.
### utilityها
```text
parseBigIntString
formatRial
formatWeightMg
formatIntegerString
compareBigIntStrings
addBigIntStrings
subtractBigIntStrings
```
### قواعد
* برای نمایش می‌توان از `bigint` استفاده کرد.
* در state فرم، مقدار اصلی string باقی بماند.
* تبدیل به `Number` برای پول و وزن ممنوع باشد.
* مقدار فارسی قبل از validation به لاتین تبدیل شود.
### تمام است وقتی
* مقادیر بزرگ‌تر از `Number.MAX_SAFE_INTEGER` صحیح نمایش داده شوند.
* تست صفر، منفی و عدد بسیار بزرگ وجود داشته باشد.
* هیچ precision loss رخ ندهد.
---
# Milestone 3 — طراحی پایه و Shell برنامه
## [x] FE-011 — تثبیت Design System
### هدف
تکمیل Design Tokenهای اصلی محصول.
### موارد
* رنگ پس‌زمینه
* رنگ سطح
* رنگ متن
* رنگ هشدار
* رنگ خطا
* رنگ موفقیت
* spacing
* radius
* shadow حداقلی
* typography
* font weight
* target size
* input height
* bottom navigation height
* desktop sidebar width
### قواعد
* Glassmorphism ممنوع.
* Gradient mesh ممنوع.
* انیمیشن سنگین ممنوع.
* رنگ طلایی صرفاً decorative و محدود باشد.
* کنتراست حداقل ۴.۵:۱.
### خروجی
```text
design-system/MASTER.md
```
### تمام است وقتی
* tokenها در Tailwind و CSS variables هم‌راستا باشند.
* حالت light کامل باشد.
* dark mode فقط اگر از قبل وجود دارد تثبیت شود؛ اضافه کردن آن ضروری نیست.
---
## [x] FE-012 — App Shell موبایل‌فرست
### هدف
ساخت Layout اصلی برنامه.
### موبایل
* Header فشرده
* Bottom navigation
* محتوای scrollable
* ناحیه امن پایین
* action اصلی نزدیک پایین صفحه
### دسکتاپ
* Sidebar
* Header
* content area
* max width مناسب
### صفحات Navigation فاز ۱
* داشبورد
* فروش
* خرید
* اشخاص
* موجودی
* گزارش‌ها
* تنظیمات
### تمام است وقتی
* عرض ۳۶۰px بدون overflow افقی کار کند.
* نقاط شکست ۶۴۰، ۱۰۲۴ و ۱۴۴۰ بررسی شوند.
* navigation با کیبورد قابل استفاده باشد.
---
## [x] FE-013 — Route Structure
### هدف
تعریف routeهای فاز ۱ با TanStack Router.
### routeها
```text
/login
/dashboard
/pricing
/parties
/parties/:partyId
/inventory
/inventory/jewelry
/inventory/coins
/sales/new
/sales/invoices
/sales/invoices/:invoiceId
/purchase/second-hand
/settlements/new
/reporting/debtors
/reporting/creditors
/reporting/profit
/settings
```
### قواعد
* routeهای خارج فاز ۱ ساخته نشوند.
* lazy loading برای featureهای بزرگ استفاده شود.
* route params با schema اعتبارسنجی شوند.
### تمام است وقتی
* routeها type-safe باشند.
* صفحه Not Found وجود داشته باشد.
* chunkهای اصلی lazy load شوند.
---
## [x] FE-014 — Loading، Empty و Error States عمومی
### componentها
* PageSkeleton
* CardSkeleton
* EmptyState
* ErrorState
* InlineError
* RetryPanel
* FullPageLoading
### قواعد
* Skeleton باعث layout shift نشود.
* Empty State فقط decorative نباشد؛ اقدام مرتبط ارائه دهد.
* متن خطا کوتاه و قابل اقدام باشد.
### تمام است وقتی
* حداقل سه Feature از componentهای مشترک استفاده کنند.
* هیچ صفحه‌ای هنگام loading سفید نماند.
---
## [x] FE-015 — Bottom Sheet و Dialog واکنش‌گرا
### هدف
ایجاد abstraction مشترک.
### رفتار
* زیر ۶۴۰px: Bottom Sheet تمام‌صفحه
* بالای ۶۴۰px: Dialog
* focus trap
* escape
* restore focus
* drag handle فقط اگر دسترس‌پذیری را خراب نکند
### تمام است وقتی
* فرم‌های داخل Sheet با کیبورد کار کنند.
* scroll body و scroll sheet تداخل نداشته باشند.
* روی Android واقعی تست شود.
---
## [x] FE-016 — Toast و اعلان‌های عملیات
### قواعد
* موفقیت کوتاه
* خطای مالی پایدارتر
* عدم وابستگی تنها به رنگ
* جلوگیری از چند toast تکراری
* عدم نمایش اطلاعات حساس
### تمام است وقتی
* ثبت موفق، خطا و conflict پوشش داده شوند.
* screen reader پیام را دریافت کند.
---
# Milestone 4 — ورودی عددی و ابزارهای دامنه‌ای
## [x] FE-017 — نرمال‌سازی ارقام و متن فارسی
### هدف
نرمال‌سازی ورودی قبل از validation.
### موارد
* ارقام فارسی به لاتین
* ارقام عربی به لاتین
* `ي` به `ی`
* `ك` به `ک`
* trim
* فاصله اضافی
* نیم‌فاصله
### تمام است وقتی
* ورودی فارسی و لاتین نتیجه یکسان داشته باشند.
* تست برای نام، موبایل، مبلغ و وزن وجود داشته باشد.
---
## [x] FE-018 — Numeric Input پایه
### قابلیت‌ها
* مقدار داخلی string
* نمایش فارسی اختیاری
* caret پایدار
* پاک کردن
* paste
* inputMode مناسب
* عدم استفاده از `type="number"`
* پشتیبانی از اعداد بزرگ
* نمایش suffix ریال، گرم یا عدد
### تمام است وقتی
* precision از بین نرود.
* paste مقدار فارسی کار کند.
* مقدار نامعتبر وارد state نهایی نشود.
---
## [x] FE-019 — کیپد عددی سفارشی
### هدف
ساخت کیپد لمسی داخل برنامه.
### کلیدها
* ۰ تا ۹
* حذف
* پاک کردن
* تأیید
* ممیز نمایشی برای گرم ورودی
* ۷۵۰
* ۷۴۰
* ۹۹۵
* ۹۲۵
* گرم
* مثقال
### قواعد
* مقدار نهایی وزن به میلی‌گرم صحیح تبدیل شود.
* shortcutها configurable باشند.
* کیپد سیستم‌عامل به‌تنهایی معیار نباشد.
* هدف لمسی حداقل ۴۴×۴۴.
### تمام است وقتی
* ورود وزن و مبلغ بدون کیبورد سیستم ممکن باشد.
* screen reader label وجود داشته باشد.
* prefers-reduced-motion رعایت شود.
---
## [x] FE-020 — Weight Input
### قابلیت‌ها
* ورود گرم
* ورود میلی‌گرم
* نمایش گرم
* تبدیل گرم به میلی‌گرم
* تبدیل مثقال به میلی‌گرم با `core-calc`
* عیار اختیاری
* validation
* نمایش وزن خالص
### قواعد
* state اصلی storage-ready باشد.
* float JS برای محاسبه استفاده نشود.
* تبدیل فقط با utility دقیق انجام شود.
### تمام است وقتی
* `23.500` گرم دقیقاً به مقدار صحیح میلی‌گرم تبدیل شود.
* ورودی‌های مرزی تست شوند.
---
## [x] FE-021 — Money Input
### قابلیت‌ها
* رشته ریالی
* جداساز هزارگان در نمایش
* مقدار لاتین در payload
* دکمه پاک کردن
* ارقام فارسی
* مقادیر بسیار بزرگ
### تمام است وقتی
* فرمت نمایش و مقدار ارسالی جدا باشند.
* کاربر بتواند مقدار را بدون پرش caret ویرایش کند.
---
## [x] FE-022 — Karat Input
### قابلیت‌ها
* مقدار integer بین ۱ و ۱۰۰۰
* shortcutهای ۷۴۰، ۷۵۰، ۹۰۰، ۹۹۵
* خواندن مقادیر پیش‌فرض از تنظیمات
* عدم هاردکد در منطق Feature
### تمام است وقتی
* مقدار خارج بازه رد شود.
* مقدار پیش‌فرض API نمایش داده شود.
* کاربر مجاز بتواند مقدار را تغییر دهد.
---
# Milestone 5 — تنظیمات سراسری کاربر
## [x] FE-023 — Unit Display Store
### هدف
پیاده‌سازی تنظیم سراسری نمایش.
### حالت‌های فاز ۱
```text
GOLD
RIAL
```
### قابلیت‌ها
* پیش‌فرض GOLD
* persistence در local storage
* امکان تغییر لحظه‌ای
* sync بین tabها
* hook مشترک
### قواعد
* state حسابداری تغییر نکند.
* فقط representation تغییر کند.
* Featureها store جدا نسازند.
### تمام است وقتی
* تغییر در یک صفحه روی بقیه صفحات اثر بگذارد.
* refresh انتخاب را حفظ کند.
* کاربر بتواند در همان صفحه تغییر دهد.
---
## [x] FE-024 — Unit Toggle عمومی
### هدف
ساخت component مشترک طلا/ریال.
### محل استفاده اجباری
* داشبورد
* مانده شخص
* صورت‌حساب
* بدهکاران
* بستانکاران
* سود
* درآمد
* کارت‌های مالی
* خلاصه فاکتور در صورت نمایش معادل
### تمام است وقتی
* تمام صفحات مالی از component مشترک استفاده کنند.
* حالت فعال برای screen reader مشخص باشد.
* target لمسی رعایت شود.
---
## [x] FE-025 — Formatter نمایش طلا و ریال
### قابلیت‌ها
* ریال
* گرم خالص ۱۰۰۰
* گرم معادل ۷۵۰
* مثقال در صورت نیاز نمایشی
* نمایش علامت مثبت/منفی
* نمایش بدهکار/بستانکار
* Tabular Numbers
### قواعد
* نرخ تبدیل باید از snapshot یا reference quote مشخص بیاید.
* تبدیل با نرخ امروز برای اسناد تاریخی ممنوع باشد.
* formatter فقط نمایش دهد و منطق حسابداری نسازد.
### تمام است وقتی
* حالت‌های مثبت، منفی، صفر و بدون نرخ تست شوند.
---
# Milestone 6 — احراز هویت
## [x] FE-026 — صفحه ورود
### وابستگی بک‌اند
```text
BE-011
```
### فیلدها
* نام کاربری یا موبایل
* رمز عبور
* انتخاب tenant در صورت نیاز
### قابلیت‌ها
* show/hide password
* loading
* خطای ورود
* session expired
* autofill مناسب
* جلوگیری از چند submit
### تمام است وقتی
* login موفق route را تغییر دهد.
* login ناموفق پیام مناسب نشان دهد.
* token خام در log نمایش داده نشود.
---
## [x] FE-027 — مدیریت Session
### قابلیت‌ها
* access token
* refresh token flow
* retry پس از refresh
* logout
* پاک‌سازی cache
* همگام‌سازی logout بین tabها
* redirect به login
### قواعد
* refresh loop ایجاد نشود.
* mutation مالی بعد از refresh بدون کنترل دوباره ارسال نشود.
* اطلاعات session در محل امن مناسب نگهداری شود.
### تمام است وقتی
* انقضای access token بدون از دست رفتن صفحه مدیریت شود.
* refresh ناموفق logout انجام دهد.
---
## [x] FE-028 — Route Guards و Permission UI
### وابستگی بک‌اند
```text
BE-012
```
### نقش‌ها
* OWNER
* MANAGER
* CASHIER
### رفتار
* route guard
* hide یا disable اقدام غیرمجاز
* صفحه Forbidden
* عدم اتکا به فرانت برای امنیت
### تمام است وقتی
* CASHIER تنظیمات حساس و گزارش سود را نبیند.
* لینک مستقیم route غیرمجاز پاسخ مناسب بدهد.
---
# Milestone 7 — مظنه
## [x] FE-029 — Widget مظنه جاری
### وابستگی بک‌اند
```text
BE-021
```
### نمایش
* مظنه
* زمان ثبت
* منبع
* وضعیت تازه یا قدیمی
* اقدام ورود دستی
### قواعد
* پول string باشد.
* زمان شمسی نمایش داده شود.
* قطع فید معامله را متوقف نکند.
### تمام است وقتی
* loading، empty، stale و error پوشش داده شوند.
* widget در Shell یا صفحات عملیاتی قابل استفاده باشد.
---
## [x] FE-030 — فرم ثبت مظنه دستی

> **وضعیت:** فقط «مبلغ مظنه» فیلد واقعی است. `createManualPriceQuoteSchema`
> (`@gold/contracts`, `.strict()`) فقط `quoteType` و `amountRial` می‌پذیرد؛
> «زمان مشاهده» ورودی نیست چون سرور خودش `observedAt` را در لحظه‌ی درج
> می‌سازد (`price-quotes.service.ts`, BE-021)، و «توضیح» ستونی در
> `price_quotes` ندارد. `quoteType` همیشه `'MAZNEH'` است — تنها عضو
> `priceQuoteTypeSchema` در فاز ۱، پس چیزی برای انتخاب نیست.
>
> «تأیید» یک `ResponsiveDialog` جدا است، نه مرحله‌ی دوم داخل همان فرم —
> این مقدار پایه‌ی محاسبه‌ی هر فاکتور بعدی می‌شود (۲-۸ CLAUDE.md). «نمایش
> نتیجه» مستقیماً از پاسخ سرور همان `submit` می‌آید (`RateDisplay` +
> `formatJalaliDistance`)، نه از یک fetch جدا؛ query هم با
> `queryKeys.pricing.all()` invalidate می‌شود تا `MaznehBar` (FE-029) و هر
> مصرف‌کننده‌ی دیگر مظنه‌ی جدید را ببینند.
>
> صفحه‌ی `/pricing` برای هر نقشی باز است (بدون `beforeLoad` نقش‌محور) —
> `RolesGuard` سمت سرور فقط روی `POST .../manual` نشسته
> (`@Roles('OWNER','MANAGER')`)، نه روی خواندن؛ فقط خودِ فرم با
> `useHasRole`/`MANUAL_QUOTE_ENTRY_ROLES` برای CASHIER پنهان می‌شود، همان
> الگوی لینک «ثبت دستی» در `MaznehBar`.
>
> ۱۲ تست (`ManualQuoteForm.test.tsx`, `PricingPage.test.tsx`) هر سه معیار
> «تمام است وقتی» را می‌سنجند، به‌علاوه‌ی گیت اعتبارسنجی، گفت‌وگوی تأیید،
> و ماندگاری خطای سرور در صفحه (نه فقط toast — قاعده‌ی FE-009).
> `pnpm gate:frontend` سبز: بسته‌ی اولیه ۱۴۴.۷KB فشرده (زیر بودجه‌ی
> ۲۰۰KB)، `PricingPage` چانک تنبل مستقل خودش را دارد.

### فیلدها
* مبلغ مظنه
* زمان مشاهده
* توضیح اختیاری
### قابلیت‌ها
* Money Input
* idempotency
* confirm
* نمایش نتیجه
* invalidate quote query
### تمام است وقتی
* مظنه ثبت شود.
* double submit سند تکراری نسازد.
* مقدار نامعتبر ارسال نشود.
---
## [x] FE-031 — تاریخچه مظنه

> **وضعیت:** بک‌اند صفحه‌بندی سرور-محور ندارد — `priceQuoteQuerySchema` فقط
> `quoteType` اختیاری دارد و `list()` (`price-quotes.service.ts`) کل فهرست
> tenant را یک‌جا برمی‌گرداند. `pagination` سمت کلاینت است: یک `useReactTable`
> با `getPaginationRowModel` (اندازه‌ی صفحه: ۱۰) روی همان آرایه‌ی کامل.
>
> هر دو نما — کارت زیر ۶۴۰px، جدول از ۶۴۰px به بالا — از **همان** صفحه‌ی
> جاریِ همان جدول می‌خوانند (`useMediaQuery`، همان ثابت شکست `ResponsiveDialog.tsx`)
> تا pagination یک منبع حقیقت داشته باشد، نه دو منطق موازی. این اولین
> استفاده‌ی واقعی `@tanstack/react-table` در یک صفحه‌ی production است
> (پیش‌تر فقط در هارنس دود اثبات شده بود)؛ پرایمیتیو `components/ui/table.tsx`
> (shadcn استاندارد، با `text-start` به‌جای `text-left`) هم همین‌جا اضافه شد.
>
> تاریخچه برای **هر نقشی** باز است، نه فقط OWNER/MANAGER —
> `GET /pricing/quotes` در `price-quotes.controller.ts` بدون `@Roles` است؛
> فقط `POST .../manual` محدود است. در `PricingPage`، `QuoteHistoryList`
> بیرون از شرط نقش رندر می‌شود.
>
> بعد از ثبت موفق مظنه‌ی دستی (FE-030)، فهرست خودکار تازه می‌شود — بدون
> کد اضافه، چون `history()` زیرِ همان `queryKeys.pricing.all()` است که
> `ManualQuoteForm` invalidate می‌کند.
>
> ۹ تست اضافه شد (`QuoteHistoryList.test.tsx` ۷، `PricingPage.test.tsx` ۲
> تازه) — بارگذاری/خطا/خالی، سوییچ جدول↔کارت، و pagination دو-صفحه‌ای.
> `pnpm gate:frontend` سبز: بسته‌ی اولیه هنوز ۱۴۴.۸KB فشرده (زیر بودجه)؛
> `@tanstack/react-table` فقط داخل چانک تنبل `PricingPage` است.

### قابلیت‌ها
* لیست
* زمان
* مبلغ
* منبع
* pagination
* حالت card روی موبایل
* جدول روی دسکتاپ
### تمام است وقتی
* زیر ۶۴۰px جدول وجود نداشته باشد.
* Tabular Numbers رعایت شود.
---
# Milestone 8 — اشخاص
## [x] FE-032 — فهرست اشخاص

> **وضعیت:** برخلاف تاریخچه‌ی مظنه (FE-031)، اینجا صفحه‌بندی **سرور-محور**
> واقعی است — `GET /parties` (`partyListQuerySchema`) واقعاً `limit`/`offset`
> می‌گیرد، پس `PartyList` به‌جای `useReactTable` فقط `limit`/`offset` را با
> والد (`PartiesPage`) رد و بدل می‌کند؛ `useParties` با
> `placeholderData: keepPreviousData` بین صفحات flash اسکلت نمی‌سازد.
>
> «مانده‌ی خلاصه» در کارت/جدول نیست: `partySchema` واقعی (BE-024) مانده‌ی
> هر شخص را برنمی‌گرداند — فقط یک تجمیع سراسری در `/parties/balance-summary`
> هست (BE-056/BE-058 هنوز نیامده). نمایش عددی که از سرور نیامده دقیقاً
> همان چیزی است که بخش ۲-۸ CLAUDE.md منع می‌کند؛ به‌جایش وضعیت
> (فعال/غیرفعال) چهارمین فیلد کارت شد — FE-069 با آمدن BE-056 این را با
> مانده‌ی واقعی جایگزین می‌کند.
>
> «ایجاد شخص» یک فرم سریع سه‌فیلدی است (نوع، نام، موبایل اختیاری) در
> `CreatePartyDialog` — نه فرم کامل با کد ملی/یادداشت/ویرایش؛ آن فرم کار
> مجزای FE-033 است («فرم ایجاد و ویرایش شخص»). `createPartySchema` مشترک
> مستقیم به‌عنوان resolver استفاده نشد چون فیلد اختیاری `mobile` آن
> `min(1)` می‌خواهد و رشته‌ی خالیِ input را رد می‌کند؛ یک schema محلی این
> را پیش از ارسال به «کلید حذف‌شده» تبدیل می‌کند.
>
> `components/ui/select.tsx` جدید عمداً روی `<select>` بومی است، نه
> `@radix-ui/react-select` — یک وابستگی تازه برای دو فیلتر ساده توجیهی
> ندارد و کیبورد سیستمی موبایل تجربه‌ی بهتری از popover دست‌ساز می‌دهد.
>
> ۲۷ تست تازه (`PartyList`, `CreatePartyDialog`, `PartiesPage`,
> `useDebouncedValue`) + fixtureهای mock از ۷ به ۱۴ شخص رسیدند (۲ غیرفعال)
> تا فیلتر وضعیت و صفحه‌بندی واقعاً قابل‌آزمون باشند (۱۴ رکورد با اندازه‌ی
> صفحه‌ی ۱۰ یعنی دقیقاً ۲ صفحه). `pnpm gate:frontend` سبز: بسته‌ی اولیه
> ۱۴۵.۱KB فشرده (زیر بودجه‌ی ۲۰۰KB)؛ تأیید دستی در مرورگر (جست‌وجو، هر دو
> فیلتر، pagination، ایجاد شخص با idempotency، نمای کارت و جدول) انجام شد.

### وابستگی بک‌اند
```text
BE-024
```
### قابلیت‌ها
* جست‌وجوی نام
* جست‌وجوی موبایل
* فیلتر نوع
* فیلتر وضعیت
* pagination
* ایجاد شخص
* نمایش مانده خلاصه در صورت آماده بودن API
### موبایل
هر کارت حداکثر ۴ داده کلیدی:
* نام
* موبایل
* نوع
* مانده خلاصه
### تمام است وقتی
* جست‌وجوی فارسی و لاتین یکسان باشد.
* empty و error state وجود داشته باشد.
* debounce کنترل‌شده باشد.
---
## [x] FE-033 — فرم ایجاد و ویرایش شخص

> **وضعیت:** یک کامپوننت (`PartyFormDialog`) هر دو حالت را پوشش می‌دهد،
> نه دو فرم جدا — پنج فیلد و اعتبارسنجی/نرمال‌سازی بین ایجاد و ویرایش
> عیناً یکی است؛ تنها تفاوت متد HTTP (`POST` در برابر `PATCH /parties/:id`
> تازه‌ی این تسک) و مقدار پیش‌فرض فیلدهاست. `party` prop تعیین‌کننده‌ی
> حالت است: نبودش ایجاد، حضورش ویرایش همان شخص. این با قاعده‌ی «مرجوعی
> B2B/B2C را با یک ماژول مشترک پیاده نکن» (بخش ۲-۵ CLAUDE.md) در تعارض
> نیست — آن دو از نظر حسابداری دو عملیات متفاوتند، این دو فقط دو مسیر
> رسیدن به یک شیء واحدند.
>
> `CreatePartyDialog` (FE-032، سه فیلد سریع) عمداً دست‌نخورده ماند و
> هنوز به دکمه‌ی «افزودن شخص» وصل است — متن خودِ همان گفت‌وگو («بقیه‌ی
> مشخصات را بعداً از صفحه‌ی شخص کامل کنید») دقیقاً همین معماری را از قبل
> پیش‌بینی کرده بود. آن «بعداً» همین‌جاست: چون FE-034 (صفحه‌ی جزئیات)
> هنوز placeholder است، دکمه‌ی ویرایش روی هر ردیف `PartyList` (کارت
> موبایل و جدول دسکتاپ، هر دو) `PartyFormDialog` را با همان شیء `Party`
> از پاسخ لیست باز می‌کند — نه با `useParty(id)` تازه، چون چنین hookی
> هنوز نیست.
>
> دکمه‌ی ویرایش عمداً بیرون از `Link` جزئیات است: `<button>` تودرتوی
> `<a>` HTML نامعتبر است. روی موبایل به همین دلیل کل کارت دیگر لینک
> نیست — فقط ناحیه‌ی نام/موبایل، مثل ستون نام در جدول دسکتاپ؛ دکمه‌ی
> ویرایش (`size="icon"`, ۴۴×۴۴) کنار آن، هدف لمسی مستقل خودش را دارد.
>
> نرمال‌سازی فارسی (`normalizedInput` از FE-017) اولین مصرف‌کننده‌ی
> واقعی‌اش همین‌جاست — نام، موبایل، کد ملی و یادداشت، هر چهار پیش از
> اعتبارسنجی zod نرمال می‌شوند؛ کد ملی هم در این فهرست است چون
> `parties.service.ts` واقعی (BE-024) دقیقاً همین را روی آن صدا می‌زند،
> نه چیزی که بشود «شناسه‌ی سیستمی» حساب کرد. تست پوشش می‌دهد که ارقام
> فارسی کد ملی به لاتین تبدیل می‌شوند.
>
> فیلدهای اختیاری در حالت ایجاد اگر خالی بمانند از payload حذف می‌شوند
> (`createPartySchema`: `optional()`)؛ در حالت ویرایش همان خالی‌بودن
> یعنی «این فیلد را پاک کن» و صریح `null` ارسال می‌شود
> (`updatePartySchema`: `nullable().optional()`) — یعنی می‌شود موبایل یا
> یادداشتِ قبلاً ثبت‌شده را از همین فرم حذف کرد.
>
> `apiPatch` (آینه‌ی `apiPost`، همان `Idempotency-Key` خودکار) به
> `api/client.ts` اضافه شد؛ mock handler `PATCH /api/parties/:id` همان
> الگوی idempotency-cache نسخه‌ی `POST` را تکرار می‌کند. `Textarea` تازه
> در `components/ui` برای «یادداشت» ساخته شد — تا این تسک چنین
> کامپوننتی نبود.
>
> ۹ تست تازه (`PartyFormDialog.test.tsx`: اعتبارسنجی، هر دو payload،
> نرمال‌سازی، double-submit، خطای سرور) + ۲ تست تازه در
> `PartyList.test.tsx` برای دکمه‌ی ویرایش (دسکتاپ و موبایل، تودرتو
> نبودنش با لینک). `pnpm typecheck`/`pnpm lint`/`pnpm test` سبز؛ تأیید
> دستی در مرورگر: باز شدن گفت‌وگو با مقدار فعلی شخص، ویرایش نام + کد
> ملی با ارقام فارسی، ثبت موفق (`PATCH` با ۲۰۰)، بازتاب فوری در لیست، و
> ماندگاری مقدار نرمال‌شده بعد از بازکردن دوباره‌ی همان گفت‌وگو.

### فیلدها
* نوع طرف حساب
* نام
* موبایل
* کد ملی اختیاری
* یادداشت
### قواعد
* B2B عملیاتی ساخته نشود.
* نوع BUSINESS فقط در مدل موجود باشد.
* کد ملی اختیاری باشد.
* Party غیرفعال از انتخاب معاملات جدید حذف شود. ⚠️ این‌جا هیچ
  انتخاب‌گر معامله‌ای وجود ندارد که این قاعده رویش اعمال شود — کار
  `FE-035` (انتخاب‌گر طرف حساب) است؛ همان‌جا باید رعایت شود.
### تمام است وقتی
* create و edit کار کنند.
* خطاهای field-level نمایش داده شوند.
* نرمال‌سازی ورودی انجام شود.
---
## [x] FE-034 — صفحه جزئیات شخص

> **وضعیت:** برخلاف تصور اولیه، هیچ بخشی به دلیل نبود بک‌اند stub نشد —
> `GET /parties/:id`، `GET /parties/:id/balances` (BE-056)،
> `GET /parties/:id/statement` (BE-057)، `POST /parties/:id/deactivate`
> و کل ماژول `settlement/` (چهار endpoint رسمی) از قبل روی سرور آماده
> بودند؛ فقط fetcher/hook/mock سمت فرانت برایشان نبود. تنها جای واقعی
> کوچک‌شدن دامنه، خودِ صفحه است: «صورت‌حساب» یک بخش جدا نساخت — همان
> کارت «آخرین معاملات» (۵ ردیف آخر `GET /parties/:id/statement`، بدون
> فیلتر) هر دو بولت را با هم پوشش می‌دهد، چون نسخه‌ی کامل با
> بازه/فیلتر/pagination عیناً کار FE-070 است. «ثبت تسویه» فقط لینک است،
> نه فرم واقعی — به مسیر تازه‌ی `/parties/:partyId/settlements/new`
> (دقیقاً همان مسیری که خودِ FE-055 در `FrontTasks.md` برایش نوشته شده)
> که فعلاً همان جانگه‌دار عمومی `SettlementsNewPlaceholder` را نشان
> می‌دهد؛ فرم واقعی کار FE-055 است.
>
> «ویرایش» بازِ همان `PartyFormDialog` ساخته‌ی FE-033 است — این صفحه
> اولین نقطه‌ی ورود واقعی آن در برنامه‌ی در حال اجراست (نه فقط تست).
> «مانده چندواحدی» ریال و طلای خالص را دو بُعد **مستقل** نگه می‌دارد
> (نه یک جفت تبدیل‌شده)، با `dualFromRial`/`dualFromPure` از `core-calc`
> یک `DualAmount` per-row برای `AmountDisplay`/`UnitToggle` می‌سازد؛
> بدون مظنه (`referenceQuoteId` ندادن یا نبودِ هیچ مظنه) مقدار خام را با
> پیام «معادل در دسترس نیست» نشان می‌دهد، هرگز صفر ساختگی. برچسب
> بدهکار/بستانکار آینه‌ی دقیق `directionFor` واقعی
> (`party-balance-report.service.ts`) است: مثبت=بدهکار، منفی=بستانکار —
> عکسِ چیزی که رنگ `text-credit`/`text-debit` ممکن است القا کند؛ آن دو
> عمداً جدا نگه داشته شدند. سکه‌ها همیشه با `formatCoinCount` جدا
> می‌مانند، بدون تبدیل به گرم.
>
> بدون هیچ `beforeLoad`/`useHasRole` نقش‌محور — نه `PartiesController`
> (ویرایش، غیرفعال‌سازی) و نه کنترلرهای تسویه (`@Roles('OWNER','MANAGER','CASHIER')`،
> یعنی هر سه نقش) هیچ `@Roles` واقعی‌ای دارند که یک نقش را جدا کند؛
> ساختن محدودیت فرانت‌اندی بدون پشتوانه‌ی سرور دقیقاً همان اشتباهی است
> که `lib/permissions.ts` هشدار می‌دهد.
>
> تأیید دستی در مرورگر: باز شدن با لینک مستقیم (بدون عبور از فهرست)،
> صحت مانده و معادلش زیر هر دو حالت کلید واحد، صحت برچسب بدهکار/بستانکار
> روی داده‌ی واقعی مثبت/منفی، غیرفعال‌سازی واقعی (وضعیت و دکمه فوراً
> به‌روز شدند)، لینک «ثبت تسویه» به مسیر تازه، و بدون اسکرول افقی روی
> ۳۷۵px. حین همین تأیید یک باگ preexisting در `ResponsiveDialog` پیدا و
> جداگانه گزارش شد (نه اصلاح، چون خارج از دامنه‌ی این تسک است): بستن
> دیالوگ با Escape یا دکمه‌ی «انصراف» — بر خلاف بستن بعد از موفقیت async
> — روی مرورگر واقعی گیر می‌کند (`data-state="closed"` ولی هنوز کاملاً
> دیده و کلیک‌پذیر)؛ در `CreatePartyDialog` (FE-032، دست‌نخورده) هم عیناً
> تکرار شد، پس ربطی به این تسک ندارد.

### بخش‌ها
* مشخصات
* مانده چندواحدی
* آخرین معاملات
* صورت‌حساب
* ثبت تسویه
* غیرفعال‌سازی
### تمام است وقتی
* layout موبایل‌فرست باشد.
* اقدام اصلی نزدیک پایین صفحه باشد.
* roleها رعایت شوند.
---
## [x] FE-035 — انتخاب‌گر طرف حساب

> **وضعیت:** `PartySelector` در `components/common/` است، نه
> `features/parties/` — چون واقعاً «مشترک بین فروش/خرید/تسویه» است و آن
> سه هنوز ساخته نشده‌اند (این component زودتر از مصرف‌کننده‌هایش آمده،
> مثل توابع `core-calc`). فعلاً هیچ فرمی به آن وصل نیست؛ برای اینکه
> «تمام است وقتی» این تسک (کیبورد، لمس، Bottom Sheet، انتخاب خودکار بعد
> از ایجاد) قابل‌تأیید باشد بدون منتظرماندن FE-041 به بعد، یک هارنس
> توسعه‌ای در `/_dev/party-selector` ساخته شد — دقیقاً همان الگوی
> `KeypadHarness.tsx`.
>
> جست‌وجو با `useParties` (نه `usePartySearch`، که هیچ مصرف‌کننده‌ای در
> کدبیس نداشت و یک باگ نهفته دارد: رشته‌ی خالی را بدون گارد به سرور
> واقعی می‌فرستد که با `min(1)` ردش می‌کند — تا این تسک هیچ‌کس این باگ
> را لمس نکرده بود). `useParties` یک پارامتر `enabled` تازه گرفت (مثل
> `usePartyBalances` در FE-034) تا وقتی جست‌وجو خالی است اصلاً fetch
> نرود؛ به‌جایش «اخیر» سمت کلاینت نشان داده می‌شود. فیلتر `status: 'ACTIVE'`
> همیشه روی جست‌وجو هست — همان قاعده‌ای که FE-033 صریحاً به این تسک
> موکول کرده بود: «شخص غیرفعال از انتخاب معاملات جدید حذف شود».
>
> «اخیر» یک store تازه‌ی Zustand (`recent-parties-store.ts`) است، کاملاً
> سمت کلاینت — هیچ endpoint بک‌اندی برای «اخیر» وجود ندارد. فقط فیلدهای
> لازم برای نمایش/انتخاب را نگه می‌دارد (نه کد ملی، نه یادداشت). چون این
> رونوشت می‌تواند از سرور عقب بیفتد، یک ردیف «اخیرِ» غیرفعال نمایش داده
> می‌شود ولی قابل‌انتخاب نیست — نه فقط نتیجه‌ی جست‌وجوی زنده.
>
> «ایجاد شخص جدید داخل Sheet» با همان `CreatePartyDialog` ساخته‌ی FE-032
> است (یک `onCreated?` تازه به آن اضافه شد)، نه بازنویسی. عمداً
> **متوالی** پیاده شد — گفت‌وگوی جست‌وجو بسته می‌شود، بعد `CreatePartyDialog`
> باز می‌شود — نه دو `ResponsiveDialog` هم‌زمان باز، چون هر دو از پورتال
> رندر می‌شوند و تعامل ناشناخته‌ای با باگ‌های گیرکردن دیالوگ (پایین
> همین یادداشت) می‌ساختند که ارزش ریسکش را نداشت.
>
> ساختار ردیف‌های نتیجه یک جزئیات ARIA مهم دارد: خودِ عنصر `role="option"`
> کلیک‌پذیر است، نه یک `button` تودرتویش — چون فوکوس واقعی همیشه روی
> input جست‌وجو می‌ماند و ردیف‌ها با `aria-activedescendant` «فوکوس مجازی»
> می‌گیرند (الگوی استاندارد ARIA combobox). نسخه‌ی اول یک `button` تودرتو
> داشت که در تست کلیک واقعی را از کار انداخت (چون کلیک روی `option`
> هرگز به فرزندش که در مسیر bubble نیست نمی‌رسد) — دقیقاً همان باگی که
> در مرورگر واقعی هم رخ می‌داد، نه فقط در تست.
>
> ⚠️ حین تأیید دستی، **دو باگ preexingی جدید در `ResponsiveDialog` پیدا و
> جداگانه گزارش شدند** (نه اصلاح، خارج از دامنه‌ی این تسک): (۱) باگ
> شناخته‌شده‌ی FE-034 (گیرکردن دیالوگ روی صفحه‌کلید‌محور/دکمه‌ی «انصراف»)
> دقیقاً همان‌جا که انتظار می‌رفت اینجا هم تکرار شد. (۲) بدتر: روی
> viewport موبایل (۳۷۵px)، باز کردن `CreatePartyDialog` (FE-032،
> دست‌نخورده — یعنی preexisting، نه چیزی که این تسک ساخته) نشان داد
> Bottom Sheet با `data-state="open"` ولی `transform` برای همیشه روی
> «کاملاً بیرون از صفحه» گیر می‌کند — یعنی ممکن است **هیچ دیالوگی روی
> موبایل واقعاً دیده نشود**. هر دو گزارش شدند؛ دومی به‌خاطر شدتش
> (Bottom Sheet قاعده‌ی ۶ CLAUDE.md برای *همه‌ی* مودال‌های زیر ۶۴۰px است)
> جداگانه پیگیری می‌شود.
>
> ۱۶ تست تازه‌ی `PartySelector.test.tsx` + ۶ تست `recent-parties-store.test.ts`
> + یک تست تازه برای `onCreated` در `CreatePartyDialog.test.tsx`.
> `pnpm typecheck`/`pnpm lint`/`pnpm test` سبز (۳۸۱ تست). تأیید دستی در
> مرورگر واقعی روی داده‌ی سرور ساختگی: جست‌وجوی زنده با فیلتر ACTIVE
> صحیح (شخص غیرفعال از نتیجه حذف شد)، ناوبری کیبورد (ArrowDown/Up/Enter)
> با `aria-activedescendant` صحیح، ایجاد شخص واقعی و انتخاب خودکار
> فوری، و ماندگاری فهرست اخیر بعد از navigate.

### هدف
component مشترک برای فرم‌های فروش، خرید و تسویه.
### قابلیت‌ها
* جست‌وجوی سریع
* ایجاد شخص جدید داخل Sheet
* recent parties
* وضعیت غیرفعال
* پاک کردن انتخاب
### تمام است وقتی
* با کیبورد و لمس کار کند.
* زیر ۶۴۰px Bottom Sheet باشد.
* پس از ایجاد شخص جدید همان شخص انتخاب شود.
---
# Milestone 9 — موجودی و کالا
## [x] FE-036 — فهرست زیورآلات
### وابستگی بک‌اند
```text
BE-026
```
### نمایش
* کد
* عنوان
* وزن
* عیار
* اجرت
* وضعیت موجودی
### قابلیت‌ها
* جست‌وجو
* فیلتر موجود
* فیلتر عیار
* pagination
* ایجاد و ویرایش

> **وضعیت:** «ایجاد و ویرایش» یعنی این تسک `JewelryItemFormDialog` را هم
> ساخت، نه فقط فهرست — بدون آن، فهرست همیشه خالی و غیرقابل‌تأیید می‌ماند.
> عمداً فقط ۶ فیلد از ۹ فیلد کامل `createJewelryItemSchema` دارد (بدون
> وزن نگین/سایر کسورات) — همان مرز مستندشده در خودِ `WeightInput.tsx`
> (FE-020): «دغدغه‌ی فرم کالای زیورآلات (FE-037) است، نه این کامپوننت
> پایه». قرارداد واقعی هم این حذف را از قبل پیش‌بینی کرده
> (`stoneWeightMg`/`otherDeductionWeightMg` روی schema عمداً `.default('0')`
> دارند) — پیش‌نمایش وزن خالص اینجا **بدون کسر نگین** است؛ پیش‌نمایش
> **دقیق** کار FE-037 است.
>
> فیلتر عیار در قرارداد واقعی سرور پشتیبانی نمی‌شود (فقط `search`/`active`).
> راهکار: با فعال‌شدن فیلتر عیار، `limit` به سقف واقعی سرور (۲۰۰) می‌رود
> و فیلتر/صفحه‌بندی سمت کلاینت روی همان دسته انجام می‌شود — همان الگوی
> `QuoteHistoryList` در FE-031، نه ابداع تازه.
>
> نوع اجرت «درصدی از ارزش طلا» (`PERCENT_X100`) یک نوع فیلد عددی تازه به
> زیرساخت مشترک کیپد اضافه کرد: `DIGIT_SPECS.percent` در `core-calc`
> (دو رقم اعشار، مقیاس ×۱۰۰ — «۷٫۵٪» → `750n`) و `PercentInput.tsx`.
>
> ⚠️ **سه باگ معماری تازه پیدا و همین‌جا اصلاح شدند** — همه از این‌که
> FE-036 اولین جایی است که فیلد عددی کیپد داخل یک `ResponsiveDialog` باز
> قرار می‌گیرد (بدون این سه، فرم داخل گفت‌وگو عملاً غیرقابل‌استفاده بود):
> ۱) کیپد با `z-40` پشت گفت‌وگو (`z-50`) گم می‌شد؛ به `z-[60]` رسید. ۲)
> Radix Dialog هنگام باز بودن با `react-remove-scroll` روی `<body>`
> `pointer-events:none` می‌گذارد و فقط محتوای خودِ دیالوگ را سوراخ
> می‌کند؛ چون کیپد (یک نمونه‌ی سراسری) بیرون از آن دیالوگ رندر می‌شود،
> با موس/لمس هم غیرقابل‌کلیک می‌شد — `pointer-events-auto` صریح روی
> `NumericKeypad` رفع کرد. ۳) **جدی‌ترین:** Radix/vaul هر ضربه بیرون از
> محتوای گفت‌وگو را «کلیک بیرون» حساب می‌کند و `onOpenChange(false)` صدا
> می‌زند — یعنی هر ضربه روی کیپد گفت‌وگو را می‌بست و (چون `close()` در
> `JewelryItemFormDialog` روی بسته‌شدن `resetForm()` صدا می‌زند) **کل
> ورودی کاربر را پاک می‌کرد**. رفع شد با `onInteractOutside` روی
> `ResponsiveDialogContent` (هم `DialogContent` هم `DrawerContent` —
> نوع `Drawer.Content` در vaul دقیقاً همان `DialogContentProps` رادیکس
> است) که ضربه‌های داخل کیپد (`data-numeric-keypad-root`) را از تشخیص
> «بیرون» معاف می‌کند. تأیید زنده در مرورگر واقعی: پرکردن کامل فرم
> (کد/عنوان/وزن/عیار/اجرت) با ضربه‌های واقعی روی کیپد، گفت‌وگو در تمام
> مدت باز ماند، دکمه‌ی ثبت درست فعال شد.
>
> یک نقص دسترسی‌پذیری **جداگانه** (نه اصلاح‌شده، گزارش‌شده) باقی ماند:
> چون کیپد بیرون از دیالوگ است، `hideOthers` رادیکس آن را از درخت
> دسترسی‌پذیری خارج می‌کند — یعنی با صفحه‌خوان، کیپد داخل گفت‌وگو در
> دسترس نیست (روی موس/لمس اثری ندارد). پیگیری جدا لازم دارد.
>
> `AppNav` یک آیتم «موجودی» تازه گرفت (بین «خرید» و «اشخاص»).
>
> `pnpm typecheck`/`pnpm lint`/`pnpm test` سبز (۴۱۳ تست، ۴۸ فایل).
> تأیید دستی در مرورگر واقعی (علاوه بر مورد بالا): آیتم ناوبری، رندر
> صفحه، باز شدن گفت‌وگوی ایجاد.
>
> دو باگ preexisting دیگر (نه از این تسک، قبلاً هم در FE-034/FE-035
> گزارش شده) دوباره تکرار شدند: گیرکردن دیالوگ روی دکمه‌ی «انصراف»
> (`data-state="closed"` ولی هنوز رندرشده)، و Bottom Sheet موبایل گیر
> در حالت بسته. جداگانه پیگیری می‌شوند، نه دامنه‌ی این تسک.
### تمام است وقتی
* موبایل card و دسکتاپ table باشد.
* Tabular Numbers رعایت شود.
---
## [x] FE-037 — فرم کالای زیورآلات
### فیلدها
* کد
* عنوان
* وزن ناخالص
* عیار
* وزن نگین
* سایر کسورات
* نوع اجرت
* مقدار اجرت
* وضعیت
### قواعد
* وزن‌ها string storage-ready باشند.
* محاسبه وزن خالص از `core-calc` باشد.
* تغییرات مالی به نسخه جدید backend منتهی شود.
* عدد صنفی هاردکد نشود.
### تمام است وقتی
* preview وزن خالص نمایش داده شود.
* validation دقیق باشد.
* payload مطابق contracts باشد.
---
## [x] FE-038 — فهرست موجودی سکه
### وابستگی بک‌اند
```text
BE-020
BE-027
```
### نمایش
* نوع سکه
* تعداد
* قیمت بازار اختیاری
* ارزش ذاتی نمایشی
* حباب فقط برای سکه بانک مرکزی
### قواعد
* تعداد integer باشد.
* سکه در state به گرم تبدیل نشود.
* حباب برای نوع غیرمجاز نمایش داده نشود.
### تمام است وقتی
* هر نوع سکه مستقل باشد.
* صفر و موجودی منفی احتمالی به‌صورت واضح نمایش داده شوند.
---
## [x] FE-039 — ثبت موجودی افتتاحیه
### وابستگی بک‌اند
```text
BE-028
```
### قابلیت‌ها
* انتخاب زیورآلات
* وزن آبشده
* تعداد انواع سکه
* مرور نهایی
* تأیید مدیر
* idempotency
### قواعد
* فقط OWNER یا MANAGER
* هشدار تغییرناپذیری
* submit اتمیک
* double submit کنترل شود
### تمام است وقتی
* نتیجه ثبت نمایش داده شود.
* failure هیچ وضعیت موفق کاذب نشان ندهد.
---
## [x] FE-040 — داشبورد موجودی
### نمایش
* مصنوعات
* آبشده
* انواع سکه
* اقلام کم‌موجود
* آخرین حرکات
### قواعد
* چندشعبه‌ای ساخته نشود.
* امانی ساخته نشود.
* نقره و شمش در UI فاز ۱ نمایش داده نشوند.
---
# Milestone 10 — فروش زیورآلات
## [x] FE-041 — صفحه شروع فروش
### هدف
ساخت shell جریان فروش سریع.
### مراحل
1. مظنه
2. مشتری
3. اقلام
4. پرداخت
5. مرور و ثبت
### قواعد
* موبایل‌فرست
* پیشرفت مراحل واضح
* حفظ draft local فقط در همان session
* draft رسمی آفلاین ساخته نشود
* اقدام اصلی پایین صفحه
### تمام است وقتی
* رفت‌وبرگشت مراحل داده را از بین نبرد.
* refresh رفتار مشخص داشته باشد.
* route leave warning برای draft وجود داشته باشد.
---
## [x] FE-042 — انتخاب کالای فروش

> **وضعیت:** قراردادهای واقعی فروش (`createJewelryCashSaleSchema`،
> `createJewelryCreditSaleSchema` در `packages/contracts/src/sales`) هرکدام
> دقیقاً یک `jewelryItemId` می‌پذیرند — نه آرایه، نه فیلد تعداد — و هیچ‌کجای
> کدبیس (نه قرارداد، نه بک‌اند، نه فرانت) مفهوم «کالای موردی» ندارد؛
> عبارت «طبق مدل backend» در این تسک به چیزی اشاره می‌کند که وجود ندارد.
> این را خودِ کدبیس هم قبلاً مستند کرده بود: mock چندخطیِ منسوخ
> `invoiceLineInputSchema` (`api/contracts.ts`) صریحاً می‌گوید تطبیق سبد
> چندقلمی با قرارداد تک‌قلمی واقعی کار **FE-045/FE-047** است، نه این تسک.
> بر همین اساس FE-042 صرفاً UI/state سمت کلاینت ساخت — نه فرمت payload
> نهایی سرور — و شکل دقیق «کالای موردی» (که در بک‌اند اصلاً تعریف نشده) را
> خودش طراحی کرد؛ اگر این فرض اشتباه است، پیش از FE-045 قابل اصلاح است.
>
> `SaleDraftItemLine` (`stores/sale-draft-store.ts`) یک discriminated union
> عمداً نامتقارن است: ردیف `CATALOG` فقط ارجاع نگه می‌دارد
> (`jewelryItemId`+کد+عنوان) چون FE-043 مشخصات مالی کامل را دوباره از همان
> کالا می‌خواند — نگه‌داشتن رونوشت اینجا دو منبع حقیقت می‌ساخت. ردیف
> `ADHOC` برعکس، وزن و عیارِ گرفته‌شده در همین تسک را کامل نگه می‌دارد چون
> هیچ `jewelryItemId`ای برای رجوع دوباره در FE-043 ندارد. کسورات/اجرت
> کالای موردی هم مثل کاتالوگ به FE-043 موکول شدند — این تسک فقط «ورود
> سریع وزن» می‌خواهد.
>
> انتخاب دوباره‌ی همان کالای کاتالوگ یک **ردیف تازه** می‌سازد، نه افزایش
> «تعداد» یک ردیف — بر خلاف `JewelryLineSelector` (FE-039، موجودی
> افتتاحیه) که با تعداد دِدوپ می‌کند. دلیل: قرارداد واقعی فروش اصلاً فیلد
> تعداد ندارد، پس فروش دو قطعه‌ی هم‌کد یعنی دو فراخوانی/دو ردیف مجزا، نه
> یک ردیف با کمیت ۲. نتایج جست‌وجو هم به همین دلیل کالای از‌قبل‌انتخاب‌شده
> را فیلتر نمی‌کنند.
>
> الگوی جست‌وجو+اخیر+کیبورد از `PartySelector` (FE-035) وام گرفته شد؛
> الگوی «چندانتخابی، گفت‌وگو بعد از افزودن باز می‌ماند» از
> `JewelryLineSelector` (FE-039). `useJewelryItems` یک پارامتر `enabled`
> تازه گرفت (`api/queries.ts`) — همان دلیل `useParties`: تا جست‌وجو خالی
> است اصلاً fetch نرود، «اخیر» سمت کلاینت (`recent-jewelry-items-store.ts`،
> تازه، هم‌الگوی `recent-parties-store.ts`) جایش را پر می‌کند.
> `generateUuid` از `api/client.ts` برای `lineId` هم `export` شد — همان
> fallback زمینه‌ی ناامن که برای Idempotency-Key از قبل لازم بود، اینجا هم
> برای کلید کاملاً محلی سبد لازم است.
>
> «نمایش موجودی» با همان الگوی ادغام `CoinInventoryPage` (FE-038):
> `GET /inventory/balances?itemType=JEWELRY`، کالای بدون حرکت صفر گرفته
> می‌شود نه حذف. عدد نمایش‌داده‌شده علاوه‌بر آن، تعداد همان کالا که همین
> حالا در همین سبد است را هم کم می‌کند (`remainingStock`) — یک تفریق عدد
> صحیح نمایشی محض، نه منطق حسابداری؛ مانده‌ی واقعی همیشه سمت سرور در
> لحظه‌ی ثبت بررسی می‌شود. تأیید دستی زنده: افزودن «دستبند ۱۸ عیار» موجودی
> نمایشی را از ۵ به ۴ کاهش داد، افزودن دوباره به ۳.
>
> `SaleWizardPage.tsx` اولین `<NumericKeypad />` خودش را گرفت — تا این تسک
> صفحه فیلد کیپدی نداشت. «بعدی» روی مرحله‌ی اقلام حالا `items.length > 0`
> می‌خواهد؛ `hasSaleDraftProgress` هم اقلام را برای هشدار خروج مسیر
> بررسی می‌کند.
>
> ⚠️ تأیید دستی در مرورگر واقعی **دو باگ preexisting شناخته‌شده‌ی
> `ResponsiveDialog` را برای سومین بار تکرار کرد** (نه اصلاح، خارج از
> دامنه‌ی این تسک — هر دو از FE-034/FE-035 مستندند و زیرساخت مشترکند، نه
> کد این تسک): (۱) دسکتاپ: بعد از انتخاب یک شخص در `PartySelector`، دیالوگ
> `data-state="closed"` می‌ماند ولی `display:flex`/`visibility:visible`
> باقی می‌ماند و کلیک‌های بعدی را می‌قاپد (باگ FE-034). (۲) موبایل (۳۷۵px):
> باز کردن `JewelryItemSelector` خودِ این تسک نشان داد Bottom Sheet با
> `data-state="open"` درست انتخاب می‌شود (`vaul` Drawer، نه Dialog — تصمیم
> ریسپانسیو درست است) ولی `transform: translateY(812px)` برای همیشه گیر
> می‌کند — یعنی هیچ Bottom Sheet روی موبایل واقعاً دیده نمی‌شود (باگ
> FE-035، دقیقاً همان مقدار). چون این باگ روی کد قدیمی‌تر و قبلاً
> تأییدشده (`JewelryItemFormDialog`, FE-036) هم عیناً بازتولید شد، تأیید
> جریان «ورود سریع وزن» با کلیک واقعی کیپد در این نشست ممکن نشد؛ به‌جایش
> ۱۲ تست خودکار (`JewelryItemSelector.test.tsx`) با همان الگوی اثبات‌شده‌ی
> `tapDigits` در `JewelryItemFormDialog.test.tsx` (کلیک واقعی روی دکمه‌های
> `NumericKeypad`، نه `user.type`) این مسیر را می‌سنجند و سبزند.
>
> بقیه‌ی «تمام است وقتی» مستقیم در مرورگر واقعی تأیید شد: جست‌وجوی «انگشتر»
> (پیشوند مشترک یک کالای فعال و یک کالای غیرفعال) فقط کالای فعال را
> برگرداند — کالای غیرفعال هرگز حتی در نتیجه‌ی جست‌وجو ظاهر نشد (فیلتر
> `active: true`)؛ گارد `select()` هم در تست پوشش دارد برای حالت «اخیر»ی
> که بعداً غیرفعال شده. انتخاب دوباره‌ی همان کالا دو ردیف مجزا ساخت. حذف
> ردیف کار کرد. «بعدی» با صفر قلم غیرفعال، با یک قلم فعال شد.
>
> ۴۳ تست تازه (`JewelryItemSelector.test.tsx` ۱۸،
> `recent-jewelry-items-store.test.ts` ۷، افزوده‌های
> `sale-draft-store.test.ts` و `SaleWizardPage.test.tsx`).
> `pnpm gate:frontend` سبز: ۵۷ فایل/۴۹۴ تست، بدون خطای typecheck/lint/
> circular، بسته‌ی اولیه ۱۴۸.۸KB فشرده (زیر بودجه‌ی ۲۰۰KB).

### قابلیت‌ها
* جست‌وجوی کد
* جست‌وجوی عنوان
* recent items
* انتخاب چند قلم
* نمایش موجودی
* ورود سریع وزن در کالای موردی طبق مدل backend
### تمام است وقتی
* کالای غیرفعال انتخاب نشود.
* انتخاب روی موبایل سریع و قابل لمس باشد.
---
## [x] FE-043 — ویرایش ردیف فروش زیورآلات

> **وضعیت:** یک شکاف واقعی زیرساختی پیدا شد: نرخ سود/مالیات/واحد گرد
> کردن مستأجر (`sales.jewelry_profit_rate_bps`،
> `tax.gold_jewelry_labor_profit_commission_rate_bps`، در
> `apps/api/src/modules/pricing/tenant-initial-settings.ts`) در دیتابیس
> نسخه‌دار **هست**، ولی `VersionedSettingsService` **هیچ کنترلر HTTP**
> ندارد — فقط از داخل سرویس‌های سرور (`SalesPricingService`) خواندنی
> است. فرانت امروز هیچ راهی برای خواندن این دو نرخ ندارد.
>
> این تسک با دو قاعده‌ی خودش دقیقاً همین را پیش‌بینی کرده بود: «نتیجه
> نهایی server-authoritative» و «mismatch سرور و کلاینت نمایش و کنترل
> شود» — یعنی پیش‌نمایش کلاینت از اول قرار نبود تضمین‌شده باشد، فقط یک
> بازخورد UX زودهنگام. راه‌حل: «سود» و «مالیات» که خودشان جزو فیلدهای
> **قابل‌ویرایش** همین تسک‌اند، با مقدار seed اولیه‌ی همان اعداد seed فاز ۱
> (۷٪ سود، ۱۰٪ مالیات — `DEFAULT_PROFIT_RATE_BPS`/`DEFAULT_TAX_RATE_BPS`
> در `sale-line-pricing.ts`) پر می‌شوند — نه یک ثابت پنهان، یک مقدار
> اولیه‌ی *دیدنی و قابل‌تغییر فوری*، همان الگوی «بدون هاردکد پنهان» که
> `KaratInput` قبلاً برای عیار پیاده کرده. `RATE_DIVISOR`/
> `DEFAULT_ROUNDING_UNIT` برعکس مستقیم از `@gold/core-calc` می‌آیند —
> این دو عدد صنفی نیستند، ثابت ریاضی مثقال↔گرم و واحد گرد کردن‌اند، و از
> قبل در `useMazneh.ts` (FE-029) هم دقیقاً به همین شکل استفاده می‌شوند.
> افزودن endpoint خواندنی واقعی برای این دو نرخ کار یک تسک بک‌اندی جداست.
>
> `SaleDraftItemLine` (هر دو شاخه‌ی CATALOG/ADHOC) یک فیلد تازه گرفت:
> `pricing: SaleLinePricingInput | null` — `null` تا وقتی ویرایشگر باز
> نشده. **نتیجه‌ی محاسبه‌شده هرگز ذخیره نمی‌شود** — فقط ورودی خام
> (`pricing`)؛ مبلغ‌های نهایی همیشه با `calculateLinePricing`
> (`sale-line-pricing.ts`، پوسته‌ای نازک روی خودِ `calculateJewelrySale`ی
> `@gold/core-calc` — همان تابعی که سرور صدا می‌زند) دوباره محاسبه
> می‌شوند، هرجا لازم باشد (خلاصه‌ی ردیف در فهرست، خودِ ویرایشگر، بعداً
> FE-044). این هم‌راستا با تصمیم خودِ FE-042 است: کاتالوگ فقط ارجاع نگه
> می‌دارد، رونوشت مالی نه.
>
> ⚠️ **یک باگ معماری واقعی پیدا و رفع شد** — نسخه‌ی اول این تسک مقدار
> اولیه‌ی فیلدها را با یک `useEffect` بعد از mount seed می‌کرد (state
> با `useState(0n)` شروع می‌شد، effect بعداً مقدار درست را می‌نشاند).
> با لاگ‌گیری هدفمند (ردیابی دقیق ترتیب اجرای effectها) ثابت شد این یک
> مسابقه‌ی همیشگی است، نه مسئله‌ی زمان‌بندی: خودِ `NumericField` یک
> effect دارد که «مقدار فعلی را به فرم اطلاع بده»، و همین effect در
> لحظه‌ی mount هم یک‌بار با مقدار خامِ صفر (بافر خالی) صدا می‌زند. چون
> این فیلدها *در همان رندری* mount می‌شوند که مقدار seed‌شده هنوز از
> طریق effect نرسیده، آن اعلان صفرِ خودِ `NumericField` مقدار
> seed‌شده را برای همیشه با صفر بازنویسی می‌کرد. راه‌حل: `SaleLinePricingDialog`
> به دو کامپوننت شکسته شد — بیرونی (واکشی کاتالوگ، حالت بارگذاری/خطا) و
> `SaleLinePricingForm` داخلی که با `key={line.lineId}` فقط وقتی رندر
> می‌شود که `initialPricing` کامل آماده باشد و همه‌ی `useState`ها
> مستقیم از همان مقدار اولیه می‌سازد — دقیقاً همان الگوی اثبات‌شده‌ی
> `JewelryItemFormDialog.tsx` (`useState(item ? BigInt(...) : 0n)`،
> مقدار درست از همان رندر اول، نه بعد از mount). یک تسک پس‌زمینه برای
> ممیزی بقیه‌ی مصرف‌کننده‌های `NumericField` (`WeightInput`/`KaratInput`/
> `MoneyInput`/`PercentInput`) از همین الگوی خطرناک باز شد.
>
> واکشی نسخه‌ی کاتالوگ با `useJewelryItem(id)` تازه (`GET
> /inventory/jewelry-items/:id`) — تا این تسک هیچ hook فرانتی این
> endpoint واقعی را صدا نمی‌زد؛ mock handler هم‌قدم اضافه شد
> (`mocks/handlers/index.ts`، همان الگوی تطبیق `PATCH` موجود).
>
> «نوع اجرت»/«مقدار اجرت» عیناً همان سوییچ `Select`+`MoneyInput`/
> `PercentInput` در `JewelryItemFormDialog.tsx` است. «کسورات بیش از وزن
> ناخالص» همان `CalcError` واقعیِ `calculateJewelrySale` را نمایش
> می‌دهد و «ذخیره» را غیرفعال می‌کند — نه یک اعتبارسنجی محلی جدا.
>
> تأیید دستی زنده در مرورگر واقعی (نه فقط mock جداگانه): افزودن «دستبند
> ۱۸ عیار» از کاتالوگ، باز کردن ویرایشگر، تأیید مستقیم `data-value`ی
> هر فیلد (وزن ۱۲۳۵۰، عیار ۷۵۰، اجرت ۳٬۵۰۰٬۰۰۰ ریال/گرم — همه از
> کاتالوگ واقعی — به‌علاوه‌ی پیش‌فرض ۷٪/۱۰٪ سود/مالیات)، پیش‌نمایش کامل
> هفت‌مقداری (نرخ گرم نمایش‌داده‌شده — ۱۱۰٬۸۰۸٬۳۸۸ ریال — عیناً با نرخ
> گرم ۷۵۰ داشبورد یکی بود، یک تأیید متقاطع مستقل)، ذخیره، و بازتاب فوری
> مبلغ کل محاسبه‌شده در فهرست ردیف‌ها. مسیر خطای واقعی (کسورات بیش از
> ناخالص) با تست‌های خودکار (نه زنده، به‌خاطر محدودیت شناخته‌شده‌ی ابزار
> تعامل با کیپد ثابت‌موقعیت در این محیط خاص خودکارسازی مرورگر) پوشش
> داده شده — همان محدودیتی که در FE-042 هم مستند شده، نه یک نقص محصول.
>
> ۲۲ تست تازه (`sale-line-pricing.test.ts` ۷ — شامل Golden case عیناً
> هم‌گام با `jewelry-sale.test.ts` در `core-calc`، `SaleLinePricingDialog.test.tsx`
> ۷، افزوده‌های `JewelryItemSelector.test.tsx` ۵). `pnpm gate:frontend`
> سبز: ۵۹ فایل/۵۱۲ تست، بدون خطای typecheck/lint/circular، بسته‌ی
> اولیه ۱۴۸٫۹KB فشرده (زیر بودجه‌ی ۲۰۰KB).

### فیلدها
* وزن
* عیار
* وزن نگین
* کسورات
* اجرت
* سود
* مالیات
### محاسبات نمایشی
* وزن خالص
* نرخ گرم
* ارزش طلا
* اجرت
* سود
* مالیات
* مبلغ کل
### قواعد
* محاسبات با `core-calc`
* نتیجه نهایی server-authoritative
* intermediate values گرد نشوند.
* mismatch سرور و کلاینت نمایش و کنترل شود.
### تمام است وقتی
* Golden cases فرانت با `core-calc` یکسان باشند.
* تغییر هر فیلد preview را به‌روز کند.
* هیچ floating point استفاده نشود.
---
## [x] FE-044 — خلاصه فاکتور فروش

> `SaleSummary.tsx` مرحله‌ی «مرور» را جایگزین EmptyState جانگه‌دار
> `SaleWizardPage` (FE-041) کرد — پنج کارت فقط‌خواندنی: مشتری، نرخ این
> فاکتور، اقلام، جمع کل، روش پرداخت.
>
> **قفل نرخ** (قاعده‌ی صریح تسک: «تغییر مظنه بازار preview ثبت‌شده را
> بی‌صدا عوض نکند»): `sale-draft-store` یک فیلد تازه گرفت،
> `lockedMazneh` — فقط رکورد خام مظنه (نه هیچ عدد مشتق‌شده‌ای)، با یک
> setter ایدمپوتنت (`lockMazneh`) که بعد از اولین قفل، فراخوانی‌های
> بعدی را بی‌صدا نادیده می‌گیرد. `SaleSummary` اولین باری که mount
> می‌شود (اولین ورود به مرور) مظنه‌ی زنده‌ی همان لحظه را قفل می‌کند؛ تا
> `reset` (فروش بعدی) دیگر عوض نمی‌شود، حتی اگر کاربر برگردد و دوباره
> مرور را باز کند. اگر مظنه‌ی بازار از لحظه‌ی قفل جلوتر برود، فقط یک
> هشدار اطلاع‌رسان («نرخ بازار از زمان ورود به این مرحله تغییر کرده»)
> نشان داده می‌شود — نرخ فاکتور دست‌نخورده می‌ماند. همه‌ی محاسبات
> (تک‌ردیفی و جمع کل) با همان `calculateLinePricing` واقعی FE-043 روی
> نرخ قفل‌شده اجرا می‌شوند، نه مظنه‌ی زنده.
>
> **ردیف بدون قیمت‌گذاری**: مرحله‌ی اقلام (FE-042) فقط «حداقل یک قلم»
> را شرط می‌کند، نه «همه قیمت‌گذاری‌شده» — یعنی کاربر می‌تواند با ردیف
> `pricing: null` به مرور برسد. این ردیف‌ها با برچسب «قیمت‌گذاری نشده»
> نشان داده می‌شوند و از جمع کل کنار گذاشته می‌شوند، با یک هشدار جداگانه
> که جمع کل جزئی است — جمع کل هرگز ساکت غلط نشان داده نمی‌شود.
>
> **«معادل طلا/ریال»**: طبق قاعده‌ی ۲-۴ CLAUDE.md یک ردیف جداست که
> همیشه هر دو واحد را با هم نشان می‌دهد (`unit="gold"` و `unit="rial"`
> صریح روی `AmountDisplay`، همان مسیر مجاز رندر عدد مالی)، برخلاف
> «مبلغ نهایی» بالای آن که از کلید تعویض واحد سراسری پیروی می‌کند.
> `<UnitToggle />` به `PageHeader` مرحله‌ی مرور اضافه شد (پیش‌تر هیچ
> نمونه‌ای در این صفحه نبود).
>
> «روش پرداخت» چون FE-050 («انتخاب روش پرداخت») هنوز ساخته نشده، فقط
> یک جانگه‌دار متنی نشان می‌دهد؛ کامنت قدیمیِ نادرست در
> `sale-draft-store.ts` («پرداخت... FE-044») به FE-050 اصلاح شد.
>
> ⚠️ **شکاف از‌قبل‌موجود پیدا شد، عمداً در این تسک رفع نشد**: مرحله‌ی
> اقلام (`JewelryItemSelector`، FE-042/043) هم از قبل `AmountDisplay`
> نشان می‌دهد ولی `PageHeader` آن صفحه هیچ‌وقت `UnitToggle` نگرفته بود —
> نقض قاعده‌ی ۲-۴. چون این شکاف مال تسک‌های قبلاً کامیت‌شده است، نه محدوده‌ی
> این تسک، به‌جای تغییر بی‌اجازه‌ی خارج از دامنه، جداگانه flag شد.
>
> ۱۵ تست تازه (`SaleSummary.test.tsx` ۱۱، افزوده‌های
> `sale-draft-store.test.ts` ۴). `pnpm gate:frontend` سبز: ۶۰ فایل/۵۲۷
> تست، بدون خطای typecheck/lint/circular، بسته‌ی اولیه ۱۴۸٫۹KB فشرده
> (زیر بودجه‌ی ۲۰۰KB).

### نمایش
* مشتری
* مظنه قفل‌شونده
* اقلام
* مجموع وزن
* اجرت
* سود
* مالیات
* مبلغ نهایی
* معادل طلا/ریال
* روش پرداخت
### قواعد
* نرخ snapshot به کاربر نمایش داده شود.
* عبارت «نرخ این فاکتور» واضح باشد.
* تغییر مظنه بازار preview ثبت‌شده را بی‌صدا عوض نکند.
---
## [x] FE-045 — ثبت فروش نقدی زیورآلات

> اولین ثبت واقعی فروش در فرانت. `useJewelryCashSaleSubmit` منطق ثبت را
> از `SaleWizardPage` بیرون نگه می‌دارد چون دکمه در نوار ثابت پایین است
> ولی خطا و رسید در بدنه‌ی صفحه رندر می‌شوند — دو نقطه‌ی رندر دور از هم با
> یک state مشترک.
>
> **سبد چندقلمی در برابر قرارداد تک‌قلمی.** شکافی که FE-042 مستند کرده و
> صریحاً به همین تسک واگذار کرده بود: `createJewelryCashSaleSchema` واقعی
> (`.strict()`) دقیقاً یک `jewelryItemId` می‌گیرد و هیچ مفهومی از «کالای
> موردی» یا آرایه‌ی خطوط ندارد. دو راه غلط کنار گذاشته شد — چند درخواست
> (یک فروش را به چند شماره‌ی فاکتور مستقل تبدیل می‌کند) و فرستادن قلم اول
> با انداختن بقیه (بی‌صدا مبلغ کمتری ثبت می‌کند). راه انتخاب‌شده:
> `prepareJewelryCashSale` (`sale-submit.ts`) یا payload معتبر می‌سازد یا
> با یکی از شش دلیل تایپ‌شده (`NO_PARTY`, `NO_QUOTE`, `NO_ITEMS`,
> `MULTIPLE_ITEMS`, `ADHOC_ITEM`, `ITEM_NOT_PRICED`) **مسدود** می‌کند و
> دلیل را کنار دکمه‌ی غیرفعال می‌نویسد. تا وقتی بک‌اند فاکتور چندقلمی
> ندارد، ثبت دقیقاً همان چیزی است که بک‌اند پشتیبانی می‌کند، نه بیشتر.
>
> **`quoteId` تازه اضافه شد.** قرارداد شناسه‌ی مظنه می‌خواهد، نه مبلغش —
> فرستادن عدد یعنی اجازه‌دادن به کلاینت برای تعیین نرخ سند. `MaznehSnapshot`
> (`useMazneh`) و `LockedMazneh` (`sale-draft-store`) هر دو فیلد `quoteId`
> گرفتند؛ `SaleSummary` همان لحظه‌ی قفل‌کردن نرخ (FE-044) شناسه را هم قفل
> می‌کند.
>
> **`useIdempotentSubmit`، نه `useMutation`** — `useMutation` هر تلاش را
> با کلید تازه‌ی داخل `apiPost` می‌فرستد، یعنی پاسخِ گم‌شده‌ی تلاش اول +
> ضربه‌ی دوم = دو فاکتور. اینجا کلید تا `reset()` صریح (فقط پس از موفقیت
> قطعی) ثابت می‌ماند؛ تست «تلاش دوباره پس از خطا همان کلید را می‌فرستد»
> همین را می‌سنجد. ضربه‌ی دوم حین ارسال را جدا، قفل سنکرون روی `ref`
> می‌گیرد (با `fireEvent` تست شده، چون `userEvent` اصلاً به دکمه‌ی
> `disabled` نمی‌رسد و چیزی سنجیده نمی‌شود).
>
> **پس از موفقیت** پیش‌نویس فوراً پاک می‌شود ولی رسید در state هوک زنده
> می‌ماند — `SaleSubmitResult` نام مشتری و نرخ قفل‌شده را از یک کپیِ
> لحظه‌ی ثبت می‌خواند، نه از store خالی‌شده. روی خطا برعکس: پیش‌نویس
> دست‌نخورده می‌ماند و `ApiErrorNotice` در بدنه‌ی صفحه (نه toast) باقی
> است. حین ارسال «قبلی» و «ثبت» هر دو غیرفعال‌اند و `useBlocker` حتی بدون
> «پیشرفت» هم روشن است.
>
> ⚠️ **ویرایش‌های ردیف (FE-043) به سرور نمی‌روند.** قرارداد هیچ فیلد
> وزن/عیار/اجرت ندارد؛ سرور از روی نسخه‌ی ثبت‌شده‌ی کالا و `quoteId` قیمت
> می‌زند. پس خروجی `calculateLinePricing` فقط پیش‌نمایش است.
> `previewPayableRial` در لحظه‌ی ثبت نگه داشته می‌شود و اگر با
> `payableRial` سرور نخواند، اختلاف صریح روی رسید نشان داده می‌شود —
> عدد سرور برنده است، ولی ساکت برنده نمی‌شود.
>
> **پاک‌سازی:** mock منسوخ `POST /api/invoices` (آرایه‌ی خطوط + مظنه‌ی
> خام) و سه schema مربوطه‌اش در `api/contracts.ts` — که خودشان مستند کرده
> بودند جایگزینی‌شان کار همین تسک است — حذف و با handler واقعی
> `POST /api/sales/invoices/jewelry` جایگزین شدند. mock جدید مثل سرور
> واقعی قیمت را خودش با همان `calculateJewelrySale` از نسخه‌ی کالا حساب
> می‌کند، کالای فروخته‌شده را از موجودی برمی‌دارد، و تکرار کلید همان
> فاکتور را برمی‌گرداند.
>
> ۲۰ تست تازه (`sale-submit.test.ts` ۱۱، افزوده‌های
> `SaleWizardPage.test.tsx` ۹). `pnpm typecheck`, `eslint`,
> `check:circular` و `pnpm test` سبز: ۶۱ فایل / ۵۴۸ تست. علاوه بر آن،
> جریان کامل روی dev server با MSW دستی هم آزموده شد: ثبت موفق (شماره‌ی
> فاکتور و مبلغ از سرور)، تکرار کلید همان فاکتور، نبود کلید = ۴۰۰،
> کم‌شدن کالا از موجودی، و مسدودشدن سبد دوقلمی.

### وابستگی بک‌اند
```text
BE-041
```
### جریان
* آماده‌سازی payload
* Idempotency-Key
* submit
* حالت pending
* جلوگیری از تغییر فرم حین submit
* دریافت فاکتور ثبت‌شده
* نمایش رسید
### تمام است وقتی
* یک فروش کامل End-to-End ثبت شود.
* double tap یک فاکتور بسازد.
* خطای میانی draft را قابل اصلاح نگه دارد.
* موفقیت فقط پس از پاسخ سرور نمایش داده شود.
---
## [x] FE-046 — رسید فروش

> `SaleReceipt.tsx` جای stub کوچک FE-045 (`SaleSubmitResult`) را گرفت —
> آن فایل حذف شد، نه اینکه کنارش بماند.
>
> **رسید = پاسخ سرور.** تمام اعداد از
> `GET /sales/invoices/:id/versions` (BE-043) می‌آیند، نه از
> `sale-draft-store` و نه از `calculateLinePricing`. `useInvoiceVersions`
> با `staleTime: Infinity` چون فاکتور append-only است (قاعده‌ی ۲-۷):
> نسخه‌ی موجود هرگز عوض نمی‌شود، فقط نسخه‌ی تازه اضافه می‌شود و آن هم از
> مسیر اصلاح فاکتور کلید را باطل می‌کند (FE-067). «نسخه‌ی جاری» با
> بیشترین `version` انتخاب می‌شود، نه با آخرین عضو آرایه — تکیه بر ترتیب
> آرایه یک فرض نانوشته است؛ تست با آرایه‌ی بی‌ترتیب همین را می‌سنجد.
>
> **تنها داده‌ی محلی روی رسید، نام مشتری است** — پاسخ فقط `partyId` دارد
> و نام یک برچسب است، نه عدد مالی. `version.partyId` با
> `outcome.party.id` سنجیده می‌شود و اگر نخواند نام نشان داده نمی‌شود،
> فقط شناسه: برچسب اشتباه روی رسیدی که به مشتری نشان داده می‌شود از
> نبود برچسب بدتر است.
>
> ⚠️ **سه چیز که این endpoint نمی‌دهد** و عمداً حدس زده نشدند:
> - **مظنه‌ی قفل‌شده** روی `sales_invoices.quoteAmountRial` ذخیره است
>   (`sales-invoice-pdf.service.ts` می‌خواندش) ولی
>   `salesInvoiceVersionHistorySchema` برنمی‌گرداندش. اینجا از `outcome`
>   (کپیِ لحظه‌ی ثبت) نشان داده و صریح «نرخ قفل‌شده‌ی این فروش» برچسب
>   می‌خورد. روی رسیدی که از فهرست فاکتورها باز شود (FE-065) این ردیف
>   بدون یک فیلد تازه در قرارداد ساختنی نیست — **یک تسک بک‌اندی لازم دارد**.
> - **پرداخت و مانده**: «پرداخت کامل / مانده صفر» از **نوع خودِ فروش**
>   (نقدی) نتیجه می‌شود، نه از مقایسه‌ی اعداد. نسیه (FE-047) این‌طور
>   نیست و به `paidRial` در قرارداد نیاز دارد.
> - **چاپ/PDF**: `GET /sales/invoices/:id/pdf` واقعاً وجود دارد، ولی
>   جریان دانلود فایل تست و کار خودش را می‌خواهد و تسک هم صریح می‌گوید
>   «در آینده». دکمه نشان داده می‌شود و `disabled` است، نه پنهان.
>
> mock جدید `GET /api/sales/invoices/:id/versions` فقط فاکتورهای ثبت‌شده
> در همان نشست را می‌شناسد — همان چیزی که POST ساخته، نه یک fixture
> ثابت؛ وگرنه رسید در توسعه چیزی نشان می‌دهد که هیچ‌وقت ثبت نشده.
>
> ۱۴ تست تازه (`SaleReceipt.test.tsx`). `typecheck`, `eslint`,
> `check:circular` و `pnpm test` سبز: ۶۲ فایل / ۵۶۲ تست.

### نمایش
* شماره فاکتور
* نسخه
* زمان
* مشتری
* اقلام
* مظنه قفل‌شده
* پرداخت
* جمع
* مانده
* اقدام چاپ یا PDF در آینده
### تمام است وقتی
* mobile receipt قابل نمایش به مشتری باشد.
* هیچ داده‌ای از محاسبه محلی جایگزین پاسخ ثبت‌شده سرور نشود.
---
## [x] FE-047 — فروش نسیه ساده

> `SalePaymentInput.tsx` در مرحله‌ی «مرور» دو حالت می‌دهد: **پرداخت
> کامل** (`paidRial === null`) و **پرداخت ناقص**. اقساط، سررسید و چک
> عمداً ساخته نشدند — بخش ۵ CLAUDE.md و خودِ BE-042 هر دو صریح بیرونشان
> گذاشته‌اند.
>
> **مسیر از روی `paidRial` انتخاب می‌شود، نه یک سوئیچ جدا.** `null` →
> `POST /sales/invoices/jewelry`؛ هر عدد دیگری، **حتی صفر** →
> `.../jewelry/credit`. صفر عمداً نسیه است نه «پرداخت کامل»: فروش با
> پرداخت صفر یک بدهی کامل است و باید در حساب شخص بنشیند، نه در صندوق.
>
> **مانده از سرور می‌آید.** `receivableRial` پاسخ نسیه مستقیم روی رسید
> نشان داده می‌شود؛ `payable − paid` سمت کلاینت هیچ‌جا محاسبه نمی‌شود،
> چون مبلغ واقعی سند را سرور می‌زند (نسخه‌ی کالا + `quoteId`) و ممکن است
> با پیش‌نمایش یکی نباشد. حتی «پرداخت‌شده» روی رسید از تفریق **دو عدد
> سرور** (`payable − receivable`) می‌آید، نه از عددی که کلاینت فرستاده.
> عددی که کنار فیلد ورودی دیده می‌شود صریح «مانده‌ی تخمینی» برچسب خورده.
>
> «بیشتر از مبلغ فاکتور» سمت کلاینت رد **نمی‌شود** — جمع کل کلاینت فقط
> پیش‌نمایش است؛ تنها قاعده‌ی قرارداد (منفی نبودن،
> `nonNegativeBigIntStringSchema`) اجرا می‌شود و بقیه تصمیم سرور است.
> برچسب بدهکار/بستانکار آینه‌ی `debtorCreditorLabel` در `PartyDetailPage`
> و پشت آن `directionFor` واقعی است.
>
> **یک کلید Idempotency برای هر دو مسیر**: اگر کاربر بعد از خطا مبلغ
> پرداختی را عوض کند و دوباره بزند، تلاش دوم همان عملیات است نه فروش
> تازه — سرور با دیدن همان کلید فاکتور دوم نمی‌سازد.
>
> ⚠️ **باگ واقعی که همین‌جا پیدا و رفع شد**: دادن مقدار store به `value`
> یک `NumericField` که خودش با `onChange` همان store را می‌نویسد، حلقه‌ی
> رندر می‌سازد (افکت مقداردهی اولیه → `setBuffer` → `onChange` → store →
> `value` تازه → …) و تست با «Maximum update depth exceeded» می‌افتاد.
> مبلغ حالا یک بار **سنکرون** در `useState` مقداردهی می‌شود، نه در افکت
> پس از mount — همان الگویی که بقیه‌ی فیلدهای کیپدی هم باید رعایت کنند.
>
> **بازآرایی‌ها:** `prepareJewelryCashSale` → `prepareJewelrySale` که حالا
> `{ mode, input }` تفکیک‌شده برمی‌گرداند؛ `useJewelryCashSaleSubmit` →
> `useJewelrySaleSubmit`؛ handler ساختگی فروش یک تابع مشترک
> (`registerJewelrySale`) شد که هر دو مسیر از آن عبور می‌کنند — تفاوت
> واقعی این دو فقط سند حسابداری و دو فیلد پرداخت است، نه قیمت‌گذاری یا
> شماره‌گذاری.
>
> ۱۰ تست تازه (`sale-submit.test.ts` ۵، `SaleWizardPage.test.tsx` ۳،
> `SaleReceipt.test.tsx` ۲). `typecheck`, `eslint`, `check:circular` و
> `pnpm test` سبز: ۶۲ فایل / ۵۷۲ تست.

### وابستگی بک‌اند
```text
BE-042
```
### قابلیت‌ها
* پرداخت ناقص
* نمایش مانده
* تعیین بعد مانده طبق قرارداد backend
* هشدار شفاف
* عدم ساخت اقساط و چک پیچیده
### تمام است وقتی
* مانده بعد از فروش در رسید نمایش داده شود.
* عبارت بدهکار/بستانکار واضح باشد.
---
# Milestone 11 — فروش سکه
## [x] FE-048 — فرم فروش سکه

> `CoinSaleForm.tsx` — برخلاف فروش زیورآلات (`SaleWizardPage`، ویزارد
> چندمرحله‌ای) یک صفحه‌ی تخت است: `createCoinSaleSchema` واقعی (BE-043)
> دقیقاً یک نوع سکه در هر فراخوانی می‌گیرد و «پرداخت» یک فیلد ساده است، نه
> دو مسیر نقدی/نسیه‌ی جدا مثل زیورآلات (FE-045/FE-047) — یک store یا
> wizard تازه برای این شکل ساده‌تر پیچیدگی بی‌دلیل بود.
>
> **سکه هرگز به وزن تبدیل نمی‌شود** (بخش ۲-۲): `count` یک `CountInput`
> مستقل است و در payload هم `number` صحیح می‌رود — تنها استثنای این قاعده
> در کل سیستم، دقیقاً مطابق `createCoinSaleSchema` واقعی.
>
> **قانون حباب** با همان الگوی `CoinInventoryList` (FE-038) اجرا می‌شود:
> `toCoinType` یک کپی محلی هشت‌خطی است (نه انتزاع مشترک زودهنگام).
> `bubble()` فقط `CentralBankMintedCoinType` می‌پذیرد، پس نوع خصوصی اصلاً
> امکان محاسبه ندارد، نه اینکه شرط زمان اجرا مخفی‌اش کند.
>
> **موجودی همان نوع** فقط نمایش داده می‌شود (فعلی و پس‌از‌فروش)، ثبت را
> مسدود نمی‌کند — نه «تمام است وقتی» و نه `CoinSalesService` واقعی چنین
> سقفی دارند. تنها سقف واقعی «پرداختی بیشتر از مبلغ فاکتور» است
> (`CoinSalePaidRialExceedsPayableError`، برخلاف نسیه‌ی زیورآلات که این
> حالت را می‌پذیرد) — همینجا هم پیش از ارسال بررسی می‌شود.
>
> **مظنه قفل نمی‌شود** — این یک صفحه‌ی تک‌مرحله‌ای است، نه یک مرور پس از
> چند قدم مثل `SaleSummary` (FE-044)؛ نرخ زنده تا لحظه‌ی ثبت می‌آید، دقیقاً
> همان الگوی `CoinInventoryList` پیش از FE-044.
>
> ⚠️ **باگ واقعی پیدا و رفع شد، از همان خانواده‌ی باگ FE-047**: مبلغ
> پرداختی ابتدا با یک مقدار مشتق‌شده (`paidTouched ? paidRial :
> payableRial`) به `MoneyInput` داده می‌شد تا «پیش‌فرض پرداخت کامل» زنده
> بماند. چون این مقدار مشتق هر بار تایپ در تعداد/قیمت عوض می‌شد، افکت
> مقداردهی اولیه‌ی خودِ `NumericField` یک اعلان `onChange` «ساختگی» ساخت —
> درست وسط تایپ «قیمت واحد بازار» — و فیلد پرداخت با یک عدد میانی ناقص
> «لمس‌شده» علامت خورد و برای همیشه از همان عدد یخ زد (نه خطای رندر بی‌نهایت
> این‌بار، چون منبع مشتق با هر onChange عوض می‌شد نه با یک store مشترک —
> ولی همان‌قدر خاموش و خطرناک بود: تست نشان داد `paidRial` نهایی «۰» ثبت
> می‌شد، نه مبلغ واقعی). اصلاح: `paidRial` فقط state محلی ساده است،
> `value`/`onChange` مستقیم روی همان؛ «پرداخت کامل» حالا یک کپی **یک‌باره**
> با کلیک دکمه است، نه یک هم‌گامی پیوسته‌ی مشتق‌شده از رندر.
>
> ۵ تست تازه (`CoinSaleForm.test.tsx`). `typecheck`, `eslint`,
> `check:circular` و `pnpm test` سبز: ۶۳ فایل / ۵۷۷ تست.

### وابستگی بک‌اند
```text
BE-043
```
### فیلدها
* نوع سکه
* تعداد
* قیمت واحد بازار
* مظنه
* پرداخت
### نمایش
* جمع قیمت
* ارزش ذاتی
* حباب
* فقط برای سکه بانک مرکزی
### قواعد
* تعداد integer
* سکه به وزن state تبدیل نشود.
* قیمت واحد string ریالی
* موجودی همان نوع بررسی شود.
### تمام است وقتی
* فروش یک یا چند نوع سکه قابل ثبت باشد.
* حباب برای نوع غیرمجاز اصلاً render نشود.
---
## [x] FE-049 — خلاصه موقعیت سکه در معامله

> `CoinPositionSummary.tsx` — کامپوننت گزارشی خالص (فقط props → JSX، بدون
> query یا store) که در `CoinSaleForm` (FE-048) ترکیب می‌شود و جای بخش
> ad-hoc آن تسک (خط «موجودی فعلی/پس از فروش» + کارت «جمع قیمت/ارزش
> ذاتی/حباب») را می‌گیرد — دو پیاده‌سازی موازی از یک مفهوم نگه‌داشته
> نمی‌شد.
>
> **ارزش‌ها روی «تعداد فروش» حساب می‌شوند، نه موجودی باقی‌مانده** — «موقعیت
> ... در معامله» یعنی ارزش خودِ همین معامله. تفاوت با نسخه‌ی قبلی FE-048:
> آنجا ارزش ذاتی/حباب «هر سکه» (تک‌واحدی) بود؛ اینجا کل موقعیت معامله‌شده
> (`coinPositionValue` روی `intrinsicValue`/`bubble` تک‌واحدی، همان تابعی
> که برای «تعداد × قیمت بازار» هم استفاده می‌شد — ضرب در تعداد، نه فرمول
> جدید).
>
> **هر نوع سکه مستقل** با طراحی، نه با قرارداد اضافه: کامپوننت دقیقاً یک
> `CoinType` می‌بیند و هیچ مجموع بین‌نوعی نمی‌سازد؛ چند نوع یعنی چند
> نمونه، نه یک نسخه‌ی جمع‌زننده.
>
> ۵ تست تازه در `CoinPositionSummary.test.tsx` (اعداد fixture همان
> `CoinInventoryPage.test.tsx` برای تطبیق دستی) + ۲ تست `CoinSaleForm.test.tsx`
> برای اتصال (موجودی جاری → «تعداد قبل») به‌روز شد. `typecheck`, `eslint`,
> `check:circular` و `pnpm test` سبز: ۶۴ فایل / ۵۸۲ تست.

### نمایش
* تعداد قبل
* تعداد فروش
* تعداد بعد
* ارزش بازار
* ارزش ذاتی
* حباب نمایشی
### قواعد
* این component گزارش است و storage را تغییر نمی‌دهد.
* انواع سکه مستقل باشند.
---
# Milestone 12 — پرداخت و تسویه
## [x] FE-050 — انتخاب روش پرداخت

> `PaymentMethodSelector.tsx` — فقط انتخاب‌گر است، نه فرم‌های خودِ روش‌ها.
> `apps/web/src/features/payments/` عمداً پوشه‌ی جدید و جدا از `sales/`
> است: Milestone 12 هم پرداخت فروش را پوشش می‌دهد هم تسویه‌ی شخص (FE-055)،
> پس این انتخاب‌گر زیرمجموعه‌ی `sales` نیست.
>
> پنج روش («روش‌های فاز ۱» خودِ تسک) یک radio-group‌اند، نه چندانتخابی —
> `COMBINED` («ترکیبی») یک مقدار مستقل پنجم است، دقیقاً چون FE-054 («تسویه
> ترکیبی») خودش یک تسک جداست، نه توسیع این کامپوننت.
>
> **هیچ فرم روشی اینجا ساخته نشد** — `RIAL`→FE-051، `GOLD`→FE-052
> (BE-046)، `COIN`→FE-053 (BE-047)، `COMBINED`→FE-054، هیچ‌کدام هنوز
> وجود ندارند. این تسک فقط تعیین می‌کند کدام فعال است؛ مصرف‌کننده‌ی آینده
> (فروش یا تسویه) بر اساس `value` تصمیم می‌گیرد کدام فرم مستقل را رندر
> کند. هنوز به هیچ صفحه‌ای وصل نشده — بدون یک صفحه‌ی واقعی که این پنج فرم
> را نیاز داشته باشد (اولینشان FE-055)، وصل‌کردنش زودرس بود.
>
> ۴ تست تازه (`PaymentMethodSelector.test.tsx`). `typecheck`, `eslint`,
> `check:circular` و `pnpm test` سبز: ۶۵ فایل / ۵۸۶ تست.

### روش‌های فاز ۱
* ریال
* طلا
* سکه
* نسیه
* ترکیبی
### قواعد
* چک پیچیده ساخته نشود.
* قسط‌بندی ساخته نشود.
* هر روش component مستقل داشته باشد.
---
## [x] FE-051 — پرداخت ریالی

> ⚠️ **این تسک در FrontTasks.md بدون «وابستگی بک‌اند» ثبت شده بود، ولی
> بک‌اندش از قبل آماده است**: `TASKS.md` → `BE-045 — پرداخت ریالی روی
> مانده‌ی شخص` (`[x]`)، `POST /parties/:partyId/settlements/rial`.
> `RialSettlementForm.tsx` مستقیم همان را می‌زند.
>
> **فقط «مبلغ» فیلد واقعی است.** «حساب مقصد در صورت نیاز» و «توضیح» در
> فهرست فیلدهای تسک بودند، اما `createRialSettlementSchema` واقعی
> (`.strict()`) فقط `amountRial`/`effectiveAt` می‌پذیرد —
> `RialSettlementsService` مقصد را همیشه ثابت (صندوق) می‌سازد، از ورودی
> کاربر نمی‌خواند. فیلدی که سرور رد می‌کند بدتر از نبودنش است، پس این دو
> عمداً ساخته نشدند.
>
> **بدون سقف «بیشتر از طلب»** — برخلاف فروش سکه (FE-048)، سرویس واقعی هیچ
> مقایسه‌ای با مانده ندارد؛ پرداخت بیشتر فقط شخص را بستانکار می‌کند، خطا
> نیست (هم‌راستا با اختلاف طبیعی خرید دست‌دوم B2C، بخش ۲-۵ CLAUDE.md).
> تنها اعتبارسنجی، همان قرارداد: مبلغ مثبت.
>
> **کامپوننت مستقل و خودکفاست** («هر روش component مستقل داشته باشد»،
> FE-050): فقط `partyId` می‌گیرد، خودش `usePartyBalances` را برای مانده‌ی
> «پرداخت کامل» می‌خواند و مستقیم submit می‌کند — بدون نیاز به صفحه‌ی
> میزبانی که هنوز ساخته نشده (FE-055). کیپد خودش را ندارد (یک `<div>`
> slot است)؛ صفحه‌ی میزبان یک `<NumericKeypad />` مشترک می‌دهد.
>
> **«پرداخت کامل» یک کپی یک‌باره است، نه هم‌گامی پیوسته** — همان درسی که
> از باگ واقعی FE-047/FE-048 آمد: مقدار مشتق‌شده‌ای که هر رندر عوض شود و
> به `value` یک فیلد کیپدی برود، فیلد را با یک عدد میانی «لمس‌شده» یخ
> می‌زند. `amountRial` فقط از `useState` خودش می‌آید.
>
> ۵ تست تازه (`RialSettlementForm.test.tsx`). `typecheck`, `eslint`,
> `check:circular` و `pnpm test` سبز: ۶۶ فایل / ۵۹۱ تست.

### فیلدها
* مبلغ
* حساب مقصد در صورت نیاز
* توضیح
### قواعد
* Money Input
* Idempotency
* مقدار string
### تمام است وقتی
* پرداخت کامل و ناقص قابل ثبت باشد.
---
## [x] FE-052 — پرداخت با طلا

> `GoldSettlementForm.tsx` — آینه‌ی `RialSettlementForm` (FE-051): فقط
> `partyId` می‌گیرد و مستقیم `POST /parties/:partyId/settlements/gold`
> (BE-046) را می‌زند. برخلاف تسویه‌ی ریالی، اینجا واقعاً تبدیل واحد در کار
> است، پس «مظنه قفل‌شونده» هم واقعاً معنا دارد.
>
> **قفل نرخ با یک گذار یک‌طرفه‌ی ساده**: `useEffect` فقط وقتی
> `lockedQuote === null` بود آن را از اولین مظنه‌ی در دسترس پر می‌کند؛
> بعد از آن شرط خودش هر نوشتن دوباره را می‌بندد. تست جداگانه‌ای این را
> می‌سنجد: مظنه‌ی زنده عوض می‌شود ولی معادل نمایشی و «نرخ ردیف» ثابت
> می‌مانند. این با باگ واقعی FE-047/FE-048 فرق دارد — آنجا منبع مشتق هر
> رندر عوض می‌شد و به یک فیلد کیپدی می‌رفت؛ اینجا یک گذار تک‌باره‌ی
> `null → مقدار` است، نه یک جریان دائماً زنده.
>
> **پیش‌نمایش («معادل نمایشی») دقیقاً فرمول سرور را آینه می‌کند** —
> `bullionPrice` از `core-calc` همان `valueOfPure(toPureMg(...), rate1000)`
> است که `GoldSettlementsService` واقعی حساب می‌کند؛ طلای خام هیچ‌وقت حباب
> ندارد (بخش ۲-۳ CLAUDE.md)، پس این فرمول تقریب نیست، دقیقاً همان محاسبه
> است. مبلغ نهایی و «نرخ ردیف» پس از ثبت از پاسخ سرور می‌آیند
> (`goldRatePerGramRial`)، نه از پیش‌نمایش محلی.
>
> ⚠️ **خطای واحد در همین تسک پیدا و رفع شد، نه در کد نهایی**: تست اول
> وزن گرم به میلی‌گرم را با ضرب در یک میلیون تایپ می‌کرد (عادت واحد
> میکروگرمی مشخصات مرجع سکه) در حالی که `DIGIT_SPECS.weight` سه رقم اعشار
> ضمنی گرم دارد — بدون جداکننده، رقم‌های تایپ‌شده بخش صحیح (گرم) می‌شوند و
> خودکار ×۱۰۰۰ به میلی‌گرم تبدیل می‌شوند؛ یعنی تایپ «۱۰» خودش ۱۰ گرم است،
> نه «۱۰۰۰۰». تست پیش از commit اصلاح شد؛ خودِ کامپوننت هیچ‌وقت این فرض
> غلط را نداشت.
>
> ۵ تست تازه (`GoldSettlementForm.test.tsx`). `typecheck`, `eslint`,
> `check:circular` و `pnpm test` سبز: ۶۷ فایل / ۵۹۶ تست.

### وابستگی بک‌اند
```text
BE-046
```
### فیلدها
* وزن
* عیار
* مظنه قفل‌شونده
* معادل نمایشی
### قواعد
* وزن خالص با core-calc
* payload وزن string
* نرخ ردیف نمایش داده شود.
---
## [x] FE-053 — پرداخت با سکه

> `CoinSettlementForm.tsx` — آینه‌ی `GoldSettlementForm` (FE-052) و
> `CoinSaleForm` (FE-048) با هم: فقط `partyId` می‌گیرد و مستقیم `POST
> /parties/:partyId/settlements/coins` (BE-047) را می‌زند؛ فرمول‌های
> ارزش ذاتی/حباب/معادل نمایشی دقیقاً همان `core-calc` که `CoinSaleForm`
> استفاده می‌کند، چون سرویس واقعی هم همان توابع را می‌زند
> (`intrinsicValue`/`bubble`/`coinPositionValue` تک‌واحدی روی تعداد).
>
> **بدون قفل مظنه** — برخلاف تسویه‌ی طلا (FE-052) که صریح «قفل‌شونده»
> می‌خواست، این تسک چنین قاعده‌ای ندارد؛ نرخ زنده تا لحظه‌ی ثبت می‌آید،
> همان الگوی خودِ `CoinSaleForm`.
>
> **«تبدیل به وزن فقط نمایشی»** به‌صورت یک خط زیر تعداد پیاده شد
> (`معادل وزنی (فقط نمایشی)`) — از `count` و `grossWeightUg` کاتالوگ
> ساخته می‌شود، در `payload` نیست. تستی که این را می‌سنجد صریح چک می‌کند
> `grossWeightMg`/`grossWeightUg` در بدنه‌ی ارسالی نباشند.
>
> ۵ تست تازه (`CoinSettlementForm.test.tsx`). `typecheck`, `eslint`,
> `check:circular` و `pnpm test` سبز: ۶۸ فایل / ۶۰۱ تست.

### وابستگی بک‌اند
```text
BE-047
```
### فیلدها
* نوع سکه
* تعداد
* نرخ بازار
* معادل نمایشی
### قواعد
* هر نوع سکه ردیف مستقل
* count integer
* تبدیل به وزن فقط نمایشی
---
## [x] FE-054 — تسویه ترکیبی

> `MixedSettlementForm.tsx` — برخلاف FE-051..053 که هر کدام دقیقاً یک
> ردیف می‌سازند، این یکی سبد چندردیفی است: `POST
> /parties/:partyId/settlements/mixed` (BE-048) یک آرایه‌ی `lines`
> می‌گیرد و همه را در **یک** تراکنش اتمیک ثبت می‌کند — «کل فرم یک
> Idempotency-Key داشته باشد» و «submit اتمیک تلقی شود» هر دو مستقیم از
> شکل خودِ endpoint می‌آیند، نه چیزی که کلاینت جداگانه تضمین کند.
>
> **چهارمین نوع ردیف («مانده اعتباری») همان `CREDIT` قرارداد واقعی
> است** — «اعتبار ریالی موجود شخص روی طلبش». سقفش
> (`MixedSettlementInsufficientCreditError`) از حساب پرداختنی شخص
> می‌آید که هیچ GET جداگانه‌ای ندارد؛ این‌جا از `rawBalances.rial` منفی
> (`GET /parties/:id/balances`، که خودش receivable/payable را
> ترکیب‌شده برمی‌گرداند) به‌عنوان تقریب معتبر استفاده شد — پیش‌نمایش و
> سقف کلاینتی، سرور مرجع نهایی می‌ماند.
>
> **یک مظنه‌ی قفل‌شده برای کل فرم، نه هر ردیف جدا** — این یک معامله‌ی
> واحد در یک لحظه است، همان استدلال «submit اتمیک»؛ فقط وقتی حداقل یک
> ردیف طلا/سکه وجود دارد قفل می‌شود (ردیف‌های خالص ریال/اعتبار اصلاً به
> مظنه نیاز ندارند). «نرخ هر ردیف جدا نمایش داده شود» با این برآورده شد
> که هر ردیف طلا/سکه پیش‌نمایش و نرخ خودش را زیر فیلدهایش نشان می‌دهد.
>
> **خطای هر ردیف کنار همان ردیف** فقط برای اعتبارسنجی کلاینتی است (فیلد
> خالی، سقف اعتبار) — سرویس‌های واقعی هیچ اندیس ردیفی در خطاهایشان
> برنمی‌گردانند، پس رد کل درخواست از سمت سرور سراسری نمایش داده می‌شود
> (`ApiErrorNotice`)، نه به یک ردیف خاص چسبانده می‌شود؛ این محدودیت
> صریح مستند شد، نه پنهان.
>
> ۶ تست تازه (`MixedSettlementForm.test.tsx`) — نمونه‌ی ریال+طلا+سکه در
> یک درخواست، سقف اعتبار، افزودن/حذف ردیف، double tap.
> `typecheck`, `eslint`, `check:circular` و `pnpm test` سبز: ۶۹ فایل /
> ۶۰۷ تست.

### وابستگی بک‌اند
```text
BE-048
```
### قابلیت‌ها
* افزودن ردیف
* حذف ردیف
* ریال
* طلا
* چند نوع سکه
* مانده اعتباری
* خلاصه پوشش بدهی
* باقی‌مانده
### قواعد
* نرخ هر ردیف جدا نمایش داده شود.
* کل فرم یک Idempotency-Key داشته باشد.
* submit اتمیک تلقی شود.
### تمام است وقتی
* نمونه ریال + طلا + سکه ثبت شود.
* خطای یک ردیف کنار همان ردیف نمایش داده شود.
* باقی‌مانده دقیق باشد.
---
## [x] FE-055 — صفحه ثبت تسویه شخص

> `SettlementsNewPage.tsx` — این تسک عمدتاً **اسمبل کردن** Milestone 12
> بود، نه ساخت چیز تازه: مانده فعلی + `PaymentMethodSelector` (FE-050) +
> رندر همان فرم انتخاب‌شده (FE-051..054)، همه‌شان از قبل مستقل و کامل
> ساخته شده بودند. جانگه‌دار قدیمی (`SettlementsNewPlaceholder`) روی
> مسیر `/parties/:partyId/settlements/new` با این صفحه جایگزین شد؛ مسیر
> تخت `/settlements/new` دست‌نخورده ماند (خارج از دامنه‌ی همین تسک).
>
> **«نسیه» عمداً از انتخاب‌گر کنار گذاشته شد** — `PaymentMethodSelector`
> یک prop تازه‌ی اختیاری (`methods`) گرفت تا این صفحه بتواند زیرمجموعه‌ی
> واقعی (`RIAL/GOLD/COIN/COMBINED`) را نشان دهد: «نسیه» یعنی الان پول
> نگرفتن روی یک فروش تازه (همان واژه‌ی `FE-047`)، نه روی بدهی‌ای که همین
> الان دارد تسویه می‌شود، و هیچ endpoint تسویه‌ی مستقلی هم برایش نیست.
>
> **«preview مانده بعد» را این صفحه نمی‌سازد** — هر چهار فرم زیرمجموعه
> از قبل موجودی جاری را خودشان می‌خوانند؛ این تسک فقط سه فرم ساده
> (`RialSettlementForm`, `GoldSettlementForm`, `CoinSettlementForm`) را
> با یک ردیف «مانده پس از این پرداخت» (`receivableRial - پیش‌نمایش`)
> تکمیل کرد — `MixedSettlementForm` از قبل «باقی‌مانده»ی خودش را داشت.
> تکرار همین محاسبه در صفحه یعنی دو منبع حقیقت برای یک عدد.
>
> **invalidate کردن مانده/statement کار این صفحه نیست** — هر چهار فرم از
> قبل بعد از موفقیت `queryKeys.parties.all()` را invalidate می‌کنند
> (که `balances`/`statement` هر دو زیرمجموعه‌اش‌اند) — دقیقاً «تمام است
> وقتی» این تسک، بدون کد اضافه.
>
> ⚠️ **یافته‌ی واقعی، نه باگ**: `UnitToggle` (سربرگ) هم یک radiogroup با
> گزینه‌های «طلا»/«ریال» است — همان برچسب‌های `PaymentMethodSelector`
> برای یک مفهوم کاملاً جدا (واحد نمایش، نه روش تسویه). این اولین صفحه‌ای
> است که هر دو کنار هم می‌نشینند؛ هر جستجوی تست باید صریح به radiogroup
> «روش پرداخت» محدود شود. برای کاربر واقعی مشکلی نیست (دو گروه با
> `aria-label` جدا و موقعیت بصری متفاوت)، ولی برای تست خودکار جدید کاشف
> بود.
>
> ۷ تست تازه (`SettlementsNewPage.test.tsx`) + ۲ تست تازه در
> `GoldSettlementForm.test.tsx`/`CoinSettlementForm.test.tsx` برای ردیف
> «مانده پس از این پرداخت». `typecheck`, `eslint`, `check:circular` و
> `pnpm test` سبز: ۷۰ فایل / ۶۱۶ تست.

### مسیر
```text
/parties/:partyId/settlements/new
```
### نمایش
* مانده فعلی
* واحد فعال
* settlement lines
* preview مانده بعد
* تأیید نهایی
### تمام است وقتی
* از صفحه شخص قابل دسترسی باشد.
* بعد از ثبت، مانده و statement invalidate شوند.
---
# Milestone 13 — خرید از مصرف‌کننده
## [x] FE-056 — صفحه خرید طلای دست‌دوم

> `PurchaseWizardPage.tsx` و `PurchaseStepper.tsx` — جریان ۸ مرحله‌ای خرید طلای دست‌دوم
> در مسیر `/purchase/second-hand` به‌صورت کاملاً مستقل از جریان فروش (بدون وابستگی به `sales/`).
>
> **قواعد پیاده‌شده:**
> * UI مستقل از ویزارد فروش و بدون کامپوننت مشترک غیرضروری.
> * مقصد پیش‌فرض کالا (موجودی آبشده) با پیام واضح در مرحله‌ی فروشنده نمایش داده می‌شود.
> * انتخاب فروشنده به اشخاص مصرف‌کننده (`CONSUMER`) محدود است؛ در صورت انتخاب همکار، هشدار صریح نمایش داده شده و دکمه‌ی «بعدی» قفل می‌شود.
> * مرحله‌ی مظنه مستقیماً با `MaznehBar` هماهنگ است و بدون مظنه معتبر پیشروی قفل می‌شود.
> * عیار پیش‌فرض ۷۴۰ یا هر عدد دیگری در UI هاردکد نشده و تنظیمات سرور ملاک خواهد بود.
> * مراحل وزنی، کسورات، عیار، مبلغ و پرداخت با پیام‌های راهنمای مشخص برای تسک‌های بعدی (FE-057 تا FE-059) آماده شده‌اند.
> * ذخیره‌سازی پیش‌نویس با `usePurchaseDraftStore` و `sessionStorage`، به‌همراه محافظ خروج مسیر (`useBlocker`).
>
> **تست‌ها:**
> * ۱۱ تست اختصاصی در `PurchaseWizardPage.test.tsx` و ۱۰ تست در `purchase-draft-store.test.ts` (مجموع ۲۱ تست تازه).
> * `typecheck`، `eslint`، `check:circular`، `build` و `test` سبز: ۷۲ فایل / ۶۳۷ تست پاس شدند.

### وابستگی بک‌اند
```text
BE-050
```
### مراحل
1. فروشنده
2. وزن‌کشی
3. کسورات
4. عیار
5. مظنه
6. مبلغ
7. پرداخت
8. رسید
### قواعد
* UI مستقل از فروش باشد.
* مقصد پیش‌فرض آبشده نمایش داده شود.
* عیار پیش‌فرض از API باشد.
* ۷۴۰ در منطق UI هاردکد نشود.
---
## [x] FE-057 — فرم وزن‌کشی دست‌دوم
### فیلدها
* وزن کل
* وزن نگین
* سایر متعلقات
* وزن خالص
* عیار خرید
* کارمزد اختیاری
### نمایش
* نرخ گرم عیار خرید
* مبلغ قبل کارمزد
* کارمزد
* مبلغ نهایی
### تمام است وقتی
* تمام محاسبات preview از core-calc باشند.
* نتیجه سرور با preview مقایسه شود.
> **پیاده‌سازی:**
> - ایجاد ماژول محاسبات دامنه `purchase-pricing.ts` مبتنی بر `calculateSecondHandGoldPurchase`، `dualFromRial` و `gramRate` از `@gold/core-calc`.
> - گسترش `purchase-draft-store.ts` با فیلدهای وزن ناخالص، کسورات (نگین و سایر)، عیار خرید انتخابی و کارمزد اختیاری.
> - پیاده‌سازی کامپوننت `SecondHandWeighingForm.tsx` با کنترل‌های اختصاصی `WeightInput`، `KaratInput`، `MoneyInput` و پیش‌نمایش دو مقیاسه طلا/ریال.
> - یکپارچه‌سازی با مراحل ۲ تا ۶ `PurchaseWizardPage.tsx` به همراه صفحه‌کلید عددی و کلید تعویض واحد سراسری.
> - اعتبارسنجی کامل با تست‌های واحد و کامپوننت (۷۴ فایل تست، ۶۴۵ تست سبز).
---
## [x] FE-058 — ثبت مشخصات فروشنده
### فیلدها
* انتخاب شخص
* نام
* موبایل
* کد ملی اختیاری
### قواعد
* کد ملی فعلاً اختیاری
* امکان ایجاد شخص inline
* اطلاعات هویتی به‌صورت امن نمایش داده شود.
> **پیاده‌سازی:**
> * مرحله‌ی مستقل `SellerDetailsStep` با انتخاب شخص موجود، خلاصه‌ی مشخصات و هشدار انتخاب همکار اضافه شد.
> * ایجاد inline فروشنده، نوع را روی مصرف‌کننده قفل می‌کند و نام، موبایل و کد ملی اختیاری را با نرمال‌سازی ارقام فارسی می‌پذیرد.
> * موبایل و کد ملی در UI پوشانده می‌شوند؛ کد ملی خام وارد recent parties یا draft خرید نمی‌شود.
> * تست‌های هدفمند ۴۳/۴۳ و gate کامل فرانت ۶۵۲/۶۵۲ سبز شدند؛ نمای ۳۶۰px، افقی و حالت تیره نیز بدون overflow یا خطای کنسول بررسی شد.
---
## [x] FE-059 — ثبت خرید دست‌دوم
### رفتار
* submit با idempotency
* نمایش pending
* receipt
* refresh موجودی آبشده
* refresh مانده شخص
### تمام است وقتی
* عملیات End-to-End موفق باشد.
* خطای سرور فرم را قابل اصلاح نگه دارد.
  * نتیجه ثبت‌شده سرور نمایش داده شود.
---
> تکمیل FE-059: ثبت با کلید و بدنه ثابت برای تلاش مجدد، جلوگیری از ارسال هم‌زمان، وضعیت pending، اصلاح فرم پس از رد قطعی سرور و رسید مبتنی بر پاسخ سرور. موجودی و مانده اشخاص پس از موفقیت بازخوانی می‌شوند. حلقه‌ی همگام‌سازی کیپد در StrictMode نیز با تست رگرسیون اصلاح شد.
>
> اعتبارسنجی: gate:frontend سبز؛ ۶۵۷ تست فرانت، typecheck، lint، بررسی وابستگی دوری و build. بسته اولیه ۱۴۹٫۷KB gzip. مسیر کامل با API شبیه‌سازی‌شده در مرورگر و رسید ۳۶۰px بدون خطای کنسول بررسی شد؛ بک‌اند واقعی و دستگاه اندروید واقعی در این اجرا بررسی نشدند.

## [x] FE-060 — خرید سکه از مصرف‌کننده
### وابستگی بک‌اند
```text
BE-051
```
### فیلدها
* شخص
* نوع سکه
* تعداد
* نرخ خرید
* پرداخت یا مانده
### قواعد
* نرخ خرید با نرخ فروش یکی فرض نشود.
* حباب فقط برای نوع مجاز نمایش داده شود.
* موجودی همان نوع سکه refresh شود.

> تکمیل FE-060: مسیر مستقل خرید سکه از مصرف‌کننده با انتخاب فروشنده، نوع، تعداد صحیح، نرخ خرید مستقل از فروش و پرداخت/مانده اضافه شد. حباب فقط برای سکه‌های ضرب بانک مرکزی نمایش می‌یابد، ثبت با Idempotency-Key و تلاش‌مجدد امن انجام می‌شود و رسید مبتنی بر پاسخ سرور همراه با بازخوانی موجودی همان نوع سکه و مانده اشخاص نمایش می‌گیرد.
>
> اعتبارسنجی: ۶۶۴ تست فرانت، typecheck و lint سبز؛ مسیر کامل ثبت با API شبیه‌سازی‌شده در مرورگر ۳۶۰px بدون خطای کنسول بررسی شد. بک‌اند واقعی و دستگاه اندروید واقعی در این اجرا بررسی نشدند.
---
# Milestone 14 — مرجوعی B2C
## [x] FE-061 — شروع Buyback از فاکتور
### وابستگی بک‌اند
```text
BE-052
```
### مسیر
از صفحه فاکتور فروش:
```text
خرید مجدد از مشتری
```
### قواعد
* عنوان «مرجوعی» به‌تنهایی استفاده نشود.
* توضیح داده شود که این عملیات خرید طلای دست‌دوم است.
* فاکتور اصلی فقط reference است.
* B2B return ساخته نشود.

> تکمیل FE-061: از رسید فاکتور فروشِ مشتری مصرف‌کننده، اقدام «خرید مجدد از مشتری» به مسیر مستقل Buyback B2C اضافه شد. صفحه‌ی آغازین به‌صراحت خرید طلای دست‌دوم بودن عملیات، فقط-مرجع بودن فاکتور اولیه و تغییرنکردن فروش قبلی را توضیح می‌دهد. برای همکار هیچ مسیر بازگشت یا Buyback ساخته نمی‌شود؛ وزن‌کشی، مقایسه و ثبت سند در FE-062 و FE-063 باقی مانده‌اند.
>
> اعتبارسنجی: gate:frontend سبز؛ ۶۶۸ تست وب، typecheck، lint، بررسی وابستگی دوری و build. بسته اولیه ۱۴۹٫۹KB gzip. نمای ۳۶۰px حالت تیره و وضعیت نبودن فاکتور مرجع در مرورگر محلی، بدون خطای کنسول بررسی شد.
---
## [x] FE-062 — صفحه مقایسه خرید اولیه و خرید امروز
### نمایش اجباری
* مبلغ خرید اولیه
* مبلغ پرداختی امروز
* اجرت سوخته
* اختلاف عیار
* تغییر مظنه
* سایر اختلافات backend
* نتیجه نهایی
### قواعد
* کمتر یا بیشتر بودن مبلغ امروز error نیست.
* از رنگ قرمز به‌عنوان هشدار خطا استفاده نشود مگر خطای واقعی.
* متن شفاف و قابل نمایش به مشتری باشد.
### تمام است وقتی
* breakdown backend به‌وضوح نمایش داده شود.
* اطلاعات نرخ و تاریخ هر دو رویداد جدا باشد.

> تکمیل FE-062: صفحه‌ی Buyback B2C اکنون وزن و عیار اندازه‌گیری‌شده‌ی امروز را فقط برای پیش‌نمایش به backend می‌فرستد و خرید اولیه و خرید امروز را با تاریخ، زمان ثبت مظنه، نرخ قفل‌شده و مبلغ مستقل نمایش می‌دهد. breakdown پاسخ backend شامل اجرت سوخته، اختلاف عیار، تغییر مظنه، سایر اختلافات و نتیجه نهایی است. کمتر یا بیشتر بودن مبلغ امروز با پیام خنثی و بدون هشدار خطا نمایش داده می‌شود. این گام عمداً پرداخت یا ثبت خرید ندارد؛ آن‌ها در FE-063 می‌آیند.
>
> اعتبارسنجی: gate:frontend سبز؛ ۶۷۴ تست وب، typecheck، lint، بررسی وابستگی دوری و build. جریان ورود وزن با کیپد، payload پیش‌نمایش، نمایش breakdown و تعویض واحد با تست کامپوننت پوشش داده شد. بسته‌ی اولیه ۱۵۰٫۰KB gzip است.
---
## [ ] FE-063 — ثبت Buyback B2C
### رفتار
* اتصال به فاکتور اصلی
* تکمیل اطلاعات وزن و عیار
* پرداخت
* idempotency
* receipt خرید
* لینک به سند جدید
### تمام است وقتی
* فاکتور اصلی تغییر نکند.
* سند خرید جدید قابل مشاهده باشد.
* موجودی آبشده refresh شود.
---
# Milestone 15 — فاکتورها و اصلاح
## [ ] FE-064 — فهرست فاکتورهای فروش
### قابلیت‌ها
* جست‌وجوی شماره
* فیلتر تاریخ
* فیلتر شخص
* فیلتر وضعیت
* pagination
* نمایش نسخه جاری
* نمایش اصلاح‌شده بودن
### موبایل
کارت با:
* شماره
* شخص
* مبلغ
* تاریخ
---
## [ ] FE-065 — صفحه جزئیات فاکتور
### نمایش
* شماره
* نسخه
* وضعیت
* مشتری
* اقلام
* پرداخت‌ها
* مظنه snapshot
* تنظیمات snapshot لازم
* ledger summary در حد مجاز
* history
* PDF
* خرید مجدد B2C
* اصلاح
### تمام است وقتی
* اطلاعات تاریخی با نرخ امروز تغییر نکنند.
* نسخه جاری واضح باشد.
---
## [ ] FE-066 — بررسی مجاز بودن اصلاح
### وابستگی بک‌اند
```text
BE-053
```
### رفتار
* دریافت policy result از backend
* نمایش دلیل عدم امکان
* نمایش نیاز به مدیر
* عدم بازسازی policy فقط در فرانت
### تمام است وقتی
* فرانت از تصمیم backend پیروی کند.
* action غیرمجاز پنهان یا disabled با توضیح باشد.
---
## [ ] FE-067 — فرم اصلاح فاکتور
### وابستگی بک‌اند
```text
BE-054
```
### فیلدها
* اقلام قابل اصلاح
* مشتری
* وزن
* عیار
* اجرت
* پرداخت
* دلیل
* توضیح برای OTHER
### نمایش
* قبل
* بعد
* اختلاف مبلغ
* اختلاف وزن
* اثر مانده
### قواعد
* شماره فاکتور تغییر نکند.
* نسخه جدید نمایش داده شود.
* پیام روشن درباره تغییرناپذیری نسخه قبلی.
---
## [ ] FE-068 — تاریخچه نسخه‌های فاکتور
### وابستگی بک‌اند
```text
BE-055
```
### نمایش
* نسخه
* زمان
* کاربر
* دلیل
* اختلاف
* وضعیت جاری
* مشاهده جزئیات
### تمام است وقتی
* نسخه قبلی قابل مشاهده باشد.
* هیچ action حذف وجود نداشته باشد.
---
# Milestone 16 — مانده و صورت‌حساب
## [ ] FE-069 — کارت مانده چندواحدی شخص
### وابستگی بک‌اند
```text
BE-056
```
### نمایش
* ریال
* طلای خالص
* هر نوع سکه
* بدهکار یا بستانکار
* معادل با واحد فعال
### قواعد
* سکه‌ها ادغام نشوند.
* واحد اصلی خام حفظ شود.
* Unit Toggle در دسترس باشد.
---
## [ ] FE-070 — صورت‌حساب شخص
### وابستگی بک‌اند
```text
BE-057
```
### قابلیت‌ها
* بازه تاریخ
* فیلتر نوع سند
* فیلتر بُعد
* running balance
* pagination
* نرخ snapshot
* unit toggle
* export PDF در آینده
### موبایل
هر رویداد card با:
* نوع
* تاریخ
* اثر
* مانده
* شماره سند
---
## [ ] FE-071 — فیلتر تاریخ شمسی
### هدف
component مشترک بازه تاریخ.
### قابلیت‌ها
* تقویم شمسی
* تبدیل امن به ISO
* امروز
* این هفته
* این ماه
* پاک کردن
* timezone tenant
### تمام است وقتی
* مرز روز timezone درست باشد.
* تاریخ‌ها در API مطابق قرارداد ارسال شوند.
---
# Milestone 17 — گزارش‌ها
## [ ] FE-072 — فهرست بدهکاران
### وابستگی بک‌اند
```text
BE-058
```
### قابلیت‌ها
* Unit Toggle
* جست‌وجو
* مرتب‌سازی
* pagination
* باز کردن شخص
* نمایش ابعاد اصلی
### تمام است وقتی
* تغییر واحد فقط نمایش را عوض کند.
* سکه‌ها در جزئیات مستقل باشند.
---
## [ ] FE-073 — فهرست بستانکاران
### قابلیت‌ها
مشابه بدهکاران با semantics صحیح بستانکاری.
### تمام است وقتی
* متن و علامت‌ها با بدهکاران اشتباه نشوند.
* Unit Toggle وجود داشته باشد.
---
## [ ] FE-074 — داشبورد اصلی
### وابستگی بک‌اند
```text
BE-059
```
### کارت‌ها
* فروش امروز
* خرید امروز
* دریافت امروز
* پرداخت امروز
* تعداد فاکتور
* بدهکاران
* بستانکاران
* موجودی آبشده
* موجودی انواع سکه
* مظنه جاری
### قواعد
* تمام کارت‌های مالی با Unit Toggle تغییر کنند.
* نقره و شمش نمایش داده نشوند.
* نمودار سنگین غیرضروری ساخته نشود.
* skeleton پایدار باشد.
---
## [ ] FE-075 — گزارش سود
### وابستگی بک‌اند
```text
BE-060
```
### نمایش
* درآمد
* بهای تمام‌شده
* سود ناخالص
* سود عملیاتی
* اثر نوسان طلا
* اثر حباب سکه
* ریال
* طلای معادل
### قواعد
* Unit Toggle اجباری
* داده محاسبه‌شده backend مبنا باشد.
* نرخ تاریخی با نرخ امروز جایگزین نشود.
* حباب شمش یا نوع غیرمجاز نمایش داده نشود.
### تمام است وقتی
* breakdown قابل فهم باشد.
* حالت نبود داده و بازه تاریخ وجود داشته باشد.
* نمودار در صورت استفاده lazy load شود.
---
## [ ] FE-076 — نمودار سبک گزارش سود
### هدف
نمایش روند سود بدون آسیب به بودجه bundle.
### قواعد
* Apache ECharts به‌صورت lazy load
* فقط در صفحه گزارش
* عدم ورود به bundle اولیه
* احترام به reduced motion
* tooltip لمسی
* fallback متنی
### تمام است وقتی
* bundle اولیه از بودجه عبور نکند.
* نمودار روی موبایل قابل استفاده باشد.
---
# Milestone 18 — PDF و اشتراک
## [ ] FE-077 — دریافت PDF فاکتور
### وابستگی بک‌اند
```text
BE-062
```
### قابلیت‌ها
* دانلود
* preview
* loading
* error
* نام فایل مناسب
* مجوز دسترسی
### تمام است وقتی
* PDF صحیح سند جاری دریافت شود.
* فایل tenant دیگر قابل دسترسی نباشد.
---
## [ ] FE-078 — دریافت PDF خرید و صورت‌حساب
### موارد
* رسید خرید دست‌دوم
* صورت‌حساب شخص
### تمام است وقتی
* actionها در صفحه مرتبط قرار بگیرند.
* موبایل دانلود یا share را پشتیبانی کند.
---
## [ ] FE-079 — اشتراک‌گذاری رسید
### قابلیت‌ها
* Web Share API در صورت پشتیبانی
* fallback دانلود
* متن کوتاه
* عدم اشتراک URL خصوصی بدون مجوز
### تمام است وقتی
* روی Android واقعی تست شود.
* fallback دسکتاپ کار کند.
---
# Milestone 19 — تنظیمات فاز ۱
## [ ] FE-080 — صفحه تنظیمات عمومی tenant
### تنظیمات
* عیار پیش‌فرض خرید دست‌دوم
* واحد گرد کردن
* سیاست گرد کردن
* عیار نمایش گزارش
* پنجره اصلاح فاکتور
* حد اختلاف نیازمند مدیر
### قواعد
* مقادیر از API بیایند.
* تغییر نسخه جدید backend بسازد.
* مقدار قدیمی تاریخچه معاملات را عوض نکند.
* فقط نقش مجاز.
---
## [ ] FE-081 — تنظیمات انواع سکه
### قابلیت‌ها
* فهرست نوع
* وزن
* عیار
* بانک مرکزی بودن
* فعال یا غیرفعال
* تاریخ اعتبار
* ایجاد نسخه جدید
### قواعد
* هیچ مقدار پیش‌فرض در UI هاردکد نشود.
* حباب فقط برای `isCentralBankMinted`.
---
## [ ] FE-082 — تنظیمات مظنه و منابع
### قابلیت‌ها
* نمایش منبع فعال
* وضعیت فید
* ورود دستی
* fallback
* عدم وابستگی فروش به اتصال فید
---
# Milestone 20 — دسترس‌پذیری و تجربه کاربری
## [ ] FE-083 — ممیزی RTL
### موارد
* logical spacing
* آیکون‌های جهت‌دار
* breadcrumb
* drawer
* table
* chart
* شماره‌ها
* mixed-direction text
### تمام است وقتی
* هیچ کلاس `ml-`, `mr-`, `pl-`, `pr-`, `left-`, `right-` غیرضروری وجود نداشته باشد.
* متن و اعداد mixed direction درست باشند.
---
## [ ] FE-084 — ممیزی دسترس‌پذیری
### موارد
* label
* aria
* focus
* keyboard
* contrast
* target size
* dialog
* bottom sheet
* toast
* error association
* reduced motion
### تمام است وقتی
* axe روی صفحات اصلی خطای جدی نداشته باشد.
* flow فروش با کیبورد قابل انجام باشد.
---
## [ ] FE-085 — ممیزی موبایل ۳۶۰px
### صفحات اجباری
* login
* dashboard
* parties
* inventory
* new sale
* new purchase
* settlement
* invoice
* reports
### معیارها
* بدون overflow افقی
* action اصلی در دسترس
* کیپد مناسب
* Bottom Sheet صحیح
* table حذف‌شده
---
## [ ] FE-086 — تست دستگاه Android واقعی
### دستگاه
حداقل یک Android میان‌رده.
### سناریوها
* login
* ثبت مظنه
* ایجاد شخص
* فروش زیورآلات
* فروش سکه
* خرید دست‌دوم
* تسویه
* مشاهده گزارش
* share PDF
### خروجی
```text
docs/android-device-test.md
```
### ثبت شود
* مدل دستگاه
* نسخه Android
* مرورگر
* مشکلات
* زمان ثبت معامله
* INP تقریبی
* رفتار کیبورد و Bottom Sheet
---
# Milestone 21 — Performance و PWA
## [ ] FE-087 — کنترل Bundle Budget
### هدف
حفظ bundle اولیه زیر ۲۰۰KB فشرده.
### کارها
* bundle analyzer
* route splitting
* lazy load chart
* lazy load PDF preview
* حذف dependency تکراری
* جلوگیری از import کل کتابخانه‌ها
### تمام است وقتی
* گزارش اندازه در CI ثبت شود.
* عبور از بودجه build را fail کند.
---
## [ ] FE-088 — بهینه‌سازی LCP
### هدف
LCP روی 3G شبیه‌سازی‌شده زیر ۲.۵ ثانیه.
### کارها
* font preload مناسب
* asset optimization
* حذف render-blocking
* skeleton
* route chunk
* جلوگیری از درخواست‌های غیرضروری startup
### محدودیت‌ها
* مهاجرت به SSR بدون تصمیم معماری جداگانه انجام نشود.
* قابلیت‌های اصلی قربانی عدد مصنوعی نشوند.
---
## [ ] FE-089 — بهینه‌سازی INP
### هدف
INP زیر ۲۰۰ms.
### تمرکز
* کیپد
* فرم فروش
* لیست‌های بزرگ
* جست‌وجو
* mutationها
* chart
### تمام است وقتی
* interactionهای اصلی profile شده باشند.
* handler سنگین یا rerender غیرضروری رفع شده باشد.
---
## [ ] FE-090 — PWA پایه فاز ۱
### قابلیت‌ها
* manifest
* installable
* icon
* app shell cache
* update notification
* offline fallback فقط برای shell
### قواعد
* ثبت معامله آفلاین ساخته نشود.
* queue آفلاین ساخته نشود.
* شماره فاکتور سمت کلاینت صادر نشود.
* فرم آفلاین سند رسمی محسوب نشود.
### تمام است وقتی
* install روی Android کار کند.
* قطع اینترنت پیام واضح بدهد.
* عملیات مالی آفلاین غیرفعال باشد.
---
# Milestone 22 — تست‌ها
## [ ] FE-091 — تست utilityهای عددی
### پوشش
* BigInt string
* فرمت ریال
* فرمت وزن
* تبدیل گرم
* ارقام فارسی
* مقدار بسیار بزرگ
* مقدار منفی
* صفر
---
## [ ] FE-092 — تست componentهای فرم مالی
### componentها
* MoneyInput
* WeightInput
* KaratInput
* NumericKeypad
* UnitToggle
* SettlementLine
### معیار
* عدم precision loss
* رفتار keyboard
* خطا
* disabled
* readonly
* paste
---
## [ ] FE-093 — Mock Server قراردادمحور
### هدف
امکان توسعه فرانت پیش از تکمیل endpointها.
### کارها
* MSW
* response مطابق `packages/contracts`
* سناریوی موفق
* validation error
* conflict
* forbidden
* network error
### قواعد
* mock shape مستقل و ساختگی نباشد.
* با تغییر contract تست fail شود.
* mock مالی به production bundle وارد نشود.
---
## [ ] FE-094 — تست Integration فروش
### سناریو
* مظنه
* شخص
* کالا
* محاسبه
* پرداخت
* submit
* receipt
### تمام است وقتی
* request payload دقیق بررسی شود.
* Idempotency-Key بررسی شود.
* پاسخ server-authoritative نمایش داده شود.
---
## [ ] FE-095 — تست Integration خرید دست‌دوم
### سناریو
* شخص
* وزن
* کسورات
* عیار
* مبلغ
* submit
* receipt
---
## [ ] FE-096 — تست Integration تسویه ترکیبی
### سناریو
* مانده شخص
* ریال
* طلا
* سکه
* preview
* submit
* مانده جدید
---
## [ ] FE-097 — تست Integration اصلاح فاکتور
### سناریو
* باز کردن فاکتور
* دریافت policy
* تغییر وزن
* دلیل
* مرور اختلاف
* submit
* مشاهده نسخه جدید
---
## [ ] FE-098 — تست E2E مسیر پایلوت
### سناریوی کامل
1. ورود
2. ثبت مظنه
3. ایجاد شخص
4. ایجاد کالای زیورآلات
5. ثبت موجودی افتتاحیه
6. فروش نقدی زیورآلات
7. فروش نسیه سکه
8. تسویه بخشی با ریال
9. تسویه بخشی با طلا
10. خرید طلای دست‌دوم
11. Buyback B2C
12. اصلاح فاکتور
13. مشاهده مانده
14. مشاهده صورت‌حساب
15. مشاهده بدهکاران
16. مشاهده سود
17. دریافت PDF
### تمام است وقتی
* flow روی viewport 360px سبز باشد.
* flow روی desktop سبز باشد.
* screenshotهای حساس بررسی شوند.
* هیچ request مالی تکراری ایجاد نشود.
---
# Milestone 23 — سخت‌سازی نهایی
## [ ] FE-099 — مدیریت Unsaved Changes
### صفحات
* فروش
* خرید
* تسویه
* اصلاح فاکتور
* تنظیمات
### رفتار
* هشدار خروج
* جلوگیری از از دست رفتن ناخواسته
* عدم ذخیره خودکار به‌عنوان سند رسمی
* پاک‌سازی پس از ثبت موفق
---
## [ ] FE-100 — مدیریت Double Submit
### موارد
* تمام mutationهای مالی
* دکمه
* Enter
* tap سریع
* retry شبکه
* back/forward
### تمام است وقتی
* تست نشان دهد یک عملیات منطقی فقط یک سند می‌سازد.
* Idempotency مبنا باشد، نه صرفاً disabled button.
---
## [ ] FE-101 — امنیت نمایش داده
### موارد
* token در log
* کد ملی
* stack trace
* request body مالی
* URLهای خصوصی PDF
* DevTools logging production
* cache اطلاعات tenant قبل
### تمام است وقتی
* logout cache tenant را پاک کند.
* ورود tenant دیگر داده قبلی را نشان ندهد.
---
## [ ] FE-102 — Tenant Switching Safety
### هدف
در صورت پشتیبانی backend از چند tenant برای یک user.
### رفتار
* پاک‌سازی query cache
* پاک‌سازی draft
* تغییر context
* route امن
* جلوگیری از نمایش داده tenant قبلی
### تمام است وقتی
* هیچ flash داده tenant قبلی رخ ندهد.
---
## [ ] FE-103 — بررسی همه صفحات مالی برای Unit Toggle
### چک‌لیست
* داشبورد
* مانده شخص
* صورت‌حساب
* بدهکاران
* بستانکاران
* سود
* خلاصه فروش
* خلاصه خرید
* تسویه
* کارت‌های درآمد و پرداخت
### تمام است وقتی
* هیچ صفحه مالی بدون Unit Toggle باقی نماند.
* انتخاب سراسری در همه صفحات یکسان باشد.
---
## [ ] FE-104 — بررسی عدم استفاده از نرخ امروز در تاریخچه
### صفحات
* فاکتور
* نسخه فاکتور
* خرید دست‌دوم
* Buyback
* صورت‌حساب
* سود
* رسید
### معیار
هر مقدار تاریخی باید از snapshot همان سند استفاده کند.
### تمام است وقتی
* تغییر مظنه جاری هیچ سند تاریخی را تغییر ندهد.
* تست regression وجود داشته باشد.
---
## [ ] FE-105 — بررسی قانون سکه
### معیارها
* سکه در state به وزن تبدیل نشده باشد.
* count integer باشد.
* انواع مستقل باشند.
* حباب فقط بانک مرکزی.
* گزارش حباب شمش وجود نداشته باشد.
* شمش اصلاً در UI فاز ۱ وجود نداشته باشد.
---
## [ ] FE-106 — حذف Mockهای عملیاتی
### هدف
جایگزینی mockهای اصلی با API واقعی.
### موارد
* auth
* pricing
* parties
* inventory
* sales
* purchase
* settlement
* reporting
### قواعد
* MSW فقط برای test و development scenario باقی بماند.
* production به mock fallback نکند.
---
## [ ] FE-107 — مستندات معماری فرانت
### فایل
```text
docs/frontend-architecture.md
```
### محتوا
* ساختار Feature
* API client
* contracts
* BigInt handling
* unit display
* forms
* idempotency
* auth
* error handling
* routing
* testing
* performance
* PWA limitations
---
## [ ] FE-108 — راهنمای اجرای فرانت
### فایل
```text
apps/web/README.md
```
### محتوا
* پیش‌نیازها
* env
* dev
* build
* tests
* mock mode
* اتصال API
* Android test
* bundle analysis
* مشکلات متداول
---
## [ ] FE-109 — Gate نهایی فرانت
### دستور
```bash
pnpm gate:frontend
```
### باید سبز باشد
* TypeScript
* ESLint
* unit tests
* integration tests
* E2E critical path
* build
* bundle budget
* accessibility smoke test
* ممنوعیت float مالی
* ممنوعیت direction classes
* ممنوعیت import از backend
* قراردادهای API
---
## [ ] FE-110 — تست پذیرش فاز ۱
### معیار اصلی
یک خرده‌فروش بتواند یک هفته همه معاملات فاز ۱ را در سیستم ثبت کند.
### سناریوهای پذیرش
* ورود
* مظنه دستی
* مشتری
* زیورآلات
* سکه
* موجودی
* فروش نقدی
* فروش نسیه ساده
* پرداخت ریالی
* پرداخت با طلا
* پرداخت با سکه
* پرداخت ترکیبی
* خرید طلای دست‌دوم
* خرید سکه
* Buyback B2C
* اصلاح فاکتور
* مانده اشخاص
* صورت‌حساب
* بدهکاران
* بستانکاران
* داشبورد
* سود
* PDF
### معیار تجربه
* ثبت معامله معمولی زیر ۱۵ ثانیه
* بدون overflow در ۳۶۰px
* بدون خطای precision
* بدون سند تکراری
* بدون نمایش نرخ امروز روی سند قدیمی
* بدون صفحه مالی فاقد Unit Toggle
* بدون حباب برای موجودیت غیرمجاز
* بدون ویژگی خارج فاز ۱
---
# ترتیب هم‌راستایی با بک‌اند
| مرحله فرانت            | وابستگی بک‌اند   |
| ---------------------- | ---------------- |
| API Client و Contracts | BE-003 تا BE-017 |
| Auth                   | BE-011 تا BE-012 |
| مظنه                   | BE-021 تا BE-022 |
| اشخاص                  | BE-023 تا BE-024 |
| موجودی                 | BE-025 تا BE-028 |
| فروش نقدی زیورآلات     | BE-038 تا BE-041 |
| فروش نسیه و سکه        | BE-042 تا BE-043 |
| تسویه                  | BE-044 تا BE-048 |
| خرید دست‌دوم           | BE-049 تا BE-051 |
| Buyback B2C            | BE-052           |
| اصلاح فاکتور           | BE-053 تا BE-055 |
| حساب اشخاص             | BE-056 تا BE-057 |
| گزارش‌ها               | BE-058 تا BE-060 |
| PDF                    | BE-061 تا BE-062 |
---
# خارج از دامنه
این موارد در فرانت فاز ۱ ساخته نشوند:
* مرجوعی B2B
* بازاریاب
* پورسانت
* آفلاین تراکنشی
* صف sync
* چندشعبه‌ای
* امانی
* نقره
* شمش
* کارخانه
* تولیدی
* شبکه تسویه
* سامانه مؤدیان
* باشگاه مشتریان
* RFID
* اتصال ترازو
* چک پیچیده
* اقساط پیچیده
* اپ native
* نسخه دسکتاپ مستقل
* GraphQL client
* WebSocket غیرضروری
* داشبورد نموداری سنگین
---
# Definition of Done عمومی فرانت
هر تسک فقط وقتی تمام است که:
* [ ] معیارهای همان تسک انجام شده باشند.
* [ ] فایل design system مرتبط خوانده شده باشد.
* [ ] `pnpm --filter web typecheck` سبز باشد.
* [ ] `pnpm --filter web lint` سبز باشد.
* [ ] `pnpm --filter web test` سبز باشد.
* [ ] build فرانت سبز باشد.
* [ ] هیچ `any` جدیدی اضافه نشده باشد.
* [ ] هیچ `@ts-ignore` جدیدی اضافه نشده باشد.
* [ ] هیچ `Math.round` جدیدی اضافه نشده باشد.
* [ ] هیچ `parseFloat` جدیدی اضافه نشده باشد.
* [ ] هیچ `toFixed` جدیدی اضافه نشده باشد.
* [ ] پول و وزن با `number` نگهداری نشده باشند.
* [ ] مقدار مالی API به‌صورت string باشد.
* [ ] RTL با logical properties رعایت شده باشد.
* [ ] عرض ۳۶۰px بررسی شده باشد.
* [ ] target لمسی ۴۴×۴۴ رعایت شده باشد.
* [ ] focus state وجود داشته باشد.
* [ ] prefers-reduced-motion رعایت شده باشد.
* [ ] loading، empty و error state وجود داشته باشد.
* [ ] عملیات نوشتنی Idempotency-Key داشته باشد.
* [ ] فایل‌های خارج از دامنه تغییر نکرده باشند.
* [ ] چک‌باکس تسک تیک خورده باشد.
* [ ] یک کامیت با شناسه همان تسک ساخته شده باشد.
add md file named front tasks into my projects
